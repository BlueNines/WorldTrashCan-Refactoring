# 语言文件名。
# 默认使用 messages/message_zh.yml；后续可切换到其他语言文件。
language: "message_zh.yml"

# 是否开启调试日志。
# 默认 false；开启后会输出配置加载、功能启停、清理决策等额外信息。
debug: false

# 是否允许旧 WorldListTrashCan 配置迁移。
# 默认 true；首次检测到旧 config.yml 和 data/data.yml 时会生成迁移报告。
migration-enabled: true
