# BLWorldTrashCan 长期硬化缺口清单

本文记录重构版后续继续修复、补测和交付前需要追踪的缺口。它不是历史流水账，而是当前 goal 的工作队列。

## 当前基线

- 仓库：`C:\Users\pc\Desktop\ai开发插件\待重构插件\WorldListTrashCan重构\refactor-workspace`
- 上一历史基线提交：`2442715 fix density remove-count semantics`
- 当前 universal jar：`dist/BLWorldTrashCan-universal.jar`
- 当前 universal jar SHA256：`68d55cdf277bb3ef30129ef75386370a0dfa0063590ce6c8f6db54b522ebdab1`
- 基线构建：`build\tools\apache-maven-3.9.9\bin\mvn.cmd package` 通过

## 执行规则

- 后续测试默认只部署 `dist/BLWorldTrashCan-universal.jar`，除非明确要验证轻量特供 jar。
- 玩家可见功能必须保留真实客户端截图或等价客户端侧证据，服务端日志只作为辅助排障。
- 不删除测试服 `logs`、`world*`、`cache`、`assets`。
- 每个独立修复完成后都要更新 README、矩阵或本清单，并提交、推送到当前插件仓库。
- 需要重新打包时，先运行 `build\tools\apache-maven-3.9.9\bin\mvn.cmd -q clean package`，再运行 `py -3 tools\rgb-visual-matrix\sync_dist_jars.py`，确保后续测试默认使用的 `dist` 不是旧包。
- 同步 `dist` 后必须运行 `py -3 tools\rgb-visual-matrix\check_dist_package_integrity.py`，确认交付 jar 的 `plugin.yml`、主类、默认资源、bStats、PrismaticAPI 重定位、新旧核心权限声明和 universal 四平台实现没有退化。

## 当前优先缺口

| 优先级 | 缺口 | 当前状态 | 验收标准 |
| --- | --- | --- | --- |
| P0 | 完整矩阵 SKIP 收敛 | 2026-06-08 当轮通用专项 `SKIP` 已全部由后续专项收敛为 PASS；当前无未收敛通用专项项 | 后续新增功能或发现回退时，必须先把功能 ID 写回矩阵，再补对应专项证据 |
| P1 | Evidence 管理防退化 | 2026-07-02 已扫描 README、新增功能说明、完整功能矩阵、长期硬化缺口清单和重构执行记录；当前 71 个上层证据引用均存在且有 Git 跟踪内容。本轮 `check_test_evidence_index.py` 自动审计最终证据、失败对照、上层文档引用和全局备份 jar 跟踪状态。历史未跟踪目录仍按本地缓存保留，备份 jar 按本地恢复缓存排除 | 后续新增证据必须同步索引或上层文档；提交前运行 `py -3 tools/rgb-visual-matrix/check_test_evidence_index.py`，未跟踪缓存不混入通过结论，备份 jar 只记录 SHA256 不提交 |

截至 2026-07-02，本清单没有未实现旧功能或已知代码级 P0/P1/P2 缺口。后续如果用户提出新需求、验收发现回退或出现新的性能/兼容问题，必须先写回本表或完整功能矩阵，再按修复、构建、真实验收、证据归档、提交推送闭环处理。

## 已收敛缺口

| 日期 | 缺口 | 结果 | 证据 |
| --- | --- | --- | --- |
| 2026-06-30 | Folia 清理通知视觉验收 | 已用真实客户端覆盖 Chat、ActionBar、BossBar、Title；Sound 通过客户端字幕辅助证明触发；Command 通过服务端输出证明执行。仍不声明整产物完全 `FOLIA_REGION_SAFE`，因为服主可配置任意 Command。 | `docs/test-evidence/cleanup-notify-visual-20260630-092840/` |
| 2026-07-02 | GUI 真实点击链路 | Spigot 26.1.2 managed、Paper 1.12.2 managed universal、Folia 1.21.8 三端真实客户端 + universal jar 覆盖公共/个人垃圾桶取放、公共分页、公共取出冷却、公共操作日志、公共黑名单保存并立即生效、个人满桶自动清空，全部 PASS。1.12.2 暴露的黑名单 GUI 上下文丢失已通过 `InventoryHolder` 绑定上下文修复。 | `docs/test-evidence/trash-gui-click-visual-20260702-223605/` |
| 2026-07-02 | GUI 取放权限 deny 分支 | 新增 `run_gui_operation_permission_visual_matrix.py`，在玩家已能打开公共/个人垃圾桶 GUI 的前提下，用临时 `PermissionDenyFixture` 显式 deny 公共取出、公共放入、个人取出、个人放入权限；Paper 1.12.2 managed、Spigot 26.1.2 managed、Folia 1.21.8 三端真实客户端共 12 项均 PASS。 | `docs/test-evidence/gui-operation-permission-visual-20260702-231000/` |
| 2026-07-01 | 文档一致性 | README 和新增功能说明已统一描述 Folia/密集实体当前低占用分片扫描、chunk 索引和跨 chunk 计算；历史执行记录中的“只扫描当前 chunk”保留为旧实现记录，不再作为当前行为。 | `README.md`、`docs/重构版新增功能说明.md` |
| 2026-07-01 | 多语言切换与缺节点回退 | 修复旧外部英文语言文件带旧调试 help 时退回中文主帮助的问题；Paper 1.12.2 managed、Spigot 26.1.2 managed、Folia 1.21.8 三端真实客户端验证 `message_en.yml` 切换和外部 `message_zh.yml` 缺 `command.help` 后的 jar 内默认回退，全部 PASS。 | `docs/test-evidence/language-visual-20260701-163536/` |
| 2026-07-01 | 旧配置迁移 F-005 | 新增 `run_legacy_migration_matrix.py`，用 Paper 1.12.2 独立临时测试服和 `dist/BLWorldTrashCan-universal.jar` 覆盖相邻旧目录 `plugins/WorldListTrashCan`、当前目录旧结构 `plugins/BLWorldTrashCan` 两条入口；RCON 证明 universal 正常加载为 `legacy-1.12`，迁移报告、新拆分配置、世界垃圾桶数据和当前目录备份全部通过断言。 | `docs/test-evidence/legacy-migration-universal-20260701-165606/` |
| 2026-07-02 | 实体清理总开关 F-048/F-049/F-050/F-052/F-056 | 新增 `run_entity_cleanup_toggle_matrix.py`，用临时 Bukkit 夹具在 Paper 1.12.2 与 Spigot 26.1.2 生成牛、僵尸、箭、经验球和黑名单命名实体；`entities.enabled=false` 时正式 `/blwtc clear true` 移除实体为 0 且 5 个目标全部保留，切回 `entities.enabled=true` 后同批实体全部清理。 | `docs/test-evidence/entity-cleanup-toggle-20260702-212255/` |
| 2026-07-01 | 船内实体保护 F-054 | 新增 `run_boat_entity_protection_matrix.py`，用临时 Bukkit 夹具在 Paper 1.12.2 与 Spigot 26.1.2 生成船内牛和普通牛；正式 `/blwtc clear true` 后船内牛仍存在且仍在船内，普通牛被清理，证明 `ignore-entities-in-boat` 在 legacy 与现代 universal 分支均生效。 | `docs/test-evidence/boat-entity-protection-20260701-171046/` |
| 2026-07-01 | 世界垃圾桶边界 F-019-F-022 | 新增 `run_world_trash_boundary_matrix.py`，用临时 Bukkit 夹具在 Paper 1.12.2 与 Spigot 26.1.2 触发正式创建、破坏和清理路径；禁止世界创建、破坏移除登记、世界黑名单降级、未加载区块跳过并降级到公共垃圾桶均 PASS。 | `docs/test-evidence/world-trash-boundary-20260701-172919/` |
| 2026-07-01 | 保护边界 F-068/F-069 | 修复不可拾取箭矢判断直连旧 `Arrow.PickupStatus` 的兼容问题，改为反射兼容 1.12 `Arrow.PickupStatus` 与 1.20+ `AbstractArrow.PickupStatus`；新增 `run_protection_boundary_matrix.py`，Paper 1.12.2 与 Spigot 26.1.2 均验证不可拾取箭矢、骷髅/无限弓追踪箭矢命中后移除，实体和玩家踩踏农田事件均取消。 | `docs/test-evidence/protection-boundary-20260701-174709/` |
| 2026-07-01 | 世界实体上限 F-070/F-072 | 新增 `run_world_entity_limit_matrix.py`，Paper 1.12.2 与 Spigot 26.1.2 均验证低占用索引建立后 COW 超量生成被正式路径拦截；同一世界写入 `ignored-worlds` 后扫描选择为 `0/0` 且生成放行。 | `docs/test-evidence/world-entity-limit-20260701-180527/` |
| 2026-07-01 | 清理通知 Chat 点击 F-058 | 修复富文本点击事件只挂顶层组件的问题，递归覆盖 Bungee 组件树；Folia 清理通知改用 Adventure 原生可点击组件发送；点击专项脚本改为截图像素定位通知文本并用窗口消息稳定打开聊天。Paper 1.12.2、Spigot 26.1.2、Folia 1.21.8 三端真实客户端均点击 `AI_CLICK_NOTIFY_0` 后触发 `/blwtc stats`，客户端日志出现“清理统计”和“公共垃圾桶”。 | `docs/test-evidence/cleanup-notify-click-20260701-190056/` |
| 2026-07-02 | Vault 扣费 F-037 | 修复 universal 反射 VaultAPI 时查找 `withdrawPlayer(Player,double)` 的签名错误，改为 `withdrawPlayer(OfflinePlayer,double)`；个人垃圾桶取出现在先确认玩家主背包存储槽可完整接收物品，再扣费。Spigot 26.1.2 managed + 真实 26.1.2 客户端 + 临时 fake Vault/Economy 覆盖余额充足、余额不足、背包满三场景，全部 PASS。 | `docs/test-evidence/vault-payment-visual-20260702-200346/` |
| 2026-07-02 | Folia PAPI | 使用 `E:\server_work\folia1.21.8`、`[PAPI]PlaceholderAPI-2.11.7-DEV-null (1).jar` 和最终 universal jar 验证 `%Wtc_ClearTime%`；PlaceholderAPI 正常加载，`Wtc` expansion 注册成功，`papi parse --null %Wtc_ClearTime%` 返回 `358`。旧 PlaceholderAPI 2.11.6 仍不能作为 Folia 前置。 | `docs/test-evidence/folia-papi-20260702-202801/` |
| 2026-07-02 | 权限负向真实客户端 F-016 | 新增 `run_permission_negative_visual_matrix.py`，临时编译 `PermissionDenyFixture` 给真实在线玩家挂 false 权限附件；Paper 1.12.2 managed、Spigot 26.1.2 managed、Folia 1.21.8 三端均验证非 OP `/blwtc reload` 被拒绝，显式 deny `global.open`/`personal.open` 后 `/blwtc global` 与 `/blwtc personal` 被拒绝，OP 后 `/blwtc reload` 成功。 | `docs/test-evidence/permission-negative-visual-20260702-210300/` |
| 2026-07-02 | 主动源码风险审计 | 已扫描源码中的 TODO/FIXME、吞异常、直接调度、异步 worker、实体/chunk 访问和反射兼容点；吞异常点均为配置降级或旧 API 兼容，普通 Bukkit 清理门禁默认 150 阈值下仍是单趟延迟清理路径，Folia 清理受 chunk 上限、批量、延迟和超时保护。`mvn test` 通过，当前未发现需要立刻修复的新代码缺口。 | `build\tools\apache-maven-3.9.9\bin\mvn.cmd -q test` |
| 2026-07-02 | Evidence 索引自动审计 | 新增 `tools/rgb-visual-matrix/check_test_evidence_index.py`，自动解析 `docs/test-evidence/README.md` 的最终证据和失败对照表，检查目录存在、Git 跟踪、README、机器摘要、上层文档引用以及已跟踪备份 jar；同步把 Folia PAPI 三个本地恢复 jar 从 Git 索引移除但保留本地文件。当前 17 个最终证据、17 个失败对照、71 个上层文档证据引用全部通过审计。 | `py -3 tools\rgb-visual-matrix\check_test_evidence_index.py` |
| 2026-07-02 | 默认配置中文注释审计 | 新增 `tools/rgb-visual-matrix/check_resource_yaml_comments.py`，扫描四个平台交付默认配置资源和现有 `dist` jar 内资源，不扫描语言包、`plugin.yml` 和历史 evidence 缓存；已补齐 `entity-limits.yml` 示例列表中 `max-count`、`radius`、`remove-count` 的中文说明。当前 28 个源码配置资源、5 个 dist jar、35 个包内配置资源、1143 个 YAML 键全部通过。 | `py -3 tools\rgb-visual-matrix\check_resource_yaml_comments.py` |
| 2026-07-02 | dist 产物同步脚本 | 新增 `tools/rgb-visual-matrix/sync_dist_jars.py`，把五个平台 Maven target jar 复制到固定 `dist/BLWorldTrashCan-*.jar` 文件名，并输出 SHA256；当前 dry-run 和实际同步均为 `changed: 0`，说明 dist 已与 target 一致。 | `py -3 tools\rgb-visual-matrix\sync_dist_jars.py --dry-run` |
| 2026-07-02 | dist 交付包完整性审计 | 新增 `tools/rgb-visual-matrix/check_dist_package_integrity.py`，检查五个交付 jar 的 `plugin.yml` 版本和主类、Java class major、默认资源、bStats 类、PrismaticAPI 重定位、新旧核心权限声明，以及 `BLWorldTrashCan-universal.jar` 是否包含 Legacy、Bukkit、Paper、Folia 四个平台实现。当前 5 个 dist jar 全部 `errors: 0`。 | `py -3 tools\rgb-visual-matrix\check_dist_package_integrity.py` |

## 已确认但需要避免回退的行为

- `/wtc clear`、`/blwtc clear` 默认等价 `true`，会忽略 guards；传入 `false` 时才遵守 guards。
- `guards.min-total-entities` 默认值是 `150`。
- `global-trash.clear-every-cleanups` 触发时必须先清空旧公共垃圾桶，再收集本轮物品。
- 新版本物品入桶必须保留原物品，不允许因 legacy material 映射或日志异常丢失。
- 密集实体 `remove-count` 表示触发后本轮最多清理多少只，不是只清理超出上限的差值。
- 密集实体候选队列是一次性消费队列，不是长期状态表。
- 公共黑名单 GUI 使用 token 点击模型：点击玩家背包物品复制 Material token，不消耗物品；点击上方 token 删除；关闭 GUI 后保存并刷新运行期配置。
- 外部语言文件缺节点时优先回退当前语言的 jar 内置节点，再回退默认中文；旧外部语言主帮助包含调试命令时也必须使用当前语言的 jar 内置主帮助。
- Vault 扣费只用于个人垃圾桶取出；必须先确认物品能完整进入玩家主背包存储槽，再调用 Economy 扣费，余额不足或背包满都不能产生扣款。
- Evidence 目录中的备份 jar 只作为本地恢复缓存，不提交到 Git；最终证据只记录被测 jar 文件名、版本和 SHA256。
- 新增或调整证据目录后，必须运行 evidence 索引审计脚本，防止最终证据误指向 FAIL 目录、失败对照没有说明原因、备份 jar 被重新纳入 Git。
- 新增或调整默认配置项后，必须运行默认配置中文注释审计脚本，防止交付资源出现无中文说明的配置项。
- 构建后必须同步 dist，再跑使用 `dist` 的验证脚本；不能把 target 已更新、dist 仍是旧包的状态写成已验收。
- 同步后的 dist 必须通过包完整性审计；尤其不能回退 `plugin.yml version: 7.0.0`、bStats `SERVICE_ID = 24350`、PrismaticAPI 重定位、新旧核心权限声明和 universal 四平台实现类。
