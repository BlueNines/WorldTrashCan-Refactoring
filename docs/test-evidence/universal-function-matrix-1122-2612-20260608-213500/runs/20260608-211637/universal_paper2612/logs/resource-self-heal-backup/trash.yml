# 世界垃圾桶设置。
world-trash:
  # 是否启用世界垃圾桶。
  enabled: true
  # 玩家创建世界垃圾桶时告示牌需要填写的文本。
  sign-create-text: "[世界垃圾桶]"
  # 创建成功后告示牌显示的文本。
  sign-created-text: "&b[&c世界垃圾桶&b]"
  # 普通玩家每个世界默认可创建的垃圾桶数量。
  default-max-count: 3
  # 是否允许清理任务写入未加载区块里的世界垃圾桶。
  # 默认 false：清理时只访问已加载区块里的世界垃圾桶，未加载位置会跳过并降级到个人或公共垃圾桶，避免同步加载区块造成卡顿。
  # 改为 true：会尽量保持旧插件行为，但可能在后台清理时同步加载远处区块，垃圾桶多或磁盘慢时会明显影响性能。
  allow-load-unloaded-chunks: false
  # 禁止创建世界垃圾桶的世界名。
  banned-worlds:
    - "world"
    - "DIM-1"
    - "DIM1"

# 公共垃圾桶设置。
global-trash:
  # 是否启用公共垃圾桶。
  enabled: true
  # 最大页数。
  max-pages: 5
  # 拿取冷却，单位毫秒。
  take-delay-millis: 500
  # 是否允许玩家把背包物品手动放入公共垃圾桶。
  allow-player-put: true
  # 每多少次清理后刷新公共垃圾桶；-1 表示永不自动刷新。
  clear-every-cleanups: 3
  # 是否记录玩家从公共垃圾桶拿取、放入物品的日志。
  log-enabled: true
  # 公共垃圾桶 GUI 外观设置。
  gui:
    # 上一页按钮的 CustomModelData；-1 表示不设置，低版本会自动忽略该外观字段。
    back-model-id: -1
    # 下一页按钮的 CustomModelData；-1 表示不设置，低版本会自动忽略该外观字段。
    next-model-id: -1
    # 背景按钮的 CustomModelData；-1 表示不设置，低版本会自动忽略该外观字段。
    background-model-id: -1
  # 不允许进入公共垃圾桶的物品类型。
  banned-materials:
    - "EGG"
    - "DIRT"

# 个人垃圾桶设置。
personal-trash:
  # 是否启用个人垃圾桶。
  enabled: true
  # 是否追踪玩家主动丢弃物品。
  # paper-1.16-1.20 支持 PDC，因此该项可完整生效。
  track-player-dropped-items: true
  # 个人垃圾桶满时是否自动清空旧内容，再尝试放入新物品。
  auto-clear-when-full: true
  # 玩家从个人垃圾桶取出物品时扣除的 Vault 金额；-1 表示不扣费。
  take-cost: -1
  # 物品自动进入个人垃圾桶时的在线提示设置。
  notify:
    # 是否在垃圾进入个人垃圾桶时提示在线玩家。
    enabled: true
    # 批量提示中最多完整展示多少个物品条目；超过后会按语言文件里的 ellipsis 追加省略标记。
    max-display-items: 3
  # 玩家主动丢弃的物品被仙人掌、岩浆、火焰等损坏时，是否在短时间内回收到垃圾桶。
  damage-recovery:
    # 回收模式。
    # disabled：不处理，等同旧配置 UseModel=3。
    # global-trash：进入公共垃圾桶，等同旧配置 UseModel=1。
    # personal-trash：进入玩家个人垃圾桶，等同旧配置 UseModel=2。
    mode: personal-trash
    # 玩家丢出物品后多少秒内被损坏才会尝试回收，过期后不再记录归属，避免长期占用内存。
    delay-seconds: 6
