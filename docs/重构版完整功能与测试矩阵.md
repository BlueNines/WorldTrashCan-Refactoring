# WorldListTrashCan 重构版完整功能与测试矩阵

本文是重构版功能验收的总清单。后续测试必须只部署 `dist/WorldListTrashCan-universal.jar`，每个目标端部署前先移除 `plugins` 目录内其它 `WorldListTrashCan*.jar`，并在证据里记录实际 jar 文件名、大小、SHA256 和 jar 内 `plugin.yml`。

## 测试端

本轮完整回归目标：

- `E:\server_work\paper-1.12.2-universal-test-server`，真实 1.12.2 客户端。
- `E:\server_work\paper-26.1.2-test-server`，真实 26.1.2 客户端。
- `E:\server_work\spigot-26.1.2-test-server`，真实 26.1.2 客户端。

不能只用服务端日志判定玩家可见功能。GUI、聊天栏、ActionBar、Title、玩家命令、个人垃圾桶提示等玩家可见链路必须保留真实客户端截图或客户端侧证据；服务端日志只作为辅助排障。

## 功能清单

| ID | 功能域 | 功能 | 默认状态 | 验收方式 |
| --- | --- | --- | --- | --- |
| F-001 | 交付与启动 | universal 整包可在 1.12.2、Paper 26.1.2、Spigot 26.1.2 加载 | 启用 | 启动日志、`plugins`、jar SHA256、`plugin.yml` |
| F-002 | 交付与启动 | universal 运行时正确识别 `legacy-1.12` 或 `paper-1.16-1.20` | 启用 | `/wtc platform` 输出 |
| F-003 | 交付与启动 | Java 8 bootstrap 不提前加载高版本 Folia 类 | 启用 | 1.12.2 启动和 platform smoke |
| F-004 | 交付与启动 | 默认资源文件缺失时可自愈补回 | 启用 | 删除配置/语言资源后 `/wtc reload` |
| F-005 | 交付与启动 | 同名旧配置完整隔离到 `old-version-config` 并迁移为新拆分配置 | 启用 | 首迁、报告、完成标记、中断重试、标记跳过和旧结构回放拒绝 |
| F-006 | 命令 | 正式长命令 `/worldlisttrashcan` | 启用 | 控制台与真实玩家命令 |
| F-007 | 命令 | 简写命令 `/wtc` | 启用 | 控制台与真实玩家命令 |
| F-008 | 命令 | `help` 输出正式帮助 | 启用 | 命令输出包含核心子命令 |
| F-009 | 命令 | `platform` 输出平台能力 | 启用 | 输出平台 ID 和能力项 |
| F-010 | 命令 | `reload` 重载配置与语言 | 启用 | 输出成功，配置改动生效 |
| F-011 | 命令 | `clear` 立即清理 | 启用 | 清理统计、路由结果、通知 |
| F-012 | 命令 | `stats` 输出清理和垃圾桶统计 | 启用 | 输出世界数、路由、公共/个人库存、剩余秒数 |
| F-013 | 命令 | `add <数量>` 玩家当前世界增加上限 | 启用 | 真实玩家命令和 `data/worlds.yml` |
| F-014 | 命令 | `add <世界名> <数量>` 控制台指定世界增加上限 | 启用 | 控制台命令和 `data/worlds.yml` |
| F-015 | 权限 | OP 旁路恢复 | 启用 | OP 玩家执行正式玩家命令 |
| F-016 | 权限 | 管理命令无权限时拒绝 | 启用 | 非 OP 或缺权限玩家负向命令 |
| F-017 | 世界垃圾桶 | 告示牌创建世界垃圾桶 | 启用 | 真实或后台创建，数据落盘 |
| F-018 | 世界垃圾桶 | 创建数量上限与 `add` 后上限生效 | 启用 | 连续创建和 `max-count` 断言 |
| F-019 | 世界垃圾桶 | 禁止世界不允许普通玩家创建 | 启用 | banned-worlds 负向创建 |
| F-020 | 世界垃圾桶 | 破坏告示牌或容器会移除登记 | 启用 | 破坏后 `data/worlds.yml` 与 summary |
| F-021 | 世界垃圾桶 | 世界物品黑名单阻止进入世界垃圾桶 | 启用 | 设置黑名单后路由降级 |
| F-022 | 世界垃圾桶 | 默认不加载未加载区块，路由降级 | 启用 | 远处未加载位置、跳过计数、公共/个人降级 |
| F-023 | 公共垃圾桶 | 公共垃圾桶 GUI 打开 | 启用 | 真实客户端 GUI 截图 |
| F-024 | 公共垃圾桶 | 公共垃圾桶分页和翻页按钮 | 启用 | 多页配置和客户端截图/点击 |
| F-025 | 公共垃圾桶 | 清理或路由进入公共垃圾桶 | 启用 | `debugroute`、`clear`、`stats` |
| F-026 | 公共垃圾桶 | 玩家从公共垃圾桶取出物品 | 启用 | GUI 点击和背包变化 |
| F-027 | 公共垃圾桶 | 玩家向公共垃圾桶放入物品 | 启用 | GUI 点击和库存变化 |
| F-028 | 公共垃圾桶 | 公共垃圾桶取出冷却 | 启用 | 连续取出触发冷却提示 |
| F-029 | 公共垃圾桶 | 公共垃圾桶操作日志 | 启用 | `plugins/WorldListTrashCan/logs/global-trash-*.log` |
| F-030 | 公共垃圾桶 | 公共黑名单 GUI 关闭保存并立即生效 | 启用 | GUI 保存、`trash.yml`、路由负向 |
| F-031 | 公共垃圾桶 | 按清理次数自动刷新公共垃圾桶 | 启用 | 第 N 次清理前清空旧库存，本轮新入库物品保留和通知 |
| F-032 | 个人垃圾桶 | 个人垃圾桶 GUI 打开 | 启用 | 真实客户端 GUI 截图 |
| F-033 | 个人垃圾桶 | 清理或路由进入个人垃圾桶 | 启用 | owner 掉落、`clear`、`debugsummary` |
| F-034 | 个人垃圾桶 | 玩家从个人垃圾桶取出物品 | 启用 | GUI 点击和背包变化 |
| F-035 | 个人垃圾桶 | 玩家向个人垃圾桶放入物品 | 启用 | GUI 点击和库存变化 |
| F-036 | 个人垃圾桶 | 个人垃圾桶满时自动清空旧内容 | 启用 | 小容量夹具或填满后路由 |
| F-037 | 个人垃圾桶 | Vault 扣费取出 | 默认关闭 | 安装 Vault/Economy，配置 `take-cost > 0` |
| F-038 | 个人垃圾桶 | 单个损坏回收提示 | 默认关闭，测试启用 | `damage-recovery.mode: personal-trash`，真实客户端提示 |
| F-039 | 个人垃圾桶 | 批量清理回收提示，未超过展示上限完整列出 | 启用 | 三类物品 owner 清理 |
| F-040 | 个人垃圾桶 | 批量清理回收提示，超过展示上限追加省略号 | 启用 | 四类物品 owner 清理 |
| F-041 | 个人垃圾桶 | Legacy/Bukkit 无 PDC 版本用短期运行态追踪 owner | 启用 | 1.12.2 owner 掉落清理进入个人桶 |
| F-042 | 个人垃圾桶 | Paper/Spigot 现代端用实体 PDC 追踪 owner 且不污染 ItemStack | 启用 | 26.1.2 owner 掉落、堆叠和入库检查 |
| F-043 | 清理策略 | ignored-worlds 跳过指定世界 | 启用 | 配置测试世界为忽略后清理无动作 |
| F-044 | 清理策略 | ignored-materials 跳过指定物品 | 启用 | 钻石掉落清理后仍存在或未路由 |
| F-045 | 清理策略 | 显示名片段跳过物品 | 启用 | 自定义名物品清理跳过 |
| F-046 | 清理策略 | Lore 片段跳过物品 | 启用 | 带 lore 物品清理跳过 |
| F-047 | 清理策略 | 路由优先级世界 -> 个人 -> 公共 -> 删除 | 启用 | 三类路由和降级用例 |
| F-048 | 清理策略 | 实体清理总开关 | 启用 | 开启/关闭对照；2026-07-02 运行态专项已覆盖 |
| F-049 | 清理策略 | 清理经验球 | 启用 | 生成经验球后清理 |
| F-050 | 清理策略 | 清理怪物 | 启用 | 生成怪物后清理 |
| F-051 | 清理策略 | 清理动物可配置 | 默认关闭 | 启用后动物清理 |
| F-052 | 清理策略 | 清理投射物 | 启用 | 生成箭矢/投射物后清理 |
| F-053 | 清理策略 | 自定义名实体保护 | 启用 | 命名实体清理跳过 |
| F-054 | 清理策略 | 船内实体保护可配置 | 默认关闭 | 启用后船内实体跳过 |
| F-055 | 清理策略 | 实体白名单优先保留 | 启用 | 白名单实体跳过 |
| F-056 | 清理策略 | 实体黑名单强制清理 | 启用 | 黑名单实体清理 |
| F-057 | 清理通知 | Chat 倒计时和完成通知 | 启用 | 客户端截图与控制台日志 |
| F-058 | 清理通知 | Chat 完成通知可点击命令 | 启用 | 客户端点击或组件结构证据 |
| F-059 | 清理通知 | ActionBar 通知 | 启用 | 客户端截图 |
| F-060 | 清理通知 | BossBar 通知 | 默认关闭 | 启用后客户端截图 |
| F-061 | 清理通知 | Title/Subtitle 通知 | 默认关闭 | 启用后客户端截图 |
| F-062 | 清理通知 | Sound 通知 | 启用 | 客户端可见/日志辅助 |
| F-063 | 清理通知 | Command 通知 | 默认关闭 | 启用后控制台命令输出 |
| F-064 | 保护功能 | 防丢弃模式阻止玩家丢弃 | 启用 | 真实玩家切换后丢弃被取消 |
| F-065 | 保护功能 | look 查询手持物品、区块实体和右键实体 | 启用 | 真实客户端聊天证据 |
| F-066 | 保护功能 | 聊天限频 | 启用 | 真实玩家连续聊天触发提示 |
| F-067 | 保护功能 | 命令限频和白名单 | 启用 | 连续命令负向、白名单对照 |
| F-068 | 保护功能 | 清理不可拾取箭矢 | 启用 | 无限弓/骷髅箭命中后移除 |
| F-069 | 保护功能 | 防踩踏农田 | 启用 | 玩家或实体踩踏事件被取消 |
| F-070 | 实体限制 | 单世界实体数量限制 | 默认关闭 | 启用后基于低占用索引超量生成被取消 |
| F-071 | 实体限制 | 密集实体限制 | 默认关闭 | 启用后通过分片快照和候选队列预算化移除 |
| F-072 | 实体限制 | ignored-worlds 跳过实体限制 | 启用 | 忽略世界对照 |
| F-073 | 消息 | 多语言文件存在并可切换 | 启用 | `language` 切换、reload、帮助输出 |
| F-074 | 消息 | 外部语言文件缺节点时回退 jar 默认节点 | 启用 | 删除节点后输出不为空 |
| F-075 | 消息 | 1.12.2 RGB 降级为传统颜色 | 启用 | 真实客户端截图 |
| F-076 | 消息 | 26.1.2 Paper/Spigot RGB Chat、ActionBar、Title | 启用 | 真实客户端截图 |
| F-077 | 消息 | GUI 标题、物品名、Lore 富文本渲染 | 启用 | `debugrgb` GUI/物品截图 |
| F-078 | PAPI | `%Wtc_ClearTime%` 返回剩余清理秒数 | 前置存在时启用 | PlaceholderAPI parse |
| F-079 | bStats | serviceId 24350，上报版本 7.0.0 | 启用 | bStats 配置、上报证据 |
| F-080 | 日志 | 测试和公共垃圾桶日志保留，不删除测试服 logs | 启用 | 证据目录和服务端 logs 保留 |
| F-081 | Folia 清理保护 | Folia 清理按已加载 chunk 分批扫描，不强行加载未加载区块 | 启用 | Folia 服务端已加载 chunk 统计、清理汇总、配置备份 |
| F-082 | Folia 清理保护 | Folia 清理运行中保护，上一轮未结束时拒绝重复启动 | 启用 | 连续 `/wtc clear`、RCON/console 响应和汇总日志 |
| F-083 | Folia 清理保护 | Folia 清理超时后释放运行中状态，下一轮可重新启动 | 启用 | 低 timeout/小批量压力配置、两次 `timedOut=true` 汇总 |
| F-084 | Folia 清理通知 | Folia 清理超时发送 `-4` 文案，不误报清理成功 | 启用 | `cleanup.yml` 备份、console/latest、玩家可见通道按需补客户端证据 |
| F-085 | 实体限制低占用扫描 | dirty chunk、loaded chunk 分片、异步候选、TTL/去重/预算删除 | 默认随实体限制启用 | `debugdensity`、压力配置、候选消费、reload/disable 清理和 Folia region 线程证据 |
| F-086 | 清理策略 | 在线人数低于 `guards.min-online-players` 时跳过自动扫地或 `/wtc clear false` | 启用 | 真实客户端 `/wtc clear false`、`/wtc stats`、服务端日志截图 |
| F-087 | 清理策略 | 目标实体数量低于 `guards.min-total-entities` 时跳过自动扫地或 `/wtc clear false`，达到阈值后正常清理 | 启用 | 真实客户端少量实体跳过、低阈值通过清理、服务端日志截图 |
| F-088 | 手动清理策略 | `/wtc clear`、`/wtc clear` 默认等价 `true`，本次忽略 guards；传入 `false` 时遵守 guards | 启用 | 真实客户端分别执行默认、`true`、`false`，对比清理统计和跳过提示 |
| F-089 | 清理控制台明细 | 独立 `notify.console` 按自定义名/名称/类型聚合实体，显示前 N、`others` 和物品实际数量 | 启用 | 自测覆盖排序与降级；真实服务端生成命名实体、普通实体和掉落物后检查 `[CleanupDetail]` 日志 |
| F-090 | 公开品牌 | 插件名、消息/日志前缀、主类和 dist 文件名统一为 `WorldListTrashCan`；命令为 `worldlisttrashcan/wtc`，权限为 `WorldListTrashCan.*`，PAPI 保持 `%Wtc_ClearTime%` | 启用 | 品牌、命令入口、plugin.yml 审计与真实客户端 `/wtc reload` |
| F-091 | 公共垃圾桶字符布局 | 1-6 行字符布局动态决定 content/翻页/背景槽；展示物支持材质候选、model-id、name、lore 和 unavailable-item；缩容旧存量进入只读临时溢出页且新物品不写入溢出页 | 启用 | 布局解析自测、旧 ModelId 迁移、1.12.2 与 Folia/现代真实客户端小布局、翻页、Lore、缩容溢出页及服务端日志 |
| F-092 | 公共垃圾桶动作按钮 | `type: actions` 按顺序执行 console/command/message；名称、Lore 和动作支持玩家内置变量及可选 PAPI，玩家视图不复制共享库存 | 按配置启用 | 配置解析、无 PAPI 降级、PAPI 实服、普通点击/禁止交互、消息/玩家命令/控制台命令和双玩家变量隔离 |

## 测试结果记录规则

每轮完整回归都要产出：

- `summary.json`：按功能 ID 写出 `PASS`、`FAIL`、`SKIP` 或 `STATIC-PASS`。
- `server-console.log`、`console-commands.log`、`client-stdout.log`、`client-stderr.log`。
- 玩家可见功能的 F2 截图。
- 被临时修改配置的备份。
- 本文档中未被运行态覆盖的功能必须写明原因，不能把“没测”混成“通过”。

`PASS` 只用于本轮真实执行过的运行态功能；`STATIC-PASS` 只用于核心纯 Java 策略、jar 内容或源码结构可以完全证明的功能；`SKIP` 只用于默认关闭且本轮没有配置前置、或者目标端不适用的功能。

## 2026-07-21 F-091 公共垃圾桶字符布局专项

本轮只部署 `dist/WorldListTrashCan-universal.jar`，SHA256 为 `d821feef1a5e9158f027c530c048527195183d4954e41af13701ec8373b9ea24`。Paper 1.12.2 与 Folia 1.21.8 均使用对应真实客户端进入服务器，结果全部 PASS；统一证据目录为 `docs/test-evidence/global-trash-layout-visual-20260721-024653/`。

- 大布局写入 12 个 64 数量堆叠后缩成 2 行、9 个内容槽和 1 个正常页，库存保持 `768 / 12 堆`，可见页数从 5 变为 2。
- 真实客户端首页显示 9 堆，点击下一页后临时溢出页显示剩余 3 堆；上一页、下一页名称和 Lore 的页码占位符正确。
- 正常页已满且溢出页仍有空槽时，`debugRoute ... routed=false`，证明新物品不会写入溢出页。
- 玩家真实点击取出一堆后库存变为 `704 / 11 堆`，再次路由返回 `routed=true`，库存恢复 `768 / 12 堆`。
- 7 行布局被拒绝并回退 `rows=6, slots=54, contentSlots=45`，旧存量仍完整。
- 1.12.2 展示传统色降级；1.21.8 Tooltip 显示 RGB 蓝色名称、黄色和灰色 Lore。

证据目录：`docs/test-evidence/global-trash-layout-visual-20260721-024653/`。

旧 ModelId 迁移到 `global-trash.gui.layout.items.a/c/b.model-id` 的最新 F-005 证据：`docs/test-evidence/legacy-migration-universal-20260721-021850/`。

## 2026-07-24 F-092 公共垃圾桶动作按钮专项

本轮只部署当前 `dist/WorldListTrashCan-universal.jar`，SHA256 为 `ee58dd0e1d4834087916b0920bf23e237a9bc5cfad6a74129ad8b1e658481635`，并同时部署 SHA256 为 `3867a27edafca1269366779cae0792bc6fa61b56ec6c9057cfb54c1ad62e4e1b` 的审计附属 Jar。Paper 1.12.2 与 Folia 1.21.8 均由对应真实客户端进入服务器，机器断言和逐张截图复核全部 PASS。

- `[message]`、`[command]`、`[console]` 按配置顺序执行，内置玩家和页码变量正确。
- 1.12.2 无 PAPI 时 `%Wtc_ClearTime%` 在按钮、动作和附属消息中原样保留；Folia 1.21.8 使用真实 PAPI 时解析为数字。
- 普通左键和右键触发；Shift 与数字键不触发。客户端物理双击实际产生两个普通 LEFT，符合动作可重复点击语义；Bukkit `DOUBLE_CLICK` 类型由单元测试直接拒绝。
- 玩家展示视图与唯一公共库存分离，按钮展示物不可拿取；关闭、reload 和 disable 会释放视图引用。
- 查水表菜单没有改为字符布局，公共垃圾桶没有预置 `/wtc audit` 跳转；附属 PAPI 通过玩家独立输入 `/wtc audit` 验收。

证据目录：`docs/test-evidence/global-trash-actions-visual-20260724-163808/`。

## 2026-06-08 universal 功能矩阵结果

本轮只使用 `dist/WorldListTrashCan-universal.jar` 测试，jar 摘要：

```text
size: 432254
sha256: 9396d8c524ff44b03d3f2a1b4d7e7848e64c93043cc9ea4635ca5967bb7e0399
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

矩阵 JSON 按子用例记录，所以同一功能 ID 可能出现多行；例如 `F-007` 会分别覆盖 `/worldlisttrashcan`、`/WorldListTrashCan`、`/WTC`、`/wtc`。本轮每端共有 99 条矩阵行。

| 服务端 | run | 总结果 | PASS | STATIC-PASS | SKIP | FAIL | 截图 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| Paper 1.12.2 | `20260608-211313` | PASS | 56 | 12 | 31 | 0 | 21 |
| Paper 26.1.2 | `20260608-211637` | PASS | 55 | 12 | 32 | 0 | 21 |
| Spigot 26.1.2 | `20260608-212037` | PASS | 55 | 12 | 32 | 0 | 21 |

证据目录：

```text
docs/test-evidence/universal-function-matrix-1122-2612-20260608-213500/
```

2026-06-08 当轮仍为 `SKIP` 的通用专项夹具项：

```text
F-005, F-019, F-020, F-021, F-022, F-024, F-026, F-027, F-028, F-029,
F-030, F-031, F-034, F-035, F-036, F-037, F-054, F-058, F-068, F-069,
F-070, F-071, F-072, F-073, F-074
```

截至 2026-07-02，以下当轮 `SKIP` 项已由后续专项收敛为 PASS：F-005 已由 2026-07-01 旧配置迁移独立服务端专项收敛；F-019、F-020、F-021、F-022 已由 2026-07-01 世界垃圾桶边界真实服务端夹具专项收敛；F-024、F-026、F-027、F-028、F-029、F-030、F-034、F-035、F-036 已由 2026-07-02 GUI 真实点击三端真实客户端专项收敛，其中 F-026/F-027/F-034/F-035 的打开后取放 deny 分支已由 2026-07-02 GUI 取放权限真实客户端专项补验；F-031 已由 2026-06-27 公共垃圾桶刷新时序三端真实客户端专项收敛；F-037 已由 2026-07-02 Vault/Economy 真实客户端专项收敛；F-054 已由 2026-07-01 船内实体保护真实服务端夹具专项收敛；F-058 已由 2026-07-01 清理通知 Chat 点击真实客户端专项收敛；F-068、F-069 已由 2026-07-01 保护边界真实服务端夹具专项收敛；F-070、F-072 已由 2026-07-01 世界实体上限真实服务端夹具专项收敛；F-071 已由 2026-06-18 密集实体真实客户端矩阵和 2026-06-28 `remove-count` 语义专项收敛；F-073、F-074 已由 2026-07-01 多语言真实客户端专项收敛。

当前仍未收敛的通用专项项：无。

跨端不适用项会在对应服务端显示为 `SKIP`：`F-003` 只用于 1.12.2 Java 8 bootstrap；`F-041/F-075` 只用于 legacy；`F-042/F-076` 只用于现代端。这类 `SKIP` 表示目标端不适用，不计入当前未收敛通用专项；如果未来要证明这些能力在其他端有新兼容路径，必须另写专项夹具或人工验收。

## 2026-07-01 旧配置迁移专项

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
sha256: 18b2f29229dba529098a94748db6abf8b729c81a0c3ab749a461d28d8d14f55b
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_legacy_migration_matrix.py
```

测试方式：

- 创建独立 Paper 1.12.2 临时服务端，复制原测试服 `cache`、`libraries` 加速启动，但不污染正在使用的三端在线回归环境。
- 用相邻旧目录 `plugins/WorldListTrashCan/config.yml`、`data/data.yml` 验证旧插件目录迁移。
- 用当前目录旧结构 `plugins/WorldListTrashCan/config.yml`、`data/data.yml` 验证覆盖式迁移和 `legacy-migration-backup/` 备份。
- 通过 RCON 执行 `plugins`、`wtc platform`、`wtc stats`、`wtc clear true`，证明 universal jar 已在真实 Bukkit 生命周期中加载并读取迁移结果。
- 断言 `legacy-migration-report.md`、`config.yml`、`cleanup.yml`、`trash.yml`、`protections.yml`、`entity-limits.yml`、`data/worlds.yml` 中的关键迁移字段。

结果：

```text
F-005: PASS
allPassed: true
证据目录: docs/test-evidence/legacy-migration-universal-20260701-165606/
```

### 2026-07-24 F-005 严格复验

- 被测 universal Jar SHA-256：`a42e1c4077f9f4250f89cb88ffbe22d2fe7be88e07efa31226e02786e1649a72`。
- Paper 1.12.2：4 个成功迁移场景、5 个安全拒绝场景全部通过；旧主配置、世界数据和 8 份语言文件完成字节级备份核对。
- Spigot 26.1.2、Folia 1.21.8：使用同一整包与同一旧版 6.9.8 原始配置，首次迁移、运行时读取和二次启动幂等全部通过。
- 真实 Forge 1.12.2 客户端：`/wtc stats` 显示公共垃圾桶页数 7，`/wtc debugnotify 0` 收到旧配置迁移消息，公共垃圾桶 GUI 显示 `1/7`、玻璃背景和 `STICK` 下一页按钮。
- 证据：`docs/test-evidence/legacy-migration-universal-20260724-000752/`、`docs/test-evidence/legacy-migration-platform-20260724-001431/`、`docs/test-evidence/legacy-migration-client-20260724-003203/`。

## 2026-07-01 船内实体保护专项

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
sha256: 18b2f29229dba529098a94748db6abf8b729c81a0c3ab749a461d28d8d14f55b
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_boat_entity_protection_matrix.py
```

测试方式：

- 编译临时 Bukkit 夹具 `BlWtcBoatFixture`，夹具只在隔离测试服内生成并查询实体，不进入正式产物。
- 在 Paper 1.12.2 与 Spigot 26.1.2 隔离服务端中临时启用 `entities.clear-animals: true`、`entities.clear-named-entities: true`、`entities.ignore-entities-in-boat: true`。
- 夹具生成一只船内牛和一只普通牛，随后执行正式 `/wtc clear true`。
- 断言船内牛仍存在且 `protectedInsideBoat=true`，普通牛 `normalExists=false`，证明船内实体跳过逻辑在平台快照和正式清理路径中生效。

结果：

```text
F-054: PASS
allPassed: true
证据目录: docs/test-evidence/boat-entity-protection-20260701-171046/
失败对照: docs/test-evidence/boat-entity-protection-20260701-170854/
```

## 2026-07-02 实体清理总开关专项

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
sha256: 18d92a709d48fc291dd77c74a3be7d543f5bb20fa0fc01c4c70ab00eb33773d4
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_entity_cleanup_toggle_matrix.py
```

测试方式：

- 编译临时 Bukkit 夹具 `BlWtcEntityToggleFixture`，夹具只在隔离测试服内生成并查询实体，不进入正式产物。
- 在 Paper 1.12.2 与 Spigot 26.1.2 隔离服务端中临时设置 `entities.enabled: false`，并把 `clear-animals`、`clear-named-entities`、`clear-monsters`、`clear-projectiles`、`clear-experience-orbs` 和黑名单规则全部配置成可清理对照。
- 夹具生成牛、僵尸、箭、经验球和黑名单命名实体，先等待一拍确认 5 个实体全部进入世界，再执行正式 `/wtc clear true`。
- 关闭总开关时断言 5 个目标全部保留且命令返回移除实体为 0；切回 `entities.enabled: true` 后再次执行正式清理，断言同一批目标全部被清理。

结果：

```text
F-048/F-049/F-050/F-052/F-056: PASS
allPassed: true
证据目录: docs/test-evidence/entity-cleanup-toggle-20260702-212255/
```

## 2026-07-01 世界垃圾桶边界专项

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
sha256: 18b2f29229dba529098a94748db6abf8b729c81a0c3ab749a461d28d8d14f55b
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_world_trash_boundary_matrix.py
```

测试方式：

- 编译临时 Bukkit 夹具 `BlWtcWorldTrashFixture`，夹具只在隔离测试服内触发正式事件、准备方块/数据和读取断言，不进入正式产物。
- 在 Paper 1.12.2 与 Spigot 26.1.2 隔离服务端中部署同一个 universal 整包。
- F-019：临时把 `world` 写入 `world-trash.banned-worlds`，用非 OP 但有创建权限的 Player 代理触发正式 `SignChangeEvent`，断言 `registered=false`。
- F-020：移除禁止世界后触发正式创建，再触发 `BlockBreakEvent`，断言 `data/worlds.yml` 登记从 `1` 变为 `0`。
- F-021：写入世界垃圾桶位置和 `STONE` 单世界黑名单，丢出 5 个 `STONE` 后执行正式 `/wtc clear true`，断言世界箱子 `chestAmount=0`，公共垃圾桶当前物品为 5。
- F-022：写入远处未加载区块位置，丢出 7 个 `STONE` 后执行正式 `/wtc clear true`，断言 `chunkLoadedAfter=false`、公共垃圾桶当前物品为 7，服务端日志包含 `worldTrashSkippedUnloadedChunks=1`。

结果：

```text
F-019: PASS
F-020: PASS
F-021: PASS
F-022: PASS
allPassed: true
证据目录: docs/test-evidence/world-trash-boundary-20260701-172919/
失败对照: docs/test-evidence/world-trash-boundary-20260701-172343/
失败对照: docs/test-evidence/world-trash-boundary-20260701-172602/
失败对照: docs/test-evidence/world-trash-boundary-20260701-172748/
失败对照: docs/test-evidence/world-trash-boundary-20260701-172836/
```

## 2026-07-01 保护边界专项

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
sha256: 0b8fe41981a5933058983d644c14fb80de11f5825e9ce02d2ce12faacf19df84
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_protection_boundary_matrix.py
```

测试方式：

- 编译临时 Bukkit 夹具 `BlWtcProtectionFixture`，只用于隔离测试服内触发正式事件和输出断言。
- 在 Paper 1.12.2 与 Spigot 26.1.2 隔离服务端中部署同一个 `dist/WorldListTrashCan-universal.jar`。
- F-068：分别触发 `ProjectileHitEvent` 的不可拾取箭矢路径，以及 `EntityShootBowEvent` 记录后的骷髅/无限弓箭矢路径，断言两支箭矢命中后均被移除。
- F-069：分别触发 `EntityInteractEvent` 与 `PlayerInteractEvent(Action.PHYSICAL)`，断言实体和玩家踩踏农田事件均被取消。

本轮同时修复 1.20+ Spigot/Paper API 中 `Arrow.PickupStatus` 迁移到 `AbstractArrow.PickupStatus` 后，旧直连枚举会导致不可拾取箭矢判断失效的问题。现在正式插件通过反射读取 `getPickupStatus` 并按状态名判断，兼容 legacy 与现代端。

结果：

```text
F-068: PASS
F-069: PASS
allPassed: true
证据目录: docs/test-evidence/protection-boundary-20260701-174709/
失败对照: docs/test-evidence/protection-boundary-20260701-173858/
失败对照: docs/test-evidence/protection-boundary-20260701-174200/
失败对照: docs/test-evidence/protection-boundary-20260701-174348/
```

## 2026-07-01 多语言真实客户端专项

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
size: 523543
sha256: 18b2f29229dba529098a94748db6abf8b729c81a0c3ab749a461d28d8d14f55b
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_language_visual_matrix.py
```

测试方式：

- 每端启动真实客户端进服，临时修改 `config.yml` 的 `language` 为 `message_en.yml`，重载后由真实客户端执行 `/wtc help`。
- 断言客户端日志和 F2 截图包含英文主帮助：`Show help`、`Open global trash`、`Show cleanup and trash statistics`。
- 再切回 `message_zh.yml`，删除外部 `messages/message_zh.yml` 的 `command.help` 节点，重载后由真实客户端执行 `/wtc help`。
- 断言客户端日志和 F2 截图包含中文主帮助：`查看帮助`、`打开公共垃圾桶`、`查看清理和垃圾桶统计`。
- 本轮同时修复旧外部英文语言文件带旧调试 help 时会被 `normalizeList` 强制退回中文主帮助的问题；现在会回到当前语言的 jar 内置主帮助。

| 服务端 | 客户端版本 | 结果 | 本轮收敛功能 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | 1.12.2 | PASS | F-073、F-074 |
| Spigot 26.1.2 managed | 26.1.2 | PASS | F-073、F-074 |
| Folia 1.21.8 | 1.21.8 | PASS | F-073、F-074 |

证据目录：

```text
docs/test-evidence/language-visual-20260721-022637/
```

## 2026-06-30 清理通知专项触发入口

五个平台命令类和 universal 总包已新增 `/wtc debugnotify <count>`，用于按 `cleanup.yml` 的正式 `notify.*` 配置直接触发指定编号的清理通知。普通端直接复用 `CleanupFeature` 的通知链路；Folia 端先切到 GlobalRegion，再复用 `FoliaRegionCleanupFeature` 的玩家通知分派逻辑，避免绕过玩家实体调度器。

该入口自身只解决 F-058、F-060、F-061、F-062、F-063 后续专项验收的触发问题，不能单独把这些功能从 `SKIP` 改为 `PASS`。后续通过标准仍然是：部署 `dist/WorldListTrashCan-universal.jar`，临时开启对应 `notify.*` 通道，使用真实客户端执行或由后台触发 `/wtc debugnotify 0` 等编号，并保留聊天栏、ActionBar、BossBar、Title 或声音/命令证据。

## 2026-06-30 清理通知专项真实客户端矩阵

本轮只使用 `dist/WorldListTrashCan-universal.jar`，jar 摘要：

```text
size: 522622
sha256: 9691aff181a60413cdb2ebfdcade97e68fbb74b3a862757de5f7c5d46aabd5fd
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试脚本：

```text
tools/rgb-visual-matrix/run_cleanup_notify_visual_matrix.py
```

测试方式：

- 每端启动真实客户端进服，临时把 `cleanup.yml` 的 `notify.chat/actionbar/bossbar/title/sound/command` 全部开启。
- 通过控制台触发 `/wtc debugnotify 0` 和 `/wtc debugnotify -5`，该命令复用正式 `sendNotify` 链路。
- 每次触发后保存真实客户端 F2 截图和服务端关键日志 PNG。
- 客户端截图必须同时能看到 Chat、ActionBar、BossBar、Title；Sound 以客户端字幕 `Experience gained` 作为可见辅助证据；Command 以服务端 `AI_WTC_NOTIFY_COMMAND_*` 输出作为执行证据。
- Folia 端仍走 `FoliaRegionCleanupFeature` 的 GlobalRegion 与 player scheduler 分派，截图只证明本插件通知链路生效，不代表服主自定义 Command 一定 region-safe。

| 服务端 | 客户端版本 | 结果 | 本轮收敛功能 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | 1.12.2 | PASS | F-057 Chat、F-059 ActionBar、F-060 BossBar、F-061 Title、F-062 Sound、F-063 Command |
| Spigot 26.1.2 managed | 26.1.2 | PASS | F-057 Chat、F-059 ActionBar、F-060 BossBar、F-061 Title、F-062 Sound、F-063 Command |
| Folia 1.21.8 | 1.21.8 | PASS | F-057 Chat、F-059 ActionBar、F-060 BossBar、F-061 Title、F-062 Sound、F-063 Command |

证据目录：

```text
docs/test-evidence/cleanup-notify-visual-20260630-092840/
```

## 2026-07-01 清理通知 Chat 点击专项矩阵

本轮新增 `tools/rgb-visual-matrix/run_cleanup_notify_click_matrix.py`，用于真实客户端点击清理完成 Chat 通知中的可点击文本。脚本会临时把 `cleanup.yml` 的 `notify.chat.click-command` 设置为 `/wtc stats`，只开启 Chat 通道，触发 `/wtc debugnotify 0`，打开聊天界面后基于截图像素定位 `AI_CLICK_NOTIFY_0` 文本并点击。通过条件是客户端日志出现 `/wtc stats` 的“清理统计”和“公共垃圾桶”输出，并保留点击前后 F2 截图。

被测 jar：

```text
dist/WorldListTrashCan-universal.jar
size: 523148
sha256: 8e664bac3b5091c3db35e0e3db74d6c51680ccba854f7733923f9c86c384b2e9
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

| 服务端 | 客户端版本 | 结果 | 本轮收敛功能 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | 1.12.2 | PASS | F-058 Chat 完成通知可点击命令 |
| Spigot 26.1.2 managed | 26.1.2 | PASS | F-058 Chat 完成通知可点击命令 |
| Folia 1.21.8 | 1.21.8 | PASS | F-058 Chat 完成通知可点击命令 |

证据目录：

```text
docs/test-evidence/cleanup-notify-click-20260701-190056/
```

## 2026-07-02 GUI 真实点击三端专项矩阵

本轮把 `tools/rgb-visual-matrix/run_trash_gui_click_visual_matrix.py` 从单端扩展为三端真实客户端矩阵，用于真实客户端点击公共/个人垃圾桶和公共黑名单 GUI。脚本会临时备份并修改测试服 `trash.yml`，部署 `dist/WorldListTrashCan-universal.jar`，通过后台 `wtc debugopen <玩家> global/personal` 和 `wtc debugplayer <玩家> globalban` 打开真实 GUI，点击槽位，保存 F2 截图、公共垃圾桶操作日志截图、黑名单保存后的 `trash.yml` 快照和 `summary.json`。

被测 jar：

```text
dist/WorldListTrashCan-universal.jar
size: 526658
sha256: b2219edc7eee635d6899a30d6a40310453e8ce378a65dc967a239c3731132c04
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试端：

```text
E:\server_work\spigot-26.1.2-test-server
Spigot 26.1.2 managed + 真实客户端 26.1.2

E:\server_work\paper-1.12.2-universal-test-server
Paper 1.12.2 managed universal + 真实客户端 1.12.2

E:\server_work\folia1.21.8
Folia 1.21.8 + 真实客户端 1.21.8
```

| 功能 | 结果 | 关键证据 |
| --- | --- | --- |
| F-024 公共垃圾桶分页和翻页按钮 | PASS | 填充 46 个公共堆叠后点击下一页，保存 `global-page-2-after-next-f2.png` |
| F-026 玩家从公共垃圾桶取出物品 | PASS | 点击公共 GUI 上方槽位后截图，`global-trash-*.log` 出现 `-global COBBLESTONEx7` |
| F-027 玩家向公共垃圾桶放入物品 | PASS | 点击玩家快捷栏放入后截图，`global-trash-*.log` 出现 `+global COBBLESTONEx7` |
| F-028 公共垃圾桶取出冷却 | PASS | 连续点击取出触发客户端聊天 `公共垃圾桶拿取冷却` |
| F-029 公共垃圾桶操作日志 | PASS | `server-screenshots/*global-trash-operation-log.png` 渲染 `+global/-global` 日志 |
| F-030 公共黑名单 GUI 关闭保存并立即生效 | PASS | `trash-after-globalban.yml` 包含 `STONE`，随后 `debugRoute ... material=STONE ... routed=false` |
| F-034 玩家从个人垃圾桶取出物品 | PASS | 点击个人 GUI 上方槽位后 `debugsummary` 显示个人垃圾桶物品为 `0` |
| F-035 玩家向个人垃圾桶放入物品 | PASS | 点击玩家快捷栏放入后 `debugsummary` 显示个人垃圾桶物品为 `5` |
| F-036 个人垃圾桶满时自动清空旧内容 | PASS | 先填满 54 堆 `STONE`，再路由 `DIRT 1`，最终个人垃圾桶只剩 `1` 个物品 |

公共黑名单 GUI 本轮同步修复为 token 点击模型和 holder 上下文绑定：点击玩家背包物品只复制 Material token 到 GUI，不消耗玩家物品；点击上方 token 删除；关闭 GUI 后保存并刷新运行期配置。旧实现先依赖光标物品，后续又在 1.12.2 暴露 `Inventory` 对象身份不稳定导致点击/关闭事件取不到同一个上下文的问题；最终通过 `InventoryHolder` 绑定 `BanContext`，并统一从 `event.getView().getTopInventory()` 获取 GUI，避免 1.12.2 保存为空。

证据目录：

```text
docs/test-evidence/trash-gui-click-visual-20260721-023817/
docs/test-evidence/trash-gui-click-visual-20260721-023817/summary.json
docs/test-evidence/trash-gui-click-visual-20260721-023817/trash-gui-click-contact-sheet.png
```

失败和修正链路不计入 PASS：

- `docs/test-evidence/trash-gui-click-visual-20260702-221755/`：三端扩展首轮失败，脚本仍按单端和单一冷却中文文案判断，且 Paper 1.12.2 黑名单保存未通过。
- `docs/test-evidence/trash-gui-click-visual-20260702-222553/`：脚本修正后暴露真实业务缺陷，Paper 1.12.2 点击玩家背包物品后 `trash.yml` 未写入 `STONE`，路由仍 `routed=true`。
- `docs/test-evidence/trash-gui-click-visual-20260702-222906/`：首次 holder 修复仍未完全收敛 1.12.2 上下文问题，F-030 继续失败。
- `docs/test-evidence/trash-gui-click-visual-20260702-223334/`：1.12.2 单端修复确认 PASS，但最终结论以三端矩阵 `223605` 为准。

## 2026-07-02 GUI 取放权限真实客户端三端专项

本轮新增 `tools/rgb-visual-matrix/run_gui_operation_permission_visual_matrix.py`，用于补验“玩家已经能打开 GUI，但具体取出/放入权限被显式 deny”的分支。脚本会临时编译并部署 `PermissionDenyFixture`，对真实在线玩家挂 false 权限附件，通过后台 `wtc debugopen <玩家> global/personal` 稳定打开真实客户端 GUI，再做真实点击、F2 截图、客户端日志和服务端库存断言。公共/个人 GUI 打开权限本身已由权限负向专项用玩家命令覆盖，本专项只验证打开后的 take/put deny 行为。

被测 jar：

```text
dist/WorldListTrashCan-universal.jar
sha256: 18d92a709d48fc291dd77c74a3be7d543f5bb20fa0fc01c4c70ab00eb33773d4
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

| 服务端 | 客户端版本 | 结果 | 关键证据 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | 1.12.2 | PASS | 公共/个人取出 deny 均出现客户端无权限提示；公共/个人放入 deny 后库存保持 `7/3` |
| Spigot 26.1.2 managed | 26.1.2 | PASS | 公共/个人取出 deny 均出现客户端无权限提示；公共操作日志无当前玩家新增 `+global` 记录 |
| Folia 1.21.8 | 1.21.8 | PASS | Folia universal 分支公共/个人 take/put deny 共 4 项 PASS，截图为真实 GUI 非世界画面 |

覆盖场景：

```text
global-take-denied
global-put-denied
personal-take-denied
personal-put-denied
```

证据目录：

```text
docs/test-evidence/gui-operation-permission-visual-20260702-231000/
docs/test-evidence/gui-operation-permission-visual-20260702-231000/summary.json
docs/test-evidence/gui-operation-permission-visual-20260702-231000/gui-operation-permission-contact-sheet.png
```

失败对照：

```text
docs/test-evidence/gui-operation-permission-visual-20260702-213616/
docs/test-evidence/gui-operation-permission-visual-20260702-225500/
```

`213616` 不是插件业务失败；首轮客户端日志已经出现无权限提示，但脚本没有匹配“没有权限从公共/个人垃圾桶取出物品”，并且 `debugsummary` 数量解析误把日志时间戳 `21:37` 当成库存数量。`225500` 是三端扩展首轮失败：1.12.2 玩家已死亡导致截图停留死亡界面，Folia 现代客户端聊天输入没有进入服务端。最终脚本改为安全创造模式初始化、1.12 中文乱码时按 summary 行序解析库存，并用 `debugopen` 打开 GUI 后再真实点击。

## 2026-07-02 权限负向真实客户端专项

本轮新增 `tools/rgb-visual-matrix/run_permission_negative_visual_matrix.py`，用于收敛 F-016 和此前 README 中保留的真实玩家负向权限缺口。脚本会临时编译 `PermissionDenyFixture`，用真实在线玩家测试 Bukkit 权限附件的 false 覆盖，避免把默认 `true` 权限误当成“无法测试负向分支”。

被测 jar：

```text
dist/WorldListTrashCan-universal.jar
sha256: 18d92a709d48fc291dd77c74a3be7d543f5bb20fa0fc01c4c70ab00eb33773d4
plugin.yml version: 7.0.0
```

| 服务端 | 客户端版本 | 结果 | 关键证据 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | 1.12.2 | PASS | 非 OP `/wtc reload` 拒绝；deny `global.open`/`personal.open` 后公共/个人 GUI 打开拒绝；OP 后 reload 成功 |
| Spigot 26.1.2 managed | 26.1.2 | PASS | 非 OP `/wtc reload` 拒绝；deny `global.open`/`personal.open` 后公共/个人 GUI 打开拒绝；OP 后 reload 成功 |
| Folia 1.21.8 | 1.21.8 | PASS | 非 OP `/wtc reload` 拒绝；deny `global.open`/`personal.open` 后公共/个人 GUI 打开拒绝；OP 后 reload 成功 |

证据目录：

```text
docs/test-evidence/permission-negative-visual-20260721-final2/
docs/test-evidence/permission-negative-visual-20260721-final2/summary.json
```

## 2026-07-02 Vault 扣费真实客户端专项

本轮新增 `tools/rgb-visual-matrix/run_vault_payment_matrix.py`，用于验证 F-037 个人垃圾桶 Vault 扣费。脚本会临时编译一个插件名为 `Vault` 的 fake Economy 夹具，注册 VaultAPI `Economy` 服务，并在测试结束后恢复测试服原有 `Vault*.jar`。真实客户端会打开个人垃圾桶 GUI，点击上方取出槽位并保存 F2 截图；服务端日志和渲染截图用于辅助确认余额、扣款次数和个人桶数量。

修复内容：

- universal 总包的 Vault 反射适配从错误的 `withdrawPlayer(Player,double)` 改为 VaultAPI 1.7 实际存在的 `withdrawPlayer(OfflinePlayer,double)`。
- 个人垃圾桶取出在扣费前先检查玩家主背包 `getStorageContents()` 是否能完整接收物品，避免背包满时先扣钱后取出失败。
- 默认语言文件新增 `personal-trash.inventory-full`，背包满提示可配置。

被测 jar：

```text
dist/WorldListTrashCan-universal.jar
size: 523626
sha256: 18d92a709d48fc291dd77c74a3be7d543f5bb20fa0fc01c4c70ab00eb33773d4
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

测试端：

```text
E:\server_work\spigot-26.1.2-test-server
Spigot 26.1.2 managed
真实客户端 26.1.2
```

| 功能 | 结果 | 关键证据 |
| --- | --- | --- |
| F-037 余额充足扣费成功 | PASS | 路由 `STONE 1` 到个人桶，点击取出后 fake Vault 余额 `$90.00`、`withdrawals=1`，个人垃圾桶物品 `0` |
| F-037 余额不足不取出不扣费 | PASS | 余额 `$5.00` 时点击 `DIRT 1`，fake Vault 余额仍 `$5.00`、`withdrawals=0`，个人垃圾桶物品保留 `1` |
| F-037 背包满不取出不扣费 | PASS | fake Vault 填满玩家主背包后点击 `STONE 1`，余额仍 `$100.00`、`withdrawals=0`，个人垃圾桶物品点击前后均为 `2` |

证据目录：

```text
docs/test-evidence/vault-payment-visual-20260702-200346/
docs/test-evidence/vault-payment-visual-20260702-200346/summary.json
docs/test-evidence/vault-payment-visual-20260702-200346/vault-payment-contact-sheet.png
```

## 2026-07-02 Folia PAPI 专项

本轮新增 `tools/rgb-visual-matrix/run_folia_papi_matrix.py`，用于验证 F-078 在 Folia 环境下的 `%Wtc_ClearTime%`。此前旧 `PlaceholderAPI 2.11.6` 会被 Folia 拒绝加载，本轮使用 `E:\server_work\folia1.21.8\plugins\[PAPI]PlaceholderAPI-2.11.7-DEV-null (1).jar` 作为支持 Folia 的真实前置。

被测 jar：

```text
dist/WorldListTrashCan-universal.jar
size: 523626
sha256: 18d92a709d48fc291dd77c74a3be7d543f5bb20fa0fc01c4c70ab00eb33773d4
plugin.yml version: 7.0.0
```

| 功能 | 结果 | 关键证据 |
| --- | --- | --- |
| F-078 Folia PAPI 注册 | PASS | PlaceholderAPI 正常启用，日志出现 `Successfully registered internal expansion: Wtc [7.0.0]` |
| F-078 Folia PAPI 解析 | PASS | `papi parse --null %Wtc_ClearTime%` 在 Folia console 返回 `358` |

证据目录：

```text
docs/test-evidence/folia-papi-20260702-202801/
docs/test-evidence/folia-papi-20260702-202801/summary.json
```

## 2026-06-15 Folia 清理压力回归结果

本轮只使用 `dist/WorldListTrashCan-universal.jar` 测试 Folia 清理保护，jar 摘要：

```text
sha256: A84D2EC08402500505D9BE4F0EDFD404AF86A861AC0DC98F8C86FC53832E7CCC
plugin.yml version: 7.0.0
folia-supported: true
```

测试端：

```text
E:\server_work\folia1.21.8
Folia 1.21.8-6, Java 25.0.3
```

| 功能 | 结果 | 证据 |
| --- | --- | --- |
| F-081 | PASS | 147 个已加载 chunk 基线清理 `timedOut=false`，`clear-every-cleanups: -1` 输出“公共垃圾桶不会自动刷新” |
| F-082 | PASS | 约 5946 个 `ai_wtc_pressure` armor_stand 被清理后，立即二次 `/wtc clear` 命中运行中保护 |
| F-083 | PASS | `chunk-batch-size: 1`、`chunk-batch-delay-ticks: 2`、`timeout-seconds: 1` 连续两次触发 `timedOut=true`，第二次能启动证明运行中状态已释放 |
| F-084 | PASS | `timedOut=true` 时输出 `Folia 清理超时 | 已释放清理状态，下一轮会继续处理剩余 chunk`，不再输出清理成功 |

证据目录：

```text
docs/test-evidence/folia-cleanup-pressure-20260615-030918/
```

## 2026-06-26 扫地启动门禁真实客户端回归

本轮只使用 `dist/WorldListTrashCan-universal.jar` 测试，jar 摘要：

```text
size: 513882
sha256: 24158f4dac77babd2961c9ee83b150b44ebaba73745e32448da606ff68dbd1e5
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

覆盖场景：

- `online-guard`：临时配置 `guards.min-online-players: 2`、`guards.min-total-entities: 0`，只有 1 名真实客户端在线，`/wtc clear` 跳过。
- `entity-guard`：临时配置 `guards.min-online-players: 1`、`guards.min-total-entities: 400`，少量测试掉落物不足 400，`/wtc clear` 跳过。
- `guard-pass-clean`：临时配置 `guards.min-online-players: 1`、`guards.min-total-entities: 1`，生成 1 个可清理掉落物后正常清理。

| 服务端 | 结果 | 关键服务端断言 | 证据目录 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | PASS | `skippedByGuard=true` 覆盖在线人数和目标实体不足，`skippedByGuard=false` 覆盖低阈值清理成功 | `docs/test-evidence/cleanup-guard-visual-20260626-210714/` |
| Spigot 26.1.2 managed | PASS | `onlinePlayers=1,minOnlinePlayers=2`，`targetEntities=1,minTotalEntities=400`，低阈值清理 `itemsRouted=4` | `docs/test-evidence/cleanup-guard-visual-20260626-210904/` |
| Folia 1.21.8 | PASS | `FoliaCleanup skippedByGuard=true` 覆盖两类门禁，低阈值清理 `chunksSeen=723,chunksDone=723,timedOut=false` | `docs/test-evidence/cleanup-guard-visual-20260626-211710/` |

每个服务端每个场景均保留 2 张真实客户端 F2 截图和 1 张服务端日志 PNG 截图，并保留完整 `server-console.log`、`console-commands.log`、客户端 stdout/stderr。

失败尝试已单独写入证据 README，不计入 PASS：PowerShell stdin runner 脱离进程、首版 RCON 脚本 BOM 路径问题、32000 逐条 RCON 造数过慢、32768 指数造数超过该测试服稳定 RCON 边界。

### 2026-06-26 扫地门禁 `-5` 文案补验

用户指出服务端日志截图没有截到 `-5` 文案后，重新检查发现两个问题：旧测试服已有 `cleanup.yml` 时不会自动补齐新增的 `-5` 通知项；普通 Bukkit/Paper/Legacy 的手动清理门禁跳过路径只写 debug 汇总，没有发送正式 `-5` 通知。当前版本已改为启动时只追加缺失的 `notify.*.messages` `-5` 默认项，并在非 Folia 跳过路径同样发送正式通知。

本轮仍只使用 `dist/WorldListTrashCan-universal.jar`，jar SHA256 为 `5D0BB85487F632DD3BC221D5BC749C5DEB3AF2FF92748E14A0C71A00CB134A0D`。

| 服务端 | 结果 | `-5` 证据 |
| --- | --- | --- |
| Spigot 26.1.2 managed | PASS | 服务端截图同时包含 `cleanup.yml` `-5;...本轮扫地已跳过...` 配置行、`skippedByGuard=true` 汇总和中文正式通知；证据目录 `docs/test-evidence/cleanup-guard-visual-20260626-214947/universal_spigot2612/` |
| Folia 1.21.8 | PASS | 服务端截图同时包含 `cleanup.yml` `-5;...本轮扫地已跳过...` 配置行、`FoliaCleanup skippedByGuard=true` 汇总和中文正式通知；证据目录 `docs/test-evidence/cleanup-guard-visual-20260626-214947/universal_folia1218/` |
| Paper 1.12.2 managed | PASS | 1.12.2 控制台中文编码会乱码，服务端截图以 `cleanup.yml` `-5;...本轮扫地已跳过...` 配置行和 `skippedByGuard=true` 汇总为准；玩家可见中文由真实客户端截图和客户端 stdout 证明；证据目录 `docs/test-evidence/cleanup-guard-visual-20260626-215753/universal_managed_paper1122/` |

## 2026-06-27 公共垃圾桶刷新时序回归

本轮修复 `global-trash.clear-every-cleanups: 3` 的时序问题：触发第 3 次刷新时，必须先清空公共垃圾桶旧库存，再执行本轮实际清理和入库；不能先把本轮物品放入公共垃圾桶后再刷新，否则第 3 次清理会把本轮新物品一起清空。

本轮只使用 `dist/WorldListTrashCan-universal.jar` 测试，jar 摘要：

```text
size: 517471
sha256: 52da08cc546e767d9a9a6b0bc983b8847c39e7ee787ec930b2c5a8aa3b756466
plugin.yml version: 7.0.0
main: pixeltech.bluenine.blworldtrashcan.plugin.universal.WorldListTrashCanUniversalPlugin
```

专项配置：

```text
cleanup.yml interval-seconds: 0
guards.min-online-players: 1
guards.min-total-entities: 0
trash.yml world-trash.enabled: false
trash.yml personal-trash.enabled: false
trash.yml global-trash.enabled: true
trash.yml global-trash.clear-every-cleanups: 3
```

验收流程：每轮先由控制台生成 1 个 `STONE` 掉落物，再由真实客户端发送 `/wtc clear`，随后真实客户端发送 `/wtc debugstock` 并 F2 截图。断言为第 1 轮公共桶库存 `1` 且 `globalTrashRefreshed=false`，第 2 轮库存 `2` 且 `globalTrashRefreshed=false`，第 3 轮库存 `1` 且 `globalTrashRefreshed=true`。

| 服务端 | 结果 | 第 3 轮断言 | 证据目录 |
| --- | --- | --- | --- |
| Paper 1.12.2 managed | PASS | 服务端日志 `globalTrashRefreshed=true`，真实客户端 `debugstock` 显示公共垃圾桶物品 `1` | `docs/test-evidence/global-refresh-visual-20260627-013926/` |
| Spigot 26.1.2 managed | PASS | 服务端日志 `globalTrashRefreshed=true`，真实客户端 `debugstock` 显示公共垃圾桶物品 `1` | `docs/test-evidence/global-refresh-visual-20260627-014123/` |
| Folia 1.21.8 | PASS | `FoliaCleanup` 日志 `globalTrashRefreshed=true`，真实客户端 `debugstock` 显示公共垃圾桶物品 `1` | `docs/test-evidence/global-refresh-visual-20260627-014340/` |

## 2026-06-18 实体密度低占用扫描压力回归结果

本轮只使用 `dist/WorldListTrashCan-universal.jar` 测试 F-085，jar 摘要：

```text
sha256: CB78511DBD9645F7127CC4D02C06BF37E89920378BBC2CCC0FED6EE2E933403B
plugin.yml version: 7.0.0
```

测试方式：

- 临时写入 `entity-limits.yml`，启用 cow 密集限制。
- 每端生成 300 只 cow。
- 将 `scanner.max-removes-per-run` 压到 `1`，确认删除预算确实生效。
- 通过 `/wtc debugdensity` 连续采样候选队列、索引实体、快照数、删除成功/跳过、TTL/重试/丢弃。
- 测试结束恢复 `server.properties` 与 `plugins/WorldListTrashCan/entity-limits.yml`，不删除 `logs`、`world*`、`cache`、`assets`。

| 服务端 | Java | 生成数量 | 最终剩余 | 关键断言 | 结果 |
| --- | --- | ---: | ---: | --- | --- |
| Paper 1.12.2 | Java 8 | 300 | 1 | `debughelp` 包含 `debugdensity`，候选创建/取出/完成 `327/309/327`，候选队列/去重 `0/0`，删除成功/跳过 `43/266`，剩余数在限制内 | PASS |
| Folia 1.21.8 | Java 25 | 300 | 6 | `debughelp` 包含 `debugdensity`，候选创建/取出/完成 `302/299/302`，候选队列/去重 `0/0`，删除成功/跳过 `294/5`，剩余数在限制内 | PASS |

证据目录：

```text
docs/test-evidence/entity-density-low-overhead-20260618-015437/
```

有效证据文件：

```text
artifact-summary.json
paper-1.12.2/summary.json
paper-1.12.2/server-console.log
paper-1.12.2/commands.log
paper-1.12.2/latest-final.log
folia-1.21.8/summary.json
folia-1.21.8/server-console.log
folia-1.21.8/commands.log
folia-1.21.8/latest-final.log
run-density-low-overhead-test.py
```

1.12.2 中删除成功数低于生成数是预期现象，不作为失败：候选在主线程删除前会重新校验是否仍超限；已经不需要删除、实体不存在或状态不匹配的候选会被消费并释放去重。最终判定以候选队列归零、实际剩余数在限制内、预算化过程存在和候选生命周期闭合为准。

## 2026-06-18 实体密度真实客户端截图矩阵

本轮用于验收 F-071 和 F-085 的玩家可见效果，只使用 `dist/WorldListTrashCan-universal.jar`，不混入任何特供 jar。

```text
sha256: 73d1069403d50ebad4e37720fd801f0109f186e91b6f2f428239282f2699bd56
plugin.yml version: 7.0.0
```

测试脚本：

```text
tools/rgb-visual-matrix/run_entity_density_visual_matrix.py
```

测试方式：

- 每端启动真实客户端进服，先生成 80 只测试 cow 并保存 before F2 截图。
- 临时写入 `entity-limits.yml`，启用 `max-count: 8`、`radius: 16`、`remove-count: 80` 的 cow 密集限制。
- 等待 `/wtc debugdensity` 出现候选创建、取出、完成，候选队列/去重归零，删除成功数大于 0。
- 客户端日志必须出现正式“密集实体清理”提示，且每端保存 notify F2 截图；不能只用 debug 面板代替玩家正式提示。
- 用真实客户端执行 `/wtc debugdensity`，客户端日志必须出现“实体密度扫描统计”，并保存 after F2 截图。
- 现代客户端用真实按键打开聊天栏并剪贴板粘贴命令；1.12.2 客户端用窗口消息 `WM_CHAR` 投递文本，避免剪贴板路径不稳定。
- Folia 端不使用 vanilla `@e` 半径 kill 做收尾，避免选择器触发 region 线程检查；本轮日志未出现 `Cannot getEntities asynchronously`。

| 服务端目录 | 客户端版本 | 结果 | 关键数据 |
| --- | --- | --- | --- |
| `E:\server_work\1.21.11spigot` | 1.21.11 | PASS | 索引 `8/8`，候选 `25/25/25`，删除 `17/8`，正式提示和玩家 debugdensity 日志均 PASS |
| `E:\server_work\folia1.21.8` | 1.21.8 | PASS | 索引 `5/5`，候选 `85/85/85`，删除 `80/5`，正式提示和玩家 debugdensity 日志均 PASS |
| `E:\server_work\server_cat_1.12.2` | 1.12.2 | PASS | 索引 `5/8`，候选 `80/80/80`，删除 `72/8`，正式提示和玩家 debugdensity 日志均 PASS |
| `E:\server_work\spigot-26.1.2-test-server` | 26.1.2 | PASS | 索引 `10/10`，候选 `89/89/89`，删除 `73/16`，正式提示和玩家 debugdensity 日志均 PASS |
| `E:\server_work\1.21.11arclight-neoforge` | 1.21.1 | PASS | 索引 `5/8`，候选 `88/88/88`，删除 `80/8`，正式提示和玩家 debugdensity 日志均 PASS |
| `E:\server_work\1.20.1fabric.banner` | 1.20.1 | PASS | 索引 `12/8`，候选 `80/80/80`，删除 `72/8`，正式提示和玩家 debugdensity 日志均 PASS |

证据目录：

```text
docs/test-evidence/entity-density-visual-20260618-101314/
```

有效证据文件：

```text
summary.json
entity-density-visual-contact-sheet.png
*/screenshots/*-density-before-f2.png
*/screenshots/*-density-notify-f2.png
*/screenshots/*-density-after-debugdensity-f2.png
*/logs/*-server-console.log
*/logs/*-console-commands.log
*/logs/*-client-stdout.log
```

## 2026-06-28 密集实体 remove-count 语义专项回归

本轮用于验收 F-071 和 F-085 的 `remove-count` 语义，只使用 `dist/WorldListTrashCan-universal.jar`。

```text
sha256: 38c33ad0a32e782c0878b858e9f8cd6cb871d858afd97ae6e216b504720958a7
plugin.yml version: 7.0.0
```

旧版 `GatherEntityLimitCount` 的第四段“清理数量”语义是：密集条件触发后，本轮最多清理这么多只；它不是只清理超出上限的差值。重构版已按该语义修复：

- 候选创建按 `min(remove-count, 当前密集数量)` 控制单轮候选数。
- 删除阶段只校验候选仍存在、类型/世界/chunk/规则有效，不再用“是否仍超限”跳过同轮候选。
- 同一密集半径内已经有待删除候选时，后续跨 chunk 快照不再追加第二组候选。
- 关闭密集实体限制后，旧候选会被消费，不继续删除。
- 截图脚本新增 `--remove-count` 参数，并自动校验正式提示里的“已清理”数值不能超过本轮 `remove-count`。

测试方式：

```powershell
py -3 tools\rgb-visual-matrix\run_entity_density_visual_matrix.py --case external_folia1218 --spawn-count 20 --remove-count 5
py -3 tools\rgb-visual-matrix\run_entity_density_visual_matrix.py --case external_paper12111 --spawn-count 20 --remove-count 5
```

| 服务端目录 | 客户端版本 | 结果 | 关键数据 | 证据目录 |
| --- | --- | --- | --- | --- |
| `E:\server_work\folia1.21.8` | 1.21.8 | PASS | `removeCountNoticeCheck` 为 PASS，正式提示每条均为“本次已清理 5 只”；首次闭合候选/取出/完成 `5/5/5`，删除/跳过 `5/0`，玩家 `/wtc debugdensity` PASS | `docs/test-evidence/entity-density-visual-20260628-032747/` |
| `E:\server_work\1.21.11spigot` | 1.21.11 | PASS | `removeCountNoticeCheck` 为 PASS，正式提示每条均为“本次已清理 5 只”；首次闭合候选/取出/完成 `10/10/10`，删除/跳过 `10/0`，玩家 `/wtc debugdensity` PASS | `docs/test-evidence/entity-density-visual-20260628-032445/` |

同时保留失败对照证据 `docs/test-evidence/entity-density-visual-20260628-030303/`：旧实现下 Folia 曾出现 `remove-count: 5` 但单条正式提示“本次已清理 10 只”，用于证明本轮修复不是脚本误判。
