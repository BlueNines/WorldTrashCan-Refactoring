# WorldListTrashCan 重构执行记录

## 2026-05-31 第一阶段启动

已在 `refactor-workspace` 中开始新实现，原项目仍保持只读。

### 已完成

- 建立多模块 Maven 骨架。
- 建立 `core`、`config`、`storage`、`shared-bukkit`、`platform-*`、`plugin-*` 模块。
- `core` 不依赖 Bukkit/Paper/Folia，已落地：
  - `ItemSnapshot`
  - `EntitySnapshot`
  - `CleanupSettings`
  - `CleanupPolicy`
  - `DefaultCleanupPolicy`
  - `CapabilityReport`
- `storage` 不依赖 Bukkit `Location`，已落地：
  - `TrashLocation`
  - `WorldTrashData`
  - `WorldTrashStorage`
- `shared-bukkit` 已落地：
  - `ServerPlatform`
  - `SchedulerAdapter`
  - `TaskHandle`
  - `BukkitSchedulerAdapter`
  - `Feature`
  - `FeatureRegistry`
- `platform-paper-1_16_1_20` 已落地第一条平台实现 `PaperPlatform`。
- `plugin-paper-1_16_1_20` 已落地插件入口、基础命令、`plugin.yml` 和多配置文件默认资源。
- 新增共同测试规格 `test-specs/cleanup-basic.yml`。

### 验证

当前环境 `mvn` 不在 PATH，本轮使用 `javac` 手动按模块顺序验证：

```text
core -> config -> storage -> shared-bukkit -> platform-paper -> plugin-paper
```

结果：通过。

警告：Java 8/17 目标版本的常规 `javac` 警告，不影响当前源码验证。

### 未完成

- 尚未接入真实清理任务。
- 尚未读取配置为 `ConfigBundle`。
- 尚未做 Paper 测试服启动 smoke。
- legacy / bukkit / folia 模块目前只有边界占位，还未实现。
- 尚未实现旧配置迁移器。

## 2026-05-31 第二阶段闭环

### 已完成

- 接入真实后台清理任务，清理决策仍由 `core` 的 `DefaultCleanupPolicy` 负责。
- 接入拆分配置加载：`config.yml`、`cleanup.yml`、`trash.yml`。
- 实现世界垃圾桶、公共垃圾桶、个人垃圾桶三类路由。
- 实现 1.12 Legacy 产物：
  - `LegacyPlatform`
  - `LegacyItemSnapshotMapper`
  - `LegacyEntitySnapshotMapper`
  - `BLWorldTrashCanLegacyPlugin`
  - `BLWorldTrashCanLegacyCommand`
- 实现现代 Paper 产物：
  - `PaperPlatform`
  - `PaperItemSnapshotMapper`
  - `PaperEntitySnapshotMapper`
  - `BLWorldTrashCanPlugin`
  - `BLWorldTrashCanCommand`
  - `VaultPaymentService`
  - `BLWorldTrashCanExpansion`
- 增加后台测试入口：
  - `/blwtc debugopen <玩家> <global|personal>`
  - `/blwtc debugworldtrash <玩家>`
  - `/blwtc debugroute <玩家> <world|personal|global> <Material> <数量>`
  - `/blwtc debugdrop <玩家> <Material> <数量> [owner]`
  - `/blwtc debugsummary <玩家>`
- 更新根目录工作流：
  - 重构旧插件时使用独立重构目录和“共同核心 + 多版本产物”边界。
  - 会改变运行态的后台测试命令必须调用正式服务、输出 debug 标记并保留完整日志。

### 构建

本机 `mvn` 不在 PATH，使用手工 `javac` 编译，JDK 21 `jar.exe` 打包。

编译顺序：

```text
core -> config -> storage -> shared-bukkit -> platform-legacy -> plugin-legacy -> platform-paper -> plugin-paper -> core test
```

结果：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar      81820 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar  84976 bytes
```

### 1.12.2 测试服验证

部署产物：

```text
paper-1.12.2-test-server/plugins/BLWorldTrashCan-legacy-1.12.jar
```

真实客户端：

```text
client-1.12.2
玩家名: AIAutoTest
服务器: 127.0.0.1:25565
```

RCON 验证命令：

```text
list
plugins
blwtc platform
blwtc debugsummary AIAutoTest
blwtc debugworldtrash AIAutoTest
blwtc debugroute AIAutoTest world STONE 3
blwtc debugroute AIAutoTest personal DIRT 4
blwtc debugroute AIAutoTest global COBBLESTONE 5
blwtc debugsummary AIAutoTest
blwtc debugdrop AIAutoTest SAND 6 owner
blwtc clear
blwtc stats
blwtc debugsummary AIAutoTest
blwtc debugopen AIAutoTest global
blwtc debugopen AIAutoTest personal
```

关键结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示当前平台为 `legacy-1.12`。
- `debugworldtrash` 成功创建并登记测试世界垃圾桶。
- `debugroute world` 成功写入世界垃圾桶。
- `debugroute personal` 成功写入个人垃圾桶。
- `debugroute global` 成功写入公共垃圾桶。
- `debugsummary` 显示世界垃圾桶、公共垃圾桶、个人垃圾桶均有物品数量。
- `debugdrop` 生成真实掉落物后，`clear` 将掉落物回收到世界垃圾桶，统计为 `回收物品 2 (世界 2, 个人 0, 公共 0)`。
- `debugopen` 对在线玩家成功返回，客户端日志确认 `AIAutoTest` 已连接测试服。

本轮保留日志：

```text
paper-1.12.2-test-server/ai-blwtc-debugroutes-rcon-main-20260531-222525.log
paper-1.12.2-test-server/ai-blwtc-debugroutes-rcon-clear-20260531-222525.log
paper-1.12.2-test-server/ai-blwtc-debugroutes-latest-20260531-222702.log
paper-1.12.2-test-server/ai-blwtc-debugroutes-client-stdout-20260531-222702.log
paper-1.12.2-test-server/ai-blwtc-debugroutes-client-stderr-20260531-222702.log
```

### 已知问题和限制

- 1.12 Legacy 不支持 PDC，因此玩家主动丢弃物的长期 owner 标记不可用；本轮通过 `debugroute personal` 验证个人垃圾桶服务和路由写入。
- 打开 GUI 时 EasyCore 报 `NoClassDefFoundError: top/wcpe/wcneteasemodrpc/item/texture/match/TextureMatchs`，这是测试服 EasyCore 前置缺类导致的 `InventoryOpenEvent` 监听异常；BLWorldTrashCan 自身命令返回成功，且服务端 debug 日志有本插件调用记录。
- 现代 Paper 产物已编译打包，但当前本机没有 1.16+ / 1.20 测试服，尚未做启动 smoke。
- `bukkit-1_13_1_15` 与 `folia-1_20` 当前仍是模块边界占位，后续需要对应测试服和 API 后再实现。

## 2026-06-01 旧功能补齐与区块加载方案记录

### 本轮原则

用户指出世界垃圾桶可能强制加载区块导致卡顿。当前判断是旧插件 6.9.8 行为还没有完全覆盖，因此本轮先补齐旧功能、完成编译和真实测试；区块加载性能问题先写方案文档，后续等旧功能闭环后再按文档落地。

方案文档位置：

```text
待重构插件/WorldListTrashCan重构/docs/世界垃圾桶区块加载性能方案.md
待重构插件/WorldListTrashCan重构/refactor-workspace/docs/世界垃圾桶区块加载性能方案.md
```

### 已补齐旧功能

- 玩家防丢弃模式：`/blwtc dropmode`，兼容 `WorldListTrashCan.DropMode`。
- 查询功能：`/blwtc look`，可查询手持物品和右键实体类型。
- 单世界黑名单 GUI：`/blwtc ban`。
- 公共垃圾桶全局黑名单 GUI：`/blwtc globalban`。
- 聊天和命令限频：读取 `protections.yml`。
- 不可拾取箭矢清理和防踩踏农田。
- 实体清理扩展：经验球、怪物、动物、投射物、自定义名、船内实体、白名单/黑名单，支持 `*` 通配。
- 世界实体数量限制和密集实体限制：读取 `entity-limits.yml`。
- 公共垃圾桶日志、公共垃圾桶按清理次数刷新、公共垃圾桶黑名单。
- 定时清理倒计时通知：Chat、ActionBar、Title、Sound、Command，读取 `notify.yml`。
- 后台测试入口：`/blwtc debugplayer <玩家> <dropmode|look|ban|globalban>`，用真实在线 `Player` 对象调用正式玩家入口。

### 构建修正

手工 `javac` 参数文件必须使用 UTF-8 无 BOM。跨版本构建还必须按目标运行时指定 `--release`：

```text
Legacy/Core/Config/Storage/Shared/LegacyPlatform/LegacyPlugin: --release 8
PaperPlatform/PaperPlugin: --release 17
```

之前只用本机 JDK 编译通过时，1.12.2 Java 8 测试服会因为 class 文件版本过高报 `UnsupportedClassVersionError`。本轮已改为按目标运行时打包。

构建结果：

```text
CorePolicySelfTest passed
compile-check passed after debugplayer
dist/BLWorldTrashCan-legacy-1.12.jar      118567 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar 121704 bytes
```

### 1.12.2 真实客户端验证

测试服：

```text
paper-1.12.2-test-server
```

测试客户端：

```text
client-1.12.2
玩家名: AIAutoTest
服务器: 127.0.0.1:25565
```

最终 RCON 覆盖：

```text
list
plugins
blwtc platform
blwtc stats
blwtc debugsummary AIAutoTest
blwtc debugplayer AIAutoTest dropmode
blwtc debugplayer AIAutoTest look
blwtc debugplayer AIAutoTest ban
blwtc debugplayer AIAutoTest globalban
blwtc debugopen AIAutoTest global
blwtc debugopen AIAutoTest personal
blwtc debugroute AIAutoTest world STONE 1
blwtc debugroute AIAutoTest personal DIRT 1
blwtc debugroute AIAutoTest global COBBLESTONE 1
blwtc clear
blwtc stats
blwtc debugsummary AIAutoTest
```

关键结果：

- `list` 显示 `AIAutoTest` 在线。
- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `legacy-1.12`。
- `debugplayer dropmode/look/ban/globalban` 均返回成功。
- `debugopen global/personal` 均返回成功。
- `debugroute world/personal/global` 均路由成功。
- `clear` 能执行实体清理，`stats` 显示清理统计更新。
- 最终 `debugsummary` 显示世界垃圾桶、公共垃圾桶、个人垃圾桶均有对应物品。

本轮保留日志：

```text
paper-1.12.2-test-server/ai-blwtc-fullregression-rcon-smoke-20260531.log
paper-1.12.2-test-server/ai-blwtc-fullregression-rcon-main-20260531.log
paper-1.12.2-test-server/ai-blwtc-fullregression-debugplayer-rcon-smoke-20260531.log
paper-1.12.2-test-server/ai-blwtc-fullregression-debugplayer-rcon-online-20260531.log
paper-1.12.2-test-server/ai-blwtc-fullregression-debugplayer-rcon-main-20260531.log
paper-1.12.2-test-server/ai-blwtc-fullregression-final-rcon-20260601.log
paper-1.12.2-test-server/ai-blwtc-fullregression-final-latest-20260601.log
paper-1.12.2-test-server/ai-blwtc-fullregression-final-stop-20260601.log
paper-1.12.2-test-server/ai-blwtc-fullregression-final-stop-latest-20260601.log
paper-1.12.2-test-server/ai-blwtc-fullregression-debugplayer-client-stdout-20260531.log
paper-1.12.2-test-server/ai-blwtc-fullregression-debugplayer-client-stderr-20260531.log
```

测试结束后已通过 RCON `stop` 正常停服，并确认本轮 `AIAutoTest` 客户端进程已结束。

### 已知测试环境噪声

- EasyCore 在打开 GUI 时因缺少 `top.wcpe.wcneteasemodrpc.item.texture.match.TextureMatchs` 报 `InventoryOpenEvent` 异常；RCON 返回和 BLWorldTrashCan debug 日志显示本插件 GUI 调用已执行。
- 测试服其他前置插件存在 MythicMobs 版本兼容警告和 Druid/MySQL 连接超时日志；本轮没有看到 BLWorldTrashCan 自身启用失败、`UnsupportedClassVersionError`、`NoSuchMethodError` 或插件命令失效。

## 2026-06-01 Bukkit 1.13-1.15 与 Folia 旧功能入口实现

### 本轮目标

用户要求先处理第三项，即 `bukkit-1_13_1_15` 与 `folia-1_20` 不再只停留在模块边界，同时继续以旧功能覆盖为主，不优先做世界垃圾桶区块加载优化。

### 已完成

- 新增 Bukkit/Spigot 1.13-1.15 平台实现：
  - `BukkitPlatform`
  - `BukkitItemSnapshotMapper`
  - `BukkitEntitySnapshotMapper`
- 新增 Bukkit/Spigot 1.13-1.15 插件入口：
  - `BLWorldTrashCanBukkitPlugin`
  - `BLWorldTrashCanBukkitCommand`
  - `BLWorldTrashCanBukkitExpansion`
  - `BukkitVaultPaymentService`
- 新增 Folia 1.20 平台实现：
  - `FoliaPlatform`
  - `FoliaSchedulerAdapter`
  - `FoliaItemSnapshotMapper`
  - `FoliaEntitySnapshotMapper`
- 新增 Folia 1.20 插件入口：
  - `BLWorldTrashCanFoliaPlugin`
  - `BLWorldTrashCanFoliaCommand`
  - `BLWorldTrashCanFoliaExpansion`
  - `FoliaVaultPaymentService`
- 为 Bukkit/Folia 两条产物补齐默认配置、`plugin.yml`、`platform.yml`、命令、权限、Vault 和 PlaceholderAPI 适配。
- 修正 shared-bukkit 的农田判断：不再直接引用 `Material.SOIL`，改为字符串兼容 `SOIL`、`FARMLAND`、`LEGACY_SOIL`，避免 1.13+ 运行时出现 `NoSuchFieldError`。
- Bukkit 1.13-1.15 产物不使用 PDC 玩家标记，避免 1.13 缺少 PDC API 导致运行时缺类；个人垃圾桶路由服务仍保留，玩家主动丢弃 owner 标记能力只在现代 Paper/Folia 产物提供。
- Folia 产物接入 `GlobalRegionScheduler`，当前不声明 `FOLIA_REGION_SAFE`；完整 region-safe 清理仍需要 Folia 实服后续验证和必要重构。

### 构建结果

本机 `mvn` 不在 PATH，本轮继续使用 UTF-8 无 BOM 参数文件手工 `javac` 编译。

```text
core -> config -> storage -> shared-bukkit -> platform-legacy -> platform-bukkit -> platform-paper -> platform-folia -> plugin-legacy -> plugin-bukkit -> plugin-paper -> plugin-folia
```

结果：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar       118604 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar 121513 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar  121719 bytes
dist/BLWorldTrashCan-folia-1.20.jar       125913 bytes
```

class 版本检查：

```text
BLWorldTrashCan-legacy-1.12.jar       -> major version 52
BLWorldTrashCan-bukkit-1.13-1.15.jar -> major version 52
BLWorldTrashCan-paper-1.16-1.20.jar  -> major version 61
BLWorldTrashCan-folia-1.20.jar       -> major version 61
```

Jar 元信息复核：

```text
BLWorldTrashCan-legacy-1.12.jar       -> main=BLWorldTrashCanLegacyPlugin, target=legacy-1.12
BLWorldTrashCan-bukkit-1.13-1.15.jar -> main=BLWorldTrashCanBukkitPlugin, api-version=1.13, target=bukkit-1.13-1.15
BLWorldTrashCan-paper-1.16-1.20.jar  -> main=BLWorldTrashCanPlugin, api-version=1.16, target=paper-1.16-1.20
BLWorldTrashCan-folia-1.20.jar       -> main=BLWorldTrashCanFoliaPlugin, api-version=1.20, folia-supported=true, target=folia-1.20
```

### 1.12 回归 smoke

因为 shared-bukkit 做了 `Material.SOIL` 兼容修正，本轮重新部署 Legacy 产物到 `paper-1.12.2-test-server` 做启动 smoke。

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `legacy-1.12`。
- `stats` 正常返回公共垃圾桶页数、个人垃圾桶数量和下次清理剩余秒数。
- 日志未出现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError` 或插件启用失败。

保留日志：

```text
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-console.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-error.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-rcon-wait.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-rcon-smoke.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-latest.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-rcon-stop.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-smoke-stop-latest.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-resmoke-rcon-smoke-2.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-resmoke-latest.log
paper-1.12.2-test-server/ai-blwtc-platforms-20260601-legacy-resmoke-rcon-stop.log
```

测试结束后已通过 RCON `stop` 正常停服。

## 2026-06-02 Paper 1.20.4 启动回归

继续按旧功能优先的路线，补现代 Paper 产物真实服务端启动验证。本轮新建独立测试服：

```text
paper-1.20.4-test-server
```

服务端来源为 PaperMC 官方下载 API，版本 `1.20.4`，构建 `499`。

### 首轮失败

首次启动暴露真实兼容错误：

```text
java.lang.NoSuchFieldError: Class org.bukkit.Material does not have member field 'org.bukkit.Material STAINED_GLASS_PANE'
```

原因是公共垃圾桶 GUI 背景直接引用了旧版本枚举 `Material.STAINED_GLASS_PANE`。该枚举在 1.20.4 已不存在，属于跨版本共享层不能直接引用的 API 差异。

修复：

- `GlobalTrashService` 不再直接引用 `Material.STAINED_GLASS_PANE`。
- 改为按字符串顺序解析 `BLACK_STAINED_GLASS_PANE`、`STAINED_GLASS_PANE`、`LEGACY_STAINED_GLASS_PANE`、`GRAY_STAINED_GLASS_PANE`、`GLASS_PANE`、`THIN_GLASS`。
- 所有候选都不可用时退回 `Material.STONE`。

### 构建结果

修复后重新执行全平台手工构建：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar       118707 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar 121616 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar  121822 bytes
dist/BLWorldTrashCan-folia-1.20.jar       126016 bytes
```

### Paper 1.20.4 smoke

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
blwtc clear
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `paper-1.16-1.20`。
- `stats` 正常返回公共垃圾桶页数、个人垃圾桶数量和下次清理剩余秒数。
- `clear` 正常执行，返回世界数、回收物品数、移除物品数和移除实体数。
- 日志未出现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError` 或插件启用失败。

保留日志：

```text
paper-1.20.4-test-server/ai-blwtc-paper1204-smoke-20260602-041556-console.log
paper-1.20.4-test-server/ai-blwtc-paper1204-smoke-20260602-041556-error.log
paper-1.20.4-test-server/ai-blwtc-paper1204-smoke-rcon-main.log
paper-1.20.4-test-server/ai-blwtc-paper1204-smoke-rcon-stop.log
paper-1.20.4-test-server/ai-blwtc-paper1204-smoke-latest.log
```

测试结束后已通过 RCON `stop` 正常停服。

### Legacy 1.12 反向回归

因为本轮修复改动在 `shared-bukkit`，需要确认高版本材质兼容修复没有反向影响 Legacy 产物。重新部署 `dist/BLWorldTrashCan-legacy-1.12.jar` 到 `paper-1.12.2-test-server` 后执行 smoke。

启动前顺手规范化 `server.properties`，移除历史 BOM 污染残留的假键，保留原有 RCON、端口、世界、缓存、assets 和 logs。

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
blwtc clear
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `legacy-1.12`。
- `stats` 正常返回公共垃圾桶页数、个人垃圾桶数量和下次清理剩余秒数。
- `clear` 正常执行，返回世界数、回收物品数、移除物品数和移除实体数。
- 日志未出现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError` 或插件启用失败。

保留日志：

```text
paper-1.12.2-test-server/ai-blwtc-material-legacy-resmoke-20260602-042235-console.log
paper-1.12.2-test-server/ai-blwtc-material-legacy-resmoke-20260602-042235-error.log
paper-1.12.2-test-server/ai-blwtc-material-legacy-resmoke-20260602-042235-rcon-wait.log
paper-1.12.2-test-server/ai-blwtc-material-legacy-resmoke-20260602-042235-rcon-main.log
paper-1.12.2-test-server/ai-blwtc-material-legacy-resmoke-20260602-042235-rcon-stop.log
paper-1.12.2-test-server/ai-blwtc-material-legacy-resmoke-20260602-042235-latest.log
```

测试结束后已通过 RCON `stop` 正常停服。

### 未完成和风险

- 当前本机没有 Bukkit/Spigot 1.13-1.15 测试服，`BLWorldTrashCan-bukkit-1.13-1.15.jar` 尚未做实服启动验证。
- 当前本机没有 Folia 1.20 测试服，`BLWorldTrashCan-folia-1.20.jar` 尚未做实服启动验证。
- Folia 产物只是把旧功能入口接进 Folia 产物和全局调度器，尚未证明清理任务全链路 region-safe；后续必须在 Folia 测试服验证后，再决定是否需要把清理扫描改成 region/entity scheduler 分段执行。

## 2026-06-02 旧配置迁移器与版本元数据收尾

本轮补齐旧 `WorldListTrashCan` 到新拆分配置的迁移器，并修复手工打包时 `plugin.yml` 版本占位符未过滤的问题。

### 实现内容

- 新增 `BukkitLegacyConfigMigrator`，在四个平台入口 `saveDefaultConfigs()` 后、读取配置集合前执行。
- 新增 `config.yml` 配置项 `migration-enabled` 和 `migration-legacy-folder`，四个平台默认资源均带中文注释。
- 迁移器支持两类来源：
  - 当前 `plugins/BLWorldTrashCan` 已经是旧结构。
  - 相邻旧目录 `plugins/WorldListTrashCan`。
- 迁移完成后生成 `legacy-migration-report.md`，后续启动看到报告就不重复迁移。
- 当前目录旧结构会先备份到 `legacy-migration-backup/`，再生成新默认 `config.yml`。
- 修正当前目录旧结构的读取顺序：先把旧 `config.yml` / `data/data.yml` 读入内存，再备份和覆盖默认配置，避免旧字段在迁移前被覆盖。
- 四个平台 `plugin.yml` 版本改为 `0.1.0-SNAPSHOT`，避免手工打包时把 `${project.version}` 带进实服。

### 构建和元数据

本机 `mvn` 仍不在 PATH，本轮继续使用手工 `javac` 和 JDK 21 `jar.exe`。参数文件使用 UTF-8 无 BOM，Legacy/Bukkit 产物使用 `--release 8`，Paper/Folia 产物使用 `--release 17`。

本轮构建目录：

```text
build/compile-legacy-migration-20260602-172428
```

构建结果：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar       129303 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  132218 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   132425 bytes
dist/BLWorldTrashCan-folia-1.20.jar        136619 bytes
```

jar 元数据核对：

```text
BLWorldTrashCan-legacy-1.12.jar       -> version=0.1.0-SNAPSHOT, major=52, placeholder=absent
BLWorldTrashCan-bukkit-1.13-1.15.jar  -> version=0.1.0-SNAPSHOT, major=52, placeholder=absent
BLWorldTrashCan-paper-1.16-1.20.jar   -> version=0.1.0-SNAPSHOT, major=61, placeholder=absent
BLWorldTrashCan-folia-1.20.jar        -> version=0.1.0-SNAPSHOT, major=61, placeholder=absent
```

### 相邻旧目录迁移 smoke

测试服：

```text
paper-1.12.2-migration-test-server
```

旧配置来源：

```text
plugins/WorldListTrashCan/config.yml
plugins/WorldListTrashCan/data/data.yml
```

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
blwtc clear
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `legacy-1.12`。
- `stats` 显示公共垃圾桶页数为 `7`，证明 `Set.GlobalTrash.MaxPage` 迁移生效。
- 启动日志显示清理周期为 `123` 秒，证明 `Set.SecondCount` 迁移生效。
- `data/worlds.yml` 迁移出 `world` 的两个坐标、`max-count: 9` 和黑名单材质。
- `legacy-migration-report.md` 记录自动迁移字段和需要人工确认字段。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError` 或插件启用失败。

保留日志：

```text
paper-1.12.2-migration-test-server/ai-blwtc-migration-adjacent-20260602-1726-console.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-adjacent-20260602-1726-error.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-adjacent-20260602-1726-rcon-main.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-adjacent-20260602-1726-rcon-stop.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-adjacent-20260602-1726-latest.log
```

首轮等待 RCON 时曾直接点调用 `.ps1`，被 PowerShell 执行策略拦截，导致误判超时；正式 RCON 改用：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '<RCON脚本>' -Port 25675 -Commands @('plugins','blwtc platform','blwtc stats','blwtc clear') -LogPath '<日志路径>'"
```

### 当前目录旧结构迁移 smoke

旧配置来源：

```text
plugins/BLWorldTrashCan/config.yml
plugins/BLWorldTrashCan/data/data.yml
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `legacy-1.12`。
- `stats` 显示公共垃圾桶页数为 `8`，证明当前目录旧结构没有在读取前被默认配置覆盖。
- 启动日志显示清理周期为 `77` 秒。
- `config.yml` 保留迁移后的 `language: legacy_current.yml`、`debug: true`，并包含新的迁移配置项。
- `trash.yml` 迁移出 `max-pages: 8`、`take-delay-millis: 300`、`default-max-count: 6`、`take-cost: 9.5`。
- `data/worlds.yml` 迁移出 `10,70,20`、`max-count: 11` 和 `COBBLESTONE`。
- `legacy-migration-backup/config.yml` 保留原始旧配置。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError` 或插件启用失败。

保留日志：

```text
paper-1.12.2-migration-test-server/ai-blwtc-migration-current-20260602-1732-console.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-current-20260602-1732-error.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-current-20260602-1732-rcon-wait.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-current-20260602-1732-rcon-main.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-current-20260602-1732-rcon-stop.log
paper-1.12.2-migration-test-server/ai-blwtc-migration-current-20260602-1732-latest.log
```

## 2026-06-02 Bukkit 1.13.2 复测与 Folia 清理保护

本轮继续收口旧功能覆盖，重点处理 Folia 产物的世界实体扫描安全问题，并用 Bukkit 1.13.2 复测确认共享层保护没有误伤普通 Bukkit。

### Folia 首轮失败

测试服：

```text
folia-1.20.1-test-server
```

服务端：

```text
folia-1.20.1-17.jar
```

首次通过控制台执行：

```text
plugins
blwtc platform
blwtc stats
blwtc clear
stop
```

失败现象：

```text
IllegalStateException: Accessing entity state off owning region's thread
FoliaEntitySnapshotMapper.toSnapshot(...)
CleanupFeature.cleanEntity(...)
```

根因：

- 当前 Folia 产物只接入了 `GlobalRegionScheduler`。
- 普通 Bukkit 风格的 `world.getEntities()` + 逐实体读取状态不能放在 Folia global thread 上执行。
- Folia 未完成 region-safe 清理前，不能让定时任务或管理员命令继续触发全世界实体扫描。

### 保守修复

已修改：

- `CleanupFeature`：当平台 id 以 `folia` 开头且未声明 `FOLIA_REGION_SAFE` 时，定时世界扫描不启动，`runNow()` 只刷新公共垃圾桶状态并跳过实体扫描。
- `BLWorldTrashCanFoliaPlugin`：暴露 `isCleanupScanSupported()`。
- `BLWorldTrashCanFoliaCommand`：`/blwtc clear` 在未支持 region-safe 时直接拒绝执行，并提示管理员。

当前提示：

```text
当前 Folia 产物尚未启用 region-safe 清理，已拒绝执行世界实体扫描。
```

### 构建和元数据

本轮构建目录：

```text
build/compile-folia-guard-20260602-180212
```

构建结果：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar       129844 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  132759 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   132966 bytes
dist/BLWorldTrashCan-folia-1.20.jar        137325 bytes
```

jar 元数据复核：

```text
dist/BLWorldTrashCan-legacy-1.12.jar       -> version=0.1.0-SNAPSHOT, major=52, target=legacy-1.12, placeholder=False
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  -> version=0.1.0-SNAPSHOT, major=52, target=bukkit-1.13-1.15, placeholder=False
dist/BLWorldTrashCan-paper-1.16-1.20.jar   -> version=0.1.0-SNAPSHOT, major=61, target=paper-1.16-1.20, placeholder=False
dist/BLWorldTrashCan-folia-1.20.jar        -> version=0.1.0-SNAPSHOT, major=61, target=folia-1.20, placeholder=False
```

### Folia 保护后复测

控制台覆盖：

```text
plugins
blwtc platform
blwtc stats
blwtc clear
stop
```

结果：

- 插件正常启用，`platform` 显示 `folia-1.20`。
- 启动日志显示当前 Folia 产物未启用 region-safe，定时世界扫描已关闭。
- `stats` 可正常返回。
- `clear` 明确拒绝执行世界实体扫描。
- 日志未再出现 `Accessing entity state off owning region's thread`。

保留日志：

```text
folia-1.20.1-test-server/ai-blwtc-folia1201-smoke-20260602-1853-latest.log
folia-1.20.1-test-server/ai-blwtc-folia1201-guarded-console-smoke-20260602-1803-latest.log
```

注意：该 Folia build 的 RCON 对 `plugins` 等命令返回 `Error executing ... (null)`，不能作为命令验证唯一证据；Folia smoke 需要通过真实控制台输入或等价 PTY 方式验证。

### Bukkit 1.13.2 回归复测

因为保护逻辑位于 `shared-bukkit` 的清理功能中，本轮重新部署当前 `dist/BLWorldTrashCan-bukkit-1.13-1.15.jar` 到：

```text
paper-1.13.2-test-server
```

有效启动命令使用 Java 8：

```text
C:\Program Files\Java\jdk-1.8\bin\java.exe -Xms512M -Xmx1024M -jar paper-1.13.2-657.jar nogui
```

本轮曾误用默认 Java 21，服务端直接输出：

```text
Unsupported Java detected (65.0). Only up to Java 12 is supported.
```

该失败属于测试环境 Java 版本误用，不是插件问题，日志已保留。

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
blwtc clear
blwtc stats
stop
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 已启用。
- `platform` 显示 `bukkit-1.13-1.15`。
- `stats` 正常返回。
- `clear` 正常扫描 3 个世界并返回清理统计。
- `clear` 后再次 `stats` 显示世界数更新为 3。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError`、`IllegalStateException` 或插件启用失败。

保留日志：

```text
paper-1.13.2-test-server/ai-blwtc-bukkit113-guard-resmoke-20260602-1830-error.log
paper-1.13.2-test-server/ai-blwtc-bukkit113-guard-resmoke-20260602-1830-java8-console.log
paper-1.13.2-test-server/ai-blwtc-bukkit113-guard-resmoke-20260602-1830-java8-error.log
paper-1.13.2-test-server/ai-blwtc-bukkit113-guard-resmoke-20260602-1830-java8-rcon-main.log
paper-1.13.2-test-server/ai-blwtc-bukkit113-guard-resmoke-20260602-1830-java8-rcon-stop.log
paper-1.13.2-test-server/ai-blwtc-bukkit113-guard-resmoke-20260602-1830-java8-latest.log
```

### 当时风险

- Folia 完整 region-safe 清理仍未实现；当前只保证不会再从危险入口触发世界实体扫描。
- 世界垃圾桶强制加载区块的性能优化在后续“世界垃圾桶未加载区块保护”阶段已经落地；保留此条是为了说明当时为什么先写方案再实施。
- 旧配置迁移报告中的 `ModelId`、BossBar 文本等字段在当时仍需要人工确认；这些字段已在后续“公共 GUI ModelId 与 BossBar 旧配置补齐”阶段承接。

## 2026-06-02 世界垃圾桶未加载区块保护

用户指出世界垃圾桶会强行加载区块导致卡顿。前面已经先记录方案，本轮在旧功能 smoke 和 Folia 保护收口后开始落地默认保护。

### 实现内容

- `TrashConfig.WorldTrashConfig` 新增 `allowLoadUnloadedChunks`。
- `ConfigBundleLoader` 读取 `world-trash.allow-load-unloaded-chunks`，默认 `false`。
- 四个平台默认 `trash.yml` 增加 `world-trash.allow-load-unloaded-chunks: false`，并写入详细中文注释。
- `WorldTrashRouter` 在访问世界垃圾桶容器前先执行 `world.isChunkLoaded(x >> 4, z >> 4)`。
- 默认未加载区块直接跳过，不调用 `getBlockAt()`、`getState()` 或任何可能同步加载区块的方法。
- 如果配置为 `true`，保留旧行为，并在加载世界垃圾桶数据时输出警告。
- `WorldTrashRouter` 增加跳过计数，`CleanupFeature` 清理汇总日志输出 `worldTrashSkippedUnloadedChunks`，后台 `debugsummary` 显示“跳过未加载区块”。

### 构建

本轮构建目录：

```text
build/compile-worldtrash-chunkguard-20260602-182612
```

构建结果：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar       130658 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  133568 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   133782 bytes
dist/BLWorldTrashCan-folia-1.20.jar        138139 bytes
```

jar 元数据复核：

```text
dist/BLWorldTrashCan-legacy-1.12.jar       -> version=0.1.0-SNAPSHOT, major=52, target=legacy-1.12, placeholder=False, chunkGuard=True
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  -> version=0.1.0-SNAPSHOT, major=52, target=bukkit-1.13-1.15, placeholder=False, chunkGuard=True
dist/BLWorldTrashCan-paper-1.16-1.20.jar   -> version=0.1.0-SNAPSHOT, major=61, target=paper-1.16-1.20, placeholder=False, chunkGuard=True
dist/BLWorldTrashCan-folia-1.20.jar        -> version=0.1.0-SNAPSHOT, major=61, target=folia-1.20, placeholder=False, chunkGuard=True
```

### 1.13.2 实服验证

测试服：

```text
paper-1.13.2-test-server
```

测试配置：

- 部署当前 `dist/BLWorldTrashCan-bukkit-1.13-1.15.jar`。
- 备份测试前 `trash.yml` 和 `data/worlds.yml`。
- 在 `data/worlds.yml` 写入远处世界垃圾桶坐标 `320000,64,320000`。
- 明确设置 `world-trash.allow-load-unloaded-chunks: false`。
- 保持公共垃圾桶启用，让世界垃圾桶不可写时可以降级。

RCON 覆盖：

```text
say AI_BLWTC_CHUNKGUARD_SUMMON_RETRY
kill @e[type=item]
summon item 0 80 0 {Item:{id:"minecraft:stone",Count:1b}}
blwtc clear
blwtc stats
say AI_BLWTC_CHUNKGUARD_RETRY_DONE
stop
```

结果：

- `summon` 成功生成 `Stone` 掉落物。
- `/blwtc clear` 返回回收物品 1。
- `/blwtc stats` 显示回收物品分布为世界 0、个人 0、公共 1。
- `latest.log` 清理汇总显示 `worldTrashSkippedUnloadedChunks=1`。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError`、`IllegalStateException` 或插件启用失败。
- 测试结束后已把测试配置另存，并恢复测试前配置。

保留日志和配置证据：

```text
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-console.log
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-error.log
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-rcon-main-2.log
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-rcon-stop.log
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-latest.log
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-trash-before.yml
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-worlds-before.yml
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-trash-test.yml
paper-1.13.2-test-server/ai-blwtc-worldtrash-chunkguard-20260602-1835-worlds-test.yml
```

### 当前风险

- `allow-load-unloaded-chunks: false` 是性能优先的行为变更：远处未加载区块里的真实箱子不会立即接收清理物品，会降级到个人、公共或删除。
- `allow-load-unloaded-chunks: true` 仍可能同步加载区块，只用于服主明确需要旧行为并接受卡顿风险的场景。
- Folia 完整 region-safe 清理仍未实现，当前 Folia 产物仍是安全拒绝世界实体扫描。

## 2026-06-02 仙人掌和岩浆损坏回收补齐

用户继续要求补齐旧功能。本轮复盘旧插件 `Set.PersonalTrashCan.OriginalFeatureClearItemAddGlobalTrash` 后，确认新实现此前只做了 PDC owner 标记，没有承接旧配置里的 `UseModel` 和 `Delay`。

### 实现内容

- `TrashConfig.PersonalTrashConfig` 新增 `damageRecoveryMode` 和 `damageRecoveryDelaySeconds`。
- `ConfigBundleLoader` 读取 `personal-trash.damage-recovery.mode` 和 `personal-trash.damage-recovery.delay-seconds`。
- 四个平台默认 `trash.yml` 增加中文注释：
  - `disabled`：不处理，对应旧 `UseModel=3`。
  - `global-trash`：进入公共垃圾桶，对应旧 `UseModel=1`。
  - `personal-trash`：进入玩家个人垃圾桶，对应旧 `UseModel=2`。
- `TrashFeature` 使用 `ConcurrentHashMap<掉落实体UUID, 玩家UUID>` 只追踪配置延迟内的玩家主动丢弃物，并用平台调度器按时移除，避免长期占用内存。
- 只处理仙人掌、岩浆、火焰这类原版物品损坏原因，不扩大到普通清理任务。
- 旧配置迁移器会把 `UseModel` 自动转换为新字符串模式，把 `Delay` 自动迁移为 `delay-seconds`，不再把这两项列为人工确认字段。
- 四个平台新增 `/blwtc debugdamage <玩家> <Material> <数量>`，用于后台生成真实掉落物并通过 Bukkit 事件总线模拟一次岩浆损坏，验证该配置是否生效。
- 四个平台新增 `/blwtc debugstock`，用于不依赖在线玩家的后台库存快照；`/blwtc stats` 同步显示公共垃圾桶当前物品数量，避免把清理任务统计误当成当前库存。

### 实测记录

测试服：`C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server`。

本轮先用真实 Forge 1.12.2 客户端发送 `/blwtc debugdamage AIAutoTest STONE 2`，因为真实客户端玩家名是 `AIClientAlpha` 且未给该账号 OP，客户端聊天栏返回无权限；失败日志保留在：

- `C:\Users\pc\Desktop\ai开发插件\客户端自动化测试工作区\runs\20260602-blwtc-damage-recovery-client\logs\forge-latest.log`
- `C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server\ai-blwtc-damage-recovery-20260602-rcon-after-client.log`

补 OP 并改用 `/blwtc debugdamage AIClientAlpha STONE 2` 后，服务端出现 `debugDamageRecovery ... recovered=true`，但 `/blwtc stats` 原本只显示清理任务统计，无法证明公共垃圾桶当前库存。因此本轮补齐 `/blwtc debugstock` 和 `stats` 库存行，然后重编译、更新 legacy jar 并重启 1.12.2 测试服。

第三轮真实客户端验证通过：

```text
客户端任务: C:\Users\pc\Desktop\ai开发插件\客户端自动化测试工作区\runs\20260602-blwtc-damage-recovery-client-debugstock\control\client-task.properties
客户端命令: /blwtc debugdamage AIClientAlpha STONE 2
客户端响应: status=PASS
服务端日志: debugDamageRecovery player=AIClientAlpha, material=STONE, amount=2, recovered=true
RCON 验证: /blwtc debugstock -> 公共垃圾桶物品 2，堆叠 1
```

保留的证据文件：

- `C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server\ai-blwtc-damage-recovery-20260602-rcon-debugstock-before.log`
- `C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server\ai-blwtc-damage-recovery-20260602-rcon-debugstock-after.log`
- `C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server\ai-blwtc-damage-recovery-20260602-final-latest.log`
- `C:\Users\pc\Desktop\ai开发插件\客户端自动化测试工作区\runs\20260602-blwtc-damage-recovery-client-debugstock\logs\forge-latest.log`
- `C:\Users\pc\Desktop\ai开发插件\客户端自动化测试工作区\runs\20260602-blwtc-damage-recovery-client-debugstock\logs\paper-latest-before-complete.log`

### 当前风险

- 该功能是短期内存追踪，不会把玩家掉落物归属永久写入本地存储；插件重载后，重载前已经丢在地上的物品不会再参与损坏回收。
- Legacy 1.12 仍不支持 PDC 长期 owner 标记；定时清理中的个人垃圾桶路由不因此改变，但损坏回收可通过本轮短期追踪正常工作。

## 2026-06-02 公共 GUI ModelId 与 BossBar 旧配置补齐

本轮继续复盘旧配置迁移报告里的人工确认字段，确认公共垃圾桶 GUI `ModelId` 和 `BossBarMessageForCount` 都有明确运行语义，可以低风险承接，不应长期留给服主手工处理。

### 实现内容

- `TrashConfig.GlobalTrashConfig` 新增 `backItemModelId`、`nextItemModelId`、`backgroundItemModelId`。
- `ConfigBundleLoader` 读取 `global-trash.gui.back-model-id`、`next-model-id`、`background-model-id`。
- `GlobalTrashService` 创建公共垃圾桶 GUI 按钮时通过反射尝试调用 `ItemMeta#setCustomModelData(Integer)`；1.12/1.13 没有该 API 时自动忽略，避免低版本 class linkage 问题。
- 四个平台默认 `trash.yml` 增加 `global-trash.gui` 配置区，并写入中文注释。
- `NotifyConfig` 新增 BossBar 通知结构，`ConfigBundleLoader` 支持旧格式 `剩余秒数;内容;样式;颜色`。
- `CleanupFeature` 在倒计时通知里发送 BossBar，支持 `0`、`-1`、`-2` 完成阶段消息，并在完成提示后延迟移除 BossBar。
- 四个平台默认 `notify.yml` 增加 `bossbar.messages` 中文注释和默认消息。
- `BukkitLegacyConfigMigrator` 自动迁移：
  - `Set.GlobalTrash.GlobalItems.BackItem.ModelId -> global-trash.gui.back-model-id`
  - `Set.GlobalTrash.GlobalItems.NextItem.ModelId -> global-trash.gui.next-model-id`
  - `Set.GlobalTrash.GlobalItems.BackgroundItem.ModelId -> global-trash.gui.background-model-id`
  - `Set.BossBarMessageForCount -> bossbar.messages`
- 迁移器不再把这些字段列为人工确认。

### 构建

本机 `mvn` 仍不在 PATH，本轮继续使用手工 `javac` 参数文件编译。打包时第一次直接多 `-C` class 目录创建 jar，JDK `jar.exe` 因重复目录项报 `duplicate entry: pixeltech/`，导致 dist 里出现 7 KB 不完整包；随后改为先合并到 staging 目录，再用 JDK 21 `jar.exe` 从单目录打包。JDK 8 `jar.exe` 在当前中文路径下会把路径编码打坏，因此最终打包固定使用 JDK 21。

最终结果：

```text
CorePolicySelfTest passed
dist/BLWorldTrashCan-legacy-1.12.jar       139294 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  142194 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   142405 bytes
dist/BLWorldTrashCan-folia-1.20.jar        146775 bytes
```

jar 复核：

```text
dist/BLWorldTrashCan-legacy-1.12.jar       -> major=52, bossClass=True, bossYaml=True, guiYaml=True
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  -> major=52, bossClass=True, bossYaml=True, guiYaml=True
dist/BLWorldTrashCan-paper-1.16-1.20.jar   -> major=61, bossClass=True, bossYaml=True, guiYaml=True
dist/BLWorldTrashCan-folia-1.20.jar        -> major=61, bossClass=True, bossYaml=True, guiYaml=True
```

### 1.12.2 实服验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

测试步骤：

- 部署当前 `dist/BLWorldTrashCan-legacy-1.12.jar`。
- 备份测试前 `plugins/BLWorldTrashCan/notify.yml` 和 `cleanup.yml`。
- 临时写入短间隔 `interval-seconds: 2`，启用 BossBar，并写入 ASCII smoke 文本。
- 启动 Paper 1.12.2 测试服。
- RCON 验证 `list`、`blwtc platform`、`blwtc stats`。
- 直接用 `AI_AUTO_SCENARIO=runner-file` 和 `AI_AUTO_TASK_FILE=<client-task.properties>` 启动真实 Forge 1.12.2 客户端，让真实玩家 `babyZiXuan` 保持在线。
- RCON 执行 `list`、`blwtc clear`、`blwtc stats`。
- 停止客户端，恢复测试前 `notify.yml` 和 `cleanup.yml`，保留服务端 logs。
- 停止 Paper 测试服，并把最终 `latest.log` 另存。

结果：

- `blwtc platform` 显示 `legacy-1.12`，能力列表包含 `boss-bar: 启用`。
- 真实客户端响应 `status=PASS`，消息为 `connected to 127.0.0.1:25565`。
- RCON `list` 显示 `There are 1/20 players online:babyZiXuan`。
- RCON `/blwtc clear` 成功返回清理统计。
- `latest.log` 在真实玩家在线期间连续出现 `AI BossBar smoke 2`、`AI BossBar smoke 1`、`AI BossBar smoke done ...`，并多轮输出 BLWorldTrashCan 清理汇总。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError`、`NoSuchMethodException`、`Could not pass` 或插件启用失败。
- 测试服临时 `notify.yml` 和 `cleanup.yml` 已恢复，`logs` 未删除。

保留证据：

```text
paper-1.12.2-test-server/ai-blwtc-bossbar-20260602-rcon-start.log
paper-1.12.2-test-server/ai-blwtc-bossbar-20260602-rcon-wait-client.log
paper-1.12.2-test-server/ai-blwtc-bossbar-20260602-rcon-clear-online.log
paper-1.12.2-test-server/ai-blwtc-bossbar-20260602-latest-before-stop.log
paper-1.12.2-test-server/ai-blwtc-bossbar-20260602-final-latest.log
paper-1.12.2-test-server/ai-blwtc-20260602-bossbar-modelid-smoke-backup/
客户端自动化测试工作区/runs/20260602-blwtc-bossbar-real-client/control/client-task.properties
客户端自动化测试工作区/runs/20260602-blwtc-bossbar-real-client/control/client-response.properties
客户端自动化测试工作区/runs/20260602-blwtc-bossbar-real-client/events.jsonl
```

### 当前风险

- Folia 完整 region-safe 清理仍未实现，当前 Folia 产物仍是安全拒绝世界实体扫描。
- 本轮 BossBar 以真实在线玩家和服务端无异常验证为主，没有额外给客户端 Mod 增加 BossBar 屏幕像素断言；后续如果要验证视觉位置，应新增截图型场景。
- 测试服上仍有其他插件 Druid/MySQL 连接失败噪声，与 BLWorldTrashCan 本轮改动无关。

## 2026-06-02 Legacy 1.12 PAPI 变量补齐

继续按旧功能覆盖矩阵复盘时，确认旧插件所有版本都注册了 `%Wtc_ClearTime%`，而重构后的 Legacy 1.12 产物只有 `PlaceholderAPI` softdepend，没有实际注册 expansion。Bukkit、Paper、Folia 三个产物已经有对应实现，因此本轮把同等逻辑补到 Legacy。

### 实现

- `bl-world-trashcan-plugin-legacy-1_12/pom.xml` 增加 `me.clip:placeholderapi:2.11.6` provided 依赖。
- 新增 `BLWorldTrashCanLegacyExpansion`，变量前缀保持旧写法 `Wtc`，`ClearTime` 返回 `plugin.getRemainingClearSeconds()`。
- `BLWorldTrashCanLegacyPlugin` 启动时检测 `PlaceholderAPI`，存在则注册 `%Wtc_ClearTime%`，不存在则只输出跳过日志。
- `README.md` 的 PAPI 变量说明改为四个产物均支持 `%Wtc_ClearTime%`。

### 构建验证

- 使用 UTF-8 no-BOM 参数文件重编 Legacy 插件类；第一次误用 BOM 参数文件时，`javac` 报 `无效的标记: ﻿-encoding`，随后改为 `System.Text.UTF8Encoding(false)` 重新生成后通过。
- `CorePolicySelfTest passed`。
- 重新生成 `dist/BLWorldTrashCan-legacy-1.12.jar`，大小 `140572` bytes。
- 包内检查包含 `BLWorldTrashCanLegacyExpansion.class`、`plugin.yml`、`platform.yml`，未打入 `me/clip` PlaceholderAPI 本体类。
- `javap` 检查 `BLWorldTrashCanLegacyExpansion` 和 `BLWorldTrashCanLegacyPlugin` 均为 `major version: 52`。

### 1.12.2 实服验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
papi parse --null %Wtc_ClearTime%
stop
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 和 `PlaceholderAPI` 均为启用状态。
- `blwtc platform` 显示 `legacy-1.12`。
- `blwtc stats` 输出下次清理剩余秒数。
- `papi parse --null %Wtc_ClearTime%` 返回 `296`，证明 legacy PAPI 变量可用。
- `latest.log` 出现 `[PlaceholderAPI] Successfully registered internal expansion: Wtc [0.1.0-SNAPSHOT]` 和 `[BLWorldTrashCan] [PlaceholderAPI] 已注册变量: %Wtc_ClearTime%`。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError`、`Could not pass` 或插件启用失败。

保留证据：

```text
paper-1.12.2-test-server/ai-blwtc-legacy-papi-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-legacy-papi-20260602-stop.log
paper-1.12.2-test-server/ai-blwtc-legacy-papi-20260602-final-latest.log
```

测试服噪声：

- 其他插件继续输出 MySQL 连接 `localhost:3306/test` 失败和 MythicMobs 旧版本异常，与 BLWorldTrashCan 本轮改动无关。

## 2026-06-02 旧 add 命令世界参数兼容

复盘旧插件命令时，确认旧入口支持 `/WorldListTrashCan add <数量>` 和 `/WorldListTrashCan add <世界名> <数量>` 两种参数形态。重构后四个平台命令只保留了玩家当前世界写法，控制台无法指定世界，本轮补回旧功能。

### 实现

- Legacy、Bukkit、Paper、Folia 四个平台命令类的 `handleAdd` 不再直接强制 `requirePlayer`。
- `/blwtc add <数量>` 保持玩家当前世界逻辑；控制台使用该写法时明确提示必须指定世界名。
- `/blwtc add <世界名> <数量>` 支持控制台和管理员指定世界；世界不存在时返回明确错误。
- 帮助文案和 `README.md` 同步增加 `/blwtc add <世界名> <数量>`。
- 根目录工作流补充：旧命令兼容矩阵必须拆分参数形态和执行者形态，不能只验证命令名。

### 构建验证

- 本机 `mvn` 仍不在 PATH，本轮使用 UTF-8 no-BOM 参数文件增量重编四个平台命令类。
- Legacy/Bukkit 命令类使用 `--release 8`，Paper/Folia 命令类使用 `--release 17`。
- 使用 JDK 21 `jar.exe` 更新四个 dist jar 中的命令 class。
- `CorePolicySelfTest passed`。
- `javap` 检查结果：Legacy/Bukkit 命令 class 为 `major version: 52`，Paper/Folia 命令 class 为 `major version: 61`。
- 包内检查四个产物仍包含 `plugin.yml`、`platform.yml`；Legacy 产物仍包含 `BLWorldTrashCanLegacyExpansion.class`。
- 更新后产物大小：Legacy `140838` bytes，Bukkit `142459` bytes，Paper `142686` bytes，Folia `147047` bytes。

### 1.12.2 实服验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

RCON 覆盖：

```text
plugins
blwtc platform
blwtc stats
blwtc add world 1
blwtc add missing_world 1
blwtc add 1
papi parse --null %Wtc_ClearTime%
stop
```

结果：

- `plugins` 显示 `BLWorldTrashCan` 和 `PlaceholderAPI` 均为启用状态。
- `blwtc platform` 显示 `legacy-1.12`。
- `blwtc add world 1` 返回 `世界 world 垃圾桶上限已调整为 4`。
- `plugins/BLWorldTrashCan/data/worlds.yml` 落盘为 `world.max-count: 4`。
- `blwtc add missing_world 1` 返回 `未找到世界: missing_world`。
- 控制台执行 `blwtc add 1` 返回必须指定世界名，并输出两种用法。
- `papi parse --null %Wtc_ClearTime%` 返回 `313`，证明本轮改动没有破坏 Legacy PAPI 变量。
- 日志未发现 BLWorldTrashCan 自身的 `UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError`、`Could not pass` 或插件启用失败。

保留证据：

```text
paper-1.12.2-test-server/ai-blwtc-add-world-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-add-world-20260602-stop.log
paper-1.12.2-test-server/ai-blwtc-add-world-20260602-final-latest.log
```

测试服噪声：

- 其他插件继续输出 MySQL 连接 `localhost:3306/test` 失败、EasyCore 依赖缺失和部分前置启用失败，与 BLWorldTrashCan 本轮命令改动无关。

## 2026-06-02 Folia region-safe 清理入口补齐

前面 Folia 首轮 smoke 已证明普通 Bukkit 风格的 `world.getEntities()` 不能放在 global scheduler 上。本轮继续收口旧功能时，先把 Folia 清理从“拒绝危险扫描”推进到“专用 region/entity scheduler 清理”，但不把整产物声明为 `FOLIA_REGION_SAFE`，因为共享实体数量限制还没有 Folia 专用实现。

### 实现

- 新增 `FoliaRegionCleanupFeature`：
  - global scheduler 只负责收集世界和已加载 chunk。
  - 每个 chunk 交给 `Bukkit.getRegionScheduler().run(...)` 读取 `chunk.getEntities()`。
  - 物品所在 region 只读取物品实体和快照。
  - 世界垃圾桶写入先取位置快照，再切到箱子所在 region 调 `routeWorldTrashAt(...)`。
  - 路由成功后通过 `item.getScheduler().execute(...)` 回到物品实体 region 删除掉落物。
  - 清理统计使用线程安全增量方法累计，完成后输出 `[FoliaCleanup]` 汇总日志。
- `BLWorldTrashCanFoliaPlugin` 不再注册共享 `CleanupFeature`，改注册 `FoliaRegionCleanupFeature`。
- Folia `/blwtc clear` 改为异步启动语义：命令只提示已启动，最终结果看后台 `[FoliaCleanup]` 或后续 `/blwtc stats`。
- `FoliaPlatform` 增加 `SCHEDULER_REGION` 能力，但仍不声明 `FOLIA_REGION_SAFE`。
- Folia 产物暂不注册共享 `EntityLimitFeature`，避免 `world.getEntities()` / `getNearbyEntities()` 在 Folia 上跨 region 访问。
- Folia 后台测试入口 `debugworldtrash`、`debugroute`、`debugdrop`、`debugdamage` 改为提交到玩家实体 scheduler 执行，减少测试命令自身造成的 Folia 线程噪声。

### 构建

本机 `mvn` 仍不在 PATH，本轮使用旧参数文件模板生成 UTF-8 无 BOM 参数文件，重编：

```text
core -> config -> storage -> shared-bukkit -> platform-folia -> plugin-folia
```

结果：

```text
compile-folia-region-cleanup passed
dist/BLWorldTrashCan-folia-1.20.jar -> 173760 bytes
BLWorldTrashCanFoliaPlugin.class -> major version 61
FoliaRegionCleanupFeature.class -> major version 61
plugin.yml -> main=pixeltech.bluenine.blworldtrashcan.plugin.folia.BLWorldTrashCanFoliaPlugin, folia-supported=true
platform.yml -> target=folia-1.20
```

### Folia 1.20.1 实服验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\folia-1.20.1-test-server
```

控制台命令：

```text
blwtc platform
blwtc stats
execute in minecraft:overworld run summon item 0 80 0 {Item:{id:"minecraft:cobblestone",Count:4b}}
blwtc clear
blwtc stats
stop
```

结果：

- 插件正常启用，`platform` 显示 `folia-1.20`。
- 能力报告显示 `scheduler-global: 启用`、`scheduler-region: 启用`、`folia-region-safe: 禁用`。
- 启动日志显示 `[FoliaCleanup] region-safe 定时清理已启动，间隔 360 秒`。
- 控制台生成 4 个圆石掉落物后执行 `/blwtc clear`，命令返回已启动异步清理。
- 清理完成日志：

```text
[FoliaCleanup] worlds=3, itemsRouted=4, itemsRemoved=0, itemsSkipped=0, entitiesRemoved=0, entitiesSkipped=30, worldTrashSkippedUnloadedChunks=0, globalTrashRefreshed=false
```

- 后续 `/blwtc stats` 显示 `回收物品: 4 (世界 0, 个人 0, 公共 4)`，公共垃圾桶当前物品 `4`、堆叠 `1`。
- 日志未发现 BLWorldTrashCan 自身的 `IllegalStateException`、Folia thread-check failure、`Could not pass`、`NoSuchMethodError`、`UnsupportedClassVersionError` 或插件启用失败。

保留证据：

```text
folia-1.20.1-test-server/ai-blwtc-folia-region-cleanup-20260602-latest.log
folia-1.20.1-test-server/ai-blwtc-folia-region-cleanup-20260602-commands.log
```

### 当前风险

- Folia 世界清理已经通过基础实服 smoke，但这只覆盖已加载 chunk 内掉落物进入公共垃圾桶的链路。
- Folia 的 BossBar、Title、Sound 等通知没有做客户端级视觉验收，因此当前仍不声明整产物 `FOLIA_REGION_SAFE`。

## 2026-06-02 Folia 专用实体限制补齐

上一轮 Folia 清理补齐后，旧功能矩阵里还剩实体数量限制在 Folia 产物中被安全禁用。本轮补 Folia 专用实现，避免共享 `EntityLimitFeature` 里的 `world.getEntities()` 和 `getNearbyEntities()` 跨 region 访问。

### 实现

- 新增 `FoliaEntityLimitFeature`。
- 通过 `EntityAddToWorldEvent` / `EntityRemoveFromWorldEvent` 维护 `world + entityType` 数量缓存。
- 启用实体限制时，使用 global scheduler 分派已加载 chunk，再用 `RegionScheduler` 在 chunk 所在 region 内读取 `chunk.getEntities()`，定期复算缓存。
- `CreatureSpawnEvent` 中按缓存判断单世界实体上限；放行时立即按实体 UUID 预占计数，避免同一 tick 连续生成绕过上限，后续 `EntityAddToWorldEvent` 用 UUID 去重，不会双计数；如果实体没有进入可调度状态或下一 tick 已无效，会释放预占计数。
- 密集实体限制不再调用 `getNearbyEntities()`，只扫描当前 chunk 内、半径范围内的同类型实体；这是为了保持 Folia region-safe，代价是跨 chunk 边界的密集实体不会在这一轮被同一次生成事件清理。
- `BLWorldTrashCanFoliaPlugin` 接回实体限制功能，但注册的是 Folia 专用 `FoliaEntityLimitFeature`。

### 构建

继续使用手工 `javac` 和 JDK 21 `jar.exe`：

```text
compile-folia-entitylimit passed
dist/BLWorldTrashCan-folia-1.20.jar -> 187351 bytes
FoliaEntityLimitFeature.class -> major version 61
```

### Folia 1.20.1 实服验证

测试前临时写入 `plugins/BLWorldTrashCan/entity-limits.yml`：

```yaml
world-limits:
  enabled: true
  ignored-worlds: []
  defaults:
    - entity: "PIG"
      max-count: 1

gather-limits:
  enabled: true
  drop-items: false
  ignored-worlds: []
  defaults:
    - entity: "COW"
      max-count: 1
      radius: 8
      remove-count: 1
```

控制台覆盖：

```text
blwtc reload
kill @e[type=pig]
kill @e[type=cow]
kill @e[type=item]
blwtc reload
execute in minecraft:overworld run summon pig 0 80 0
execute in minecraft:overworld run summon pig 0 80 0
execute in minecraft:overworld run summon cow 2 80 0
execute in minecraft:overworld run summon cow 2 80 0
execute in minecraft:overworld run summon item 0 80 0 {Item:{id:"minecraft:cobblestone",Count:2b}}
blwtc clear
blwtc stats
stop
```

结果：

- `FoliaEntityLimit` 启动缓存复算任务，间隔 `60` 秒。
- PIG 第二次生成被单世界上限拦截：

```text
[FoliaEntityLimit] 已拦截实体生成 world=world, type=PIG, current=1, max=1
```

- COW 当前 chunk 密集限制生效：

```text
[FoliaEntityLimit] 已清理密集实体 world=world, type=COW, removed=1, radius=8, scope=current-chunk
```

- 清理复测仍通过：

```text
[FoliaCleanup] worlds=3, itemsRouted=9, itemsRemoved=0, itemsSkipped=0, entitiesRemoved=0, entitiesSkipped=24, worldTrashSkippedUnloadedChunks=0, globalTrashRefreshed=false
```

- 后续 `/blwtc stats` 显示最近清理世界数 `3`，回收物品 `9`。
- 测试后已恢复原 `entity-limits.yml`，未删除 logs、world、cache 或 assets。
- 日志未发现 BLWorldTrashCan 自身的 `IllegalStateException`、Folia thread-check failure、`Could not pass`、`NoSuchMethodError`、`UnsupportedClassVersionError` 或插件启用失败。

保留证据：

```text
folia-1.20.1-test-server/ai-blwtc-folia-entitylimit-20260602-final-latest.log
folia-1.20.1-test-server/ai-blwtc-folia-entitylimit-20260602-final-commands.log
folia-1.20.1-test-server/ai-blwtc-folia-entitylimit-20260602-entity-limits-before.yml
```

### 当前风险

- Folia 密集实体限制当前只扫描当前 chunk；这是为了不跨 region 查询。它能覆盖大多数堆叠在同一 chunk 的密集实体，但不等价于旧 Bukkit `getNearbyEntities(radius)` 的跨 chunk 语义。
- Folia 的 BossBar、Title、Sound 等通知仍没有做客户端级视觉验收，因此当前仍不声明整产物 `FOLIA_REGION_SAFE`。

## 2026-06-02 Folia 清理通知后台链路补齐

继续审计旧功能矩阵时，发现 Folia 专用 `FoliaRegionCleanupFeature` 已经替代共享 `CleanupFeature`，但尚未发送 `notify.yml` 的倒计时通知。共享清理原本支持 Chat、ActionBar、BossBar、Title、Sound 和 Command，因此这是 Folia 产物的旧功能缺口。

### 实现

- `FoliaRegionCleanupFeature` 补齐 `sendNotify`：
  - Chat：控制台日志直接输出；玩家聊天消息提交到玩家实体 scheduler。
  - ActionBar、Title、Sound：提交到玩家实体 scheduler。
  - BossBar：在全局上下文更新标题、样式、颜色和进度，玩家加入/移除提交到玩家实体 scheduler。
  - Command：保留旧配置语义，从全局上下文执行控制台命令。
- Folia 自动倒计时每秒发送剩余秒数通知。
- Folia 异步清理完成后发送 `0` 完成通知，再按公共垃圾桶是否刷新发送 `-1` 或 `-2`。
- 停用功能时取消 BossBar 延迟移除任务，并尝试移除 BossBar。

### 构建

```text
dist/BLWorldTrashCan-folia-1.20.jar -> 199084 bytes
FoliaRegionCleanupFeature.class -> major version 61
```

### Folia 1.20.1 后台 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\folia-1.20.1-test-server
```

测试前临时改动：

- `cleanup.yml`：`interval-seconds: 2`
- `notify.yml`：
  - Chat 开启并输出 `AI Folia notify 2/1/done/remain`
  - ActionBar、BossBar、Title、Sound 开启
  - Command 开启并执行 `say AI_FOLIA_NOTIFY_COMMAND_DONE`

结果：

- 自动清理按 2 秒间隔启动。
- `latest.log` 连续出现倒计时通知：

```text
AI Folia notify 2
AI Folia notify 1
```

- 清理完成后出现统计和完成通知：

```text
[FoliaCleanup] worlds=3, itemsRouted=4, itemsRemoved=0, itemsSkipped=0, entitiesRemoved=0, entitiesSkipped=22, worldTrashSkippedUnloadedChunks=0, globalTrashRefreshed=false
AI Folia notify done 4 4 0
AI Folia notify remain 2
[Server] AI_FOLIA_NOTIFY_COMMAND_DONE
```

- 后续自动清理继续输出完成通知，公共垃圾桶刷新轮次出现 `globalTrashRefreshed=true`。
- 测试后已恢复原 `cleanup.yml` 和 `notify.yml`，未删除 logs、world、cache 或 assets。
- 日志未发现 BLWorldTrashCan 自身的 `IllegalStateException`、Folia thread-check failure、`Could not pass`、`NoSuchMethodError`、`UnsupportedClassVersionError` 或插件启用失败。

保留证据：

```text
folia-1.20.1-test-server/ai-blwtc-folia-notify-20260602-latest.log
folia-1.20.1-test-server/ai-blwtc-folia-notify-20260602-commands.log
folia-1.20.1-test-server/ai-blwtc-folia-notify-20260602-cleanup-before.yml
folia-1.20.1-test-server/ai-blwtc-folia-notify-20260602-notify-before.yml
```

### 当前风险

- 本轮是后台 smoke，没有 Folia 1.20 客户端在线验证 ActionBar、BossBar、Title 和 Sound 的屏幕/听觉表现。
- Command 通知允许服主配置任意控制台命令；这类命令可能触发其它插件的非 region-safe 行为，因此当前仍不声明整产物 `FOLIA_REGION_SAFE`。

## 2026-06-02 默认通知配置补回清理后提醒

继续对照旧 `WorldListTrashCan/config.yml` 时，发现旧默认配置中 Chat、ActionBar、Title 都包含清理后的 `-1/-2` 提醒；新拆分后的默认 `notify.yml` 只有 BossBar 默认包含这两类提醒。运行代码已经支持 `-1/-2`，缺口在四个平台产物的默认资源文件。

### 实现

- 四个平台默认 `notify.yml` 的 Chat、ActionBar、Title 都补回：
  - `-1`：本轮清理完成但公共垃圾桶未刷新。
  - `-2`：公共垃圾桶已刷新。
- 保留原有 BossBar `-1/-2` 默认消息。
- 不改通知解析器和运行逻辑。

### 打包和检查

本轮是资源默认值修复，使用 JDK 21 `jar.exe` 更新四个 dist jar 内的 `notify.yml`：

```text
dist/BLWorldTrashCan-legacy-1.12.jar      -> 140868 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar -> 142489 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar  -> 142716 bytes
dist/BLWorldTrashCan-folia-1.20.jar       -> 199114 bytes
```

包内 `notify.yml` 检查结果：

```text
BLWorldTrashCan-legacy-1.12.jar notify-minus1=4 notify-minus2=4
BLWorldTrashCan-bukkit-1.13-1.15.jar notify-minus1=4 notify-minus2=4
BLWorldTrashCan-paper-1.16-1.20.jar notify-minus1=4 notify-minus2=4
BLWorldTrashCan-folia-1.20.jar notify-minus1=4 notify-minus2=4
```

`git diff --check` 未发现空白错误；Git 仅提示这些 YAML 在下次检出时可能按仓库设置转换为 CRLF。

## 2026-06-02 高版本 PAPI 变量补测

继续审计 PAPI 覆盖时，README 已写四个平台都提供 `%Wtc_ClearTime%`，但此前只有 Legacy 1.12 在 PlaceholderAPI 2.11.6 下做过实服解析。本轮补测 Bukkit 1.13.2、Paper 1.20.4，并尝试验证 Folia 1.20.1。

### Bukkit 1.13.2

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.13.2-test-server
```

临时部署：

- `plugins/placeholderapi-2.11.6.jar`
- 当前本地 `dist/BLWorldTrashCan-bukkit-1.13-1.15.jar`

RCON：

```text
plugins
papi parse --null %Wtc_ClearTime%
blwtc stats
```

结果：

```text
Plugins (2): BLWorldTrashCan, PlaceholderAPI
papi parse --null %Wtc_ClearTime% -> 315
blwtc stats -> 下次清理剩余秒数: 314
```

保留证据：

```text
paper-1.13.2-test-server/ai-blwtc-papi-bukkit113-20260602-console.log
paper-1.13.2-test-server/ai-blwtc-papi-bukkit113-20260602-rcon.log
paper-1.13.2-test-server/ai-blwtc-papi-bukkit113-20260602-rcon-stop.log
```

### Paper 1.20.4

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.20.4-test-server
```

临时部署：

- `plugins/placeholderapi-2.11.6.jar`
- 当前本地 `dist/BLWorldTrashCan-paper-1.16-1.20.jar`

RCON：

```text
plugins
papi parse --null %Wtc_ClearTime%
blwtc stats
```

结果：

```text
Server Plugins (2): BLWorldTrashCan, PlaceholderAPI
papi parse --null %Wtc_ClearTime% -> 333
blwtc stats -> 下次清理剩余秒数: 332
```

保留证据：

```text
paper-1.20.4-test-server/ai-blwtc-papi-paper1204-20260602-console.log
paper-1.20.4-test-server/ai-blwtc-papi-paper1204-20260602-rcon.log
paper-1.20.4-test-server/ai-blwtc-papi-paper1204-20260602-rcon-stop.log
```

### Folia 1.20.1

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\folia-1.20.1-test-server
```

结果：

- 本地 `PlaceholderAPI 2.11.6` 在 Folia 启动阶段被拒绝加载：

```text
Could not load plugin 'PlaceholderAPI v2.11.6' as it is not marked as supporting Folia!
```

- BLWorldTrashCan 正常启用，但因为未检测到 PlaceholderAPI，跳过变量注册：

```text
[BLWorldTrashCan] [PlaceholderAPI] 未检测到 PlaceholderAPI，跳过变量注册。
```

- Folia RCON 对 `plugins`、`papi parse --null %Wtc_ClearTime%`、`blwtc stats` 均返回 `Error executing ... (null)`，本轮以 console/latest 日志作为真实启动证据。
- 测试后已把临时 `plugins/placeholderapi-2.11.6.jar` 改名为 `placeholderapi-2.11.6.jar.disabled-ai-papi-folia-unsupported-20260602`，避免后续 Folia 测试继续被该前置污染。

保留证据：

```text
folia-1.20.1-test-server/ai-blwtc-papi-folia1201-20260602-console.log
folia-1.20.1-test-server/ai-blwtc-papi-folia1201-20260602-rcon.log
folia-1.20.1-test-server/ai-blwtc-papi-folia1201-20260602-rcon-stop.log
```

### 当前风险

- Folia 产物的 PAPI 变量代码已经存在，但本机缺少支持 Folia 的 PlaceholderAPI 前置，因此 Folia PAPI 变量仍未完成运行态验收。
- 不能用 Bukkit/Paper 的 PAPI 验证结果替代 Folia；后续拿到 Folia-compatible PlaceholderAPI 后，需要重新执行 `plugins`、`papi parse --null %Wtc_ClearTime%` 和 `blwtc stats`。

## 2026-06-02 多语言消息服务补齐

继续审计旧插件消息文件时，确认旧 `WorldListTrashCan/src/main/resources/message/` 提供简中、繁中、英文、西语多语言文件；新实现此前只保留了 `messages/message_zh.yml`，且大量正式玩家文案硬编码在服务和命令类中。

### 实现

- 新增 `BukkitMessageService`，按 `config.yml -> language` 加载 `plugins/BLWorldTrashCan/messages/<文件名>`。
- 四个平台入口在启动和 `/blwtc reload` 时重载语言文件，并保存 jar 内自带语言资源。
- 旧服外部语言文件缺少新节点时，会继续回退 jar 内默认 `message_zh.yml` 节点，避免升级后提示空白。
- 四个平台产物都内置 `message_zh.yml`、`message_zh_TW.yml`、`message_en.yml`、`message_es.yml`。
- 已外置正式玩家文案：主命令、帮助、平台能力、统计、add 命令、公共/个人垃圾桶、世界垃圾桶创建/移除、黑名单 GUI、防丢弃模式、look 查询和手持物品/区块实体查询。
- `debug*` 后台测试命令的夹具返回仍保留内部中文，用于测试诊断，不作为普通玩家语言包范围。

### 构建

本机 `mvn` 不在 PATH，本轮使用 JDK 21 `javac` / `jar.exe` 手工构建：

```text
build/compile-message-service-20260602
build/package-message-service-20260602-full2
```

编译顺序：

```text
core -> config -> storage -> shared-bukkit -> platform-legacy -> platform-bukkit -> platform-paper -> platform-folia -> plugin-legacy -> plugin-bukkit -> plugin-paper -> plugin-folia
```

验证结果：

```text
shared-bukkit + plugin-legacy + plugin-bukkit + plugin-paper + plugin-folia 重编译通过
Legacy/Bukkit/shared class major version = 52
Paper/Folia class major version = 61
```

最终本地 dist：

```text
dist/BLWorldTrashCan-legacy-1.12.jar      159690 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar 161365 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar  161564 bytes
dist/BLWorldTrashCan-folia-1.20.jar       217388 bytes
```

包内检查确认 Legacy 与 Folia jar 均包含：

```text
messages/message_zh.yml
messages/message_zh_TW.yml
messages/message_en.yml
messages/message_es.yml
pixeltech/bluenine/blworldtrashcan/bukkit/message/BukkitMessageService.class
```

### Legacy 1.12 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份旧 `plugins/BLWorldTrashCan-legacy-1.12.jar`。
2. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
3. 临时把 `plugins/BLWorldTrashCan/config.yml` 的 `language` 改为 `message_en.yml`。
4. 启动 1.12.2 测试服，不删除 `logs`、`world*`、`cache`、`assets`。
5. RCON 执行 `blwtc reload`、`blwtc help`、`blwtc platform`、`blwtc stats`。
6. 停服后恢复 `config.yml` 为 `message_zh.yml`，保留测试日志和生成的语言文件。

结果：

```text
[BLWorldTrashCan] [Message] 已加载语言文件: messages/message_en.yml
blwtc reload -> BLWorldTrashCan reloaded.
blwtc help -> Show help / Show this build's capabilities / Run one cleanup immediately ...
blwtc platform -> Current platform: legacy-1.12
blwtc stats -> Cleanup statistics / Seconds until next cleanup
```

证据：

```text
paper-1.12.2-test-server/ai-blwtc-message-service-20260602-console.log
paper-1.12.2-test-server/ai-blwtc-message-service-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-message-service-20260602-stop.log
paper-1.12.2-test-server/ai-blwtc-message-service-20260602-error.log
```

测试环境噪声：

- 本轮 console 日志里有其它前置插件的 MySQL、WcpeLib、EasyLib、MythicMobs 报错。
- BLWorldTrashCan 自身日志显示正常加载、重载和停服，未出现插件启用失败、类版本错误、`NoSuchMethodError` 或 `NoSuchFieldError`。

## 2026-06-02 Legacy 1.12 打开权限补齐

继续逐项对照旧 `WorldListTrashCan` 命令时，发现 Bukkit、Paper、Folia 三个命令类已经在 `global/globaltrash/trash` 与 `personal/playertrash` 分支检查打开权限，但 Legacy 1.12 命令类只做了玩家身份检查后直接打开 GUI。旧插件对公共垃圾桶和个人垃圾桶打开入口分别使用 `WorldListTrashCan.GlobalTrashOpen` 与 `WorldListTrashCan.PlayerTrash`，因此这是 Legacy 产物的旧行为缺口。

### 实现

- `BLWorldTrashCanLegacyCommand` 的公共垃圾桶入口补齐权限校验：
  - 新权限：`blworldtrashcan.global.open`
  - 旧权限：`WorldListTrashCan.GlobalTrashOpen`
- `BLWorldTrashCanLegacyCommand` 的个人垃圾桶入口补齐权限校验：
  - 新权限：`blworldtrashcan.personal.open`
  - 旧权限：`WorldListTrashCan.PlayerTrash`
- 权限缺失时复用语言文件中已有的 `command.no-global-open-permission` 与 `command.no-personal-open-permission` 节点。

### 构建

只变更 Legacy 1.12 命令类，本轮未重打其它平台产物：

```text
javac @build/compile-message-service-20260602/plugin-legacy.args
build/package-legacy-permission-20260602
dist/BLWorldTrashCan-legacy-1.12.jar -> 159822 bytes
```

静态检查：

```text
BLWorldTrashCanLegacyCommand major version: 52
jar 内包含 command.no-global-open-permission
jar 内包含 command.no-personal-open-permission
jar 内包含 WorldListTrashCan.GlobalTrashOpen
jar 内包含 WorldListTrashCan.PlayerTrash
```

### 1.12.2 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份旧 `plugins/BLWorldTrashCan-legacy-1.12.jar` 为 `BLWorldTrashCan-legacy-1.12.jar.ai-backup-legacy-permission-20260602-2234`。
2. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
3. 使用 Java 8 和相对 `-jar paper-1.12.2-1620.jar` 启动测试服，不删除 `logs`、`world*`、`cache`、`assets`。
4. RCON 执行 `plugins`、`blwtc platform`、`blwtc stats`、`WorldListTrashCan help`、`WTC stats`、`wtc globaltrash`、`wtc playertrash`、`blwtc reload`。
5. RCON `stop` 停服并保存 `logs/latest.log` 快照。

结果：

```text
plugins -> BLWorldTrashCan 已启用
blwtc platform -> 当前平台 legacy-1.12
WorldListTrashCan help -> 返回帮助
WTC stats -> 返回统计
wtc globaltrash -> 该命令只能由玩家执行
wtc playertrash -> 该命令只能由玩家执行
blwtc reload -> BLWorldTrashCan 已重载
```

本轮没有启动真实客户端，因此未做“玩家缺少权限时不能打开 GUI”的负向视觉验证；该分支已通过字节码和代码静态确认进入 Legacy jar，后续如安装权限插件或准备无权限真实玩家，可补做玩家负向用例。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-legacy-permission-20260602-console.log
paper-1.12.2-test-server/ai-blwtc-legacy-permission-20260602-error.log
paper-1.12.2-test-server/ai-blwtc-legacy-permission-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-legacy-permission-20260602-stop.log
paper-1.12.2-test-server/ai-blwtc-legacy-permission-20260602-final-latest.log
```

测试环境噪声：

- 测试服仍有 WcpeLib、EasyLib、CoreBridge、MythicMobs 和 MySQL 连接失败相关噪声。
- 使用更窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

## 2026-06-02 垃圾桶 GUI 操作权限 OP 旁路补齐

继续对照旧 GUI 权限逻辑时，发现旧公共垃圾桶和个人垃圾桶在玩家从 GUI 取出、放入物品时，权限判断都是 `player.isOp()` 或旧权限节点通过。新实现已经支持新旧权限节点，但没有显式保留 OP 旁路；在默认权限下通常无差异，但如果权限插件显式否定节点，旧 OP 仍能操作，新实现可能被拒绝。

### 实现

- `GlobalTrashService` 的取出、放入权限改为通过 `hasTrashPermission()` 判断。
- `PersonalTrashService` 的取出、放入权限改为通过 `hasTrashPermission()` 判断。
- 两个服务的判断逻辑均为：

```text
player.isOp()
或现代权限节点
或旧 WorldListTrashCan 权限节点
```

涉及权限：

```text
blworldtrashcan.global.take
WorldListTrashCan.GlobalTrashTakeItem
blworldtrashcan.global.put
WorldListTrashCan.GlobalTrashPutItem
blworldtrashcan.personal.take
WorldListTrashCan.PersonalTrashTakeItem
blworldtrashcan.personal.put
WorldListTrashCan.PersonalTrashPutItem
```

### 构建

重编译：

```text
core
config
storage
shared-bukkit
platform-legacy
platform-bukkit
platform-paper
platform-folia
plugin-legacy
plugin-bukkit
plugin-paper
plugin-folia
```

重新打包：

```text
build/package-trash-op-permission-20260602
dist/BLWorldTrashCan-legacy-1.12.jar       160987 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  162529 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   162709 bytes
dist/BLWorldTrashCan-folia-1.20.jar        218553 bytes
```

静态检查：

```text
Legacy jar 内 GlobalTrashService.hasTrashPermission 包含 Player.isOp()
Legacy jar 内 PersonalTrashService.hasTrashPermission 包含 Player.isOp()
```

### 1.12.2 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份旧 `plugins/BLWorldTrashCan-legacy-1.12.jar` 为 `BLWorldTrashCan-legacy-1.12.jar.ai-backup-trash-op-permission-*`。
2. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
3. 使用 Java 8 和相对 `-jar paper-1.12.2-1620.jar` 启动测试服。
4. RCON 执行 `plugins`、`blwtc platform`、`blwtc stats`、`blwtc reload`、`blwtc clear`、`blwtc stats`。
5. RCON `stop` 停服并保存 `latest.log` 快照。

结果：

```text
plugins -> BLWorldTrashCan 已启用
blwtc platform -> 当前平台 legacy-1.12
blwtc stats -> 正常返回公共垃圾桶页数、当前物品、下次清理秒数
blwtc reload -> BLWorldTrashCan 已重载
blwtc clear -> 清理完成，未发现异常
```

本轮没有安装权限插件做“OP 被显式否定权限节点时仍能取放”的真实 GUI 负向测试；该分支通过代码和 jar 字节码证明已进入最终产物。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-trash-op-permission-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-trash-op-permission-20260602-final-latest.log
```

测试环境噪声：

- 测试服仍有 WcpeLib、EasyLib、CoreBridge、MythicMobs 和 MySQL 连接失败相关噪声。
- 使用更窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

## 2026-06-02 实体清理总开关迁移补齐

继续对照旧 `WorldListTrashCan/config.yml` 时，发现旧配置 `Set.ClearEntity.Flag` 是实体清理总开关；新拆分后的 `cleanup.yml` 只保留了经验球、怪物、动物、投射物、自定义名和船内实体等子开关。这样旧服如果关闭整个实体清理，迁移到新版本后仍可能按子项清理实体，属于旧行为缺口。

### 实现

- `cleanup.yml` 增加：

```yaml
entities:
  # 是否启用实体清理总开关。
  # 默认 true；改为 false 时，经验球、怪物、动物、投射物、实体黑名单等实体清理规则都会整体跳过。
  enabled: true
```

- 四个平台默认配置均补齐该中文注释配置项。
- `ConfigBundleLoader` 读取 `entities.enabled`，默认 `true`，避免现有配置缺少该字段时行为突变。
- `CleanupSettings` 增加 `entityCleanupEnabled` 快照和 `isEntityCleanupEnabled()`。
- `DefaultCleanupPolicy.decideEntity()` 在实体总开关关闭时直接返回 `SKIP/entity-cleanup-disabled`，总开关优先于经验球、怪物、投射物和实体黑名单。
- `BukkitLegacyConfigMigrator` 将旧字段 `Set.ClearEntity.Flag` 迁移为 `entities.enabled`。
- `CorePolicySelfTest` 增加关闭实体总开关时 Zombie 被跳过的断言。

### 构建

重编译：

```text
core
config
storage
shared-bukkit
platform-legacy
platform-bukkit
platform-paper
platform-folia
plugin-legacy
plugin-bukkit
plugin-paper
plugin-folia
```

核心自测：

```text
CorePolicySelfTest passed
```

重新打包：

```text
build/package-entity-cleanup-toggle-20260602
dist/BLWorldTrashCan-legacy-1.12.jar       160843 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  162385 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   162565 bytes
dist/BLWorldTrashCan-folia-1.20.jar        218409 bytes
```

静态检查：

```text
Legacy/Bukkit class major version = 52
Paper/Folia class major version = 61
四个平台 jar 的 cleanup.yml 均包含 entities.enabled: true
Legacy jar 内 CleanupSettings 暴露 isEntityCleanupEnabled()
Legacy jar 内 DefaultCleanupPolicy 包含 entity-cleanup-disabled 分支
```

### 1.12.2 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份旧 `plugins/BLWorldTrashCan-legacy-1.12.jar` 为 `BLWorldTrashCan-legacy-1.12.jar.ai-backup-entity-toggle-20260602-*`。
2. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
3. 使用 Java 8 和相对 `-jar paper-1.12.2-1620.jar` 启动测试服。
4. RCON 执行 `plugins`、`blwtc platform`、`blwtc stats`、`blwtc reload`、`blwtc clear`、`blwtc stats`、`papi parse --null %Wtc_ClearTime%`。
5. 临时将测试服 `plugins/BLWorldTrashCan/cleanup.yml` 写入 `entities.enabled: false` 并 `blwtc reload`，尝试用原版 `summon` 生成 Zombie 做关闭语义运行态验证。
6. 恢复测试服 `cleanup.yml`，再次 `blwtc reload`，RCON `stop` 停服并保存 `latest.log` 快照。

结果：

```text
plugins -> BLWorldTrashCan 已启用
blwtc platform -> 当前平台 legacy-1.12
blwtc stats -> 正常返回公共垃圾桶页数、当前物品、下次清理秒数
blwtc reload -> BLWorldTrashCan 已重载
blwtc clear -> 清理完成，未发现异常
papi parse --null %Wtc_ClearTime% -> 356
```

原版召唤子用例未成立：

```text
summon Zombie 0 80 0 -> Cannot summon the object out of the world
summon zombie 0 64 0 -> Cannot summon the object out of the world
summon minecraft:zombie 0 64 0 -> Cannot summon the object out of the world
```

因此，本轮运行态只证明新 jar 在 1.12.2 测试服加载、reload、clear 和 PAPI 均正常；`entities.enabled: false` 的关闭语义由 `CorePolicySelfTest` 与 jar 字节码检查证明。测试服配置已恢复，未留下临时关闭项。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-entity-toggle-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-entity-toggle-20260602-disabled-rcon.log
paper-1.12.2-test-server/ai-blwtc-entity-toggle-20260602-disabled-rcon-2.log
paper-1.12.2-test-server/ai-blwtc-entity-toggle-20260602-restore-stop-rcon.log
paper-1.12.2-test-server/ai-blwtc-entity-toggle-20260602-final-latest.log
```

测试环境噪声：

- 测试服仍有 WcpeLib、EasyLib、CoreBridge、MythicMobs 和 MySQL 连接失败相关噪声。
- 使用更窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

## 2026-06-02 公共黑名单保存即时生效

继续审计 GUI 类旧功能语义时，发现世界黑名单保存后会立即写入 `WorldTrashRouter` 缓存，但公共黑名单保存只写入 `trash.yml` 并提示 `/blwtc reload 后完全生效`。旧插件关闭公共黑名单 GUI 后会更新全局黑名单集合并重载配置，基本是即时生效；新实现这里不应退化成需要服主手动 reload。

### 实现

- `BanGuiFeature` 增加 `reloadCallback`，公共黑名单 `trash.yml` 保存成功后调用。
- Legacy、Bukkit、Paper、Folia 四个平台入口都把自身 `reloadPlugin()` 传入 `BanGuiFeature`。
- 保存公共黑名单后会沿正式 reload 路径刷新：
  - `ConfigBundle`
  - `BukkitMessageService`
  - `GlobalTrashService`
  - `WorldTrashRouter`
  - 其它已注册功能模块
- 四个平台的 `message_zh.yml`、`message_zh_TW.yml`、`message_en.yml`、`message_es.yml` 中，`ban-gui.global-save-success` 从“需要 reload”改为“已立即生效”。

### 构建

重编译：

```text
shared-bukkit
plugin-legacy
plugin-bukkit
plugin-paper
plugin-folia
```

重新打包：

```text
build/package-globalban-instant-20260602
dist/BLWorldTrashCan-legacy-1.12.jar       160643 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  162185 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   162365 bytes
dist/BLWorldTrashCan-folia-1.20.jar        218209 bytes
```

静态检查：

```text
Legacy/Bukkit class major version = 52
Paper/Folia class major version = 61
BanGuiFeature jar 内包含 reloadCallback 和 refreshAfterGlobalBanSave
四个平台 jar 均包含 BanGuiFeature.class 和 messages/message_zh.yml
```

### 1.12.2 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份旧 `plugins/BLWorldTrashCan-legacy-1.12.jar` 为 `BLWorldTrashCan-legacy-1.12.jar.ai-backup-globalban-instant-20260602-2245`。
2. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
3. 使用 Java 8 和相对 `-jar paper-1.12.2-1620.jar` 启动测试服。
4. RCON 执行 `plugins`、`blwtc platform`、`blwtc stats`、`blwtc reload`、`blwtc stats`。
5. RCON `stop` 停服并保存 `logs/latest.log` 快照。

结果：

```text
plugins -> BLWorldTrashCan 已启用
blwtc platform -> 当前平台 legacy-1.12
blwtc stats -> 正常返回公共垃圾桶页数、当前物品、下次清理秒数
blwtc reload -> BLWorldTrashCan 已重载
```

本轮没有启动真实客户端操作 `/blwtc globalban` GUI，因此“关闭 GUI 后玩家收到已立即生效提示”的视觉链路尚未做真实玩家验证；代码路径和 jar 字节码已经确认保存后会调用正式 reload。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-globalban-instant-20260602-console.log
paper-1.12.2-test-server/ai-blwtc-globalban-instant-20260602-error.log
paper-1.12.2-test-server/ai-blwtc-globalban-instant-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-globalban-instant-20260602-stop.log
paper-1.12.2-test-server/ai-blwtc-globalban-instant-20260602-final-latest.log
```

测试环境噪声：

- 测试服仍有 WcpeLib、EasyLib、CoreBridge、MythicMobs 和 MySQL 连接失败相关噪声。
- 使用更窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

## 2026-06-02 命令权限 OP 旁路补齐

### 背景

继续旧功能差异审计时，对照旧 `WorldListTrashCan` 主命令发现：旧插件的 `reload/clear/add/global/playertrash/dropmode/look/ban/globalban` 等入口普遍存在 `sender.isOp()` 旁路或 OP 管理语义。重构后四个平台命令类此前主要依赖 `hasPermission(...)`，在权限插件显式改动默认权限时可能与旧插件行为不一致。

### 修改

- `BLWorldTrashCanLegacyCommand`
- `BLWorldTrashCanBukkitCommand`
- `BLWorldTrashCanCommand`
- `BLWorldTrashCanFoliaCommand`

四个平台命令类新增：

```text
hasAdminPermission(CommandSender sender)
hasAnyPermission(CommandSender sender, String... permissions)
```

权限语义：

- `reload/clear/add/debug*`：OP 或 `blworldtrashcan.admin`。
- `global/globaltrash/trash`：OP、`blworldtrashcan.global.open` 或旧 `WorldListTrashCan.GlobalTrashOpen`。
- `personal/playertrash`：OP、`blworldtrashcan.personal.open` 或旧 `WorldListTrashCan.PlayerTrash`。
- `dropmode`：OP、`blworldtrashcan.dropmode` 或旧 `WorldListTrashCan.DropMode`。
- `look`：OP、`blworldtrashcan.look` 或旧 `WorldListTrashCan.Look`。
- `ban/globalban`：OP、旧黑名单 GUI 权限或 `blworldtrashcan.admin`。

### 构建

全模块重新编译通过：

```text
core
config
storage
shared-bukkit
platform-legacy
platform-bukkit
platform-paper
platform-folia
plugin-legacy
plugin-bukkit
plugin-paper
plugin-folia
```

继续使用 JDK 21 `jar.exe` 更新四个 dist jar 的命令 class：

```text
dist/BLWorldTrashCan-legacy-1.12.jar       161178 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  162712 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   162895 bytes
dist/BLWorldTrashCan-folia-1.20.jar        218739 bytes
```

静态检查：

```text
Legacy/Bukkit class major version = 52
Paper/Folia class major version = 61
四个平台 jar 的命令 class 均包含 CommandSender.isOp()
plugin.yml main 与 platform.yml target 均正确
git diff --check 仅有 Windows LF -> CRLF 提示
```

### 1.12.2 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
2. 使用 Java 8 启动 `paper-1.12.2-1620.jar`。
3. RCON 执行 `plugins`、`blwtc platform`、`WorldListTrashCan platform`、`blwtc stats`、`blwtc reload`、`blwtc clear`、`blwtc add world 1`、`papi parse --null %Wtc_ClearTime%`、`blwtc debugstock`。
4. RCON `stop` 停服并保存 `logs/latest.log` 快照。

结果：

```text
plugins -> BLWorldTrashCan 已启用
blwtc platform -> 当前平台 legacy-1.12
WorldListTrashCan platform -> 当前平台 legacy-1.12
blwtc reload -> BLWorldTrashCan 已重载
blwtc clear -> 清理完成
blwtc add world 1 -> 世界 world 垃圾桶上限已调整
papi parse --null %Wtc_ClearTime% -> 返回剩余秒数
blwtc debugstock -> 正常返回垃圾桶库存
```

本轮没有接入真实玩家做负向权限测试；玩家专属命令的 OP 旁路已通过源码和最终 jar 字节码确认。测试服中 WcpeLib/MySQL 与 EasyCore 缺类仍有外部噪声，窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-command-op-permission-20260602-rcon.log
paper-1.12.2-test-server/ai-blwtc-command-op-permission-20260602-final-latest.log
```

## 2026-06-02 世界垃圾桶有效上限补齐

### 背景

继续旧功能差异审计时，对照旧插件发现 `/WorldListTrashCan add [世界名] <数量>` 会修改 `WorldData.<world>.RashMaxCount`，告示牌创建时使用这个运行数据判断数量上限；旧插件 OP 创建告示牌垃圾桶还能绕过数量上限。

重构后 `/blwtc add <世界名> <数量>` 已经会写入 `plugins/BLWorldTrashCan/data/worlds.yml` 的 `worlds.<world>.max-count`，但 `TrashFeature.canCreateMore(...)` 仍只读取 `trash.yml` 的 `world-trash.default-max-count`。结果是命令落盘成功，却没有参与正式创建限制判断。

### 修改

- `WorldTrashRouter` 新增 `getEffectiveMaxTrashCanCount(World, int)`，优先使用 `data/worlds.yml` 的单世界 `max-count`，缺失或小于等于 0 时回退默认上限。
- `WorldTrashRouter.addWorldTrash(...)` 改为按有效上限限制后台登记路径。
- `WorldTrashRouter.addWorldTrash(Block, int, boolean)` 新增 `bypassMaxCount` 参数，用于恢复旧插件 OP 告示牌创建绕过数量上限的行为。
- `TrashFeature.canCreateMore(...)` 改为读取有效上限。
- `TrashFeature.onSignChange(...)` 在最终登记时把 `player.isOp()` 传入路由器，避免前置判断放行但最终保存又被上限拦截。

### 构建

全模块重新编译通过：

```text
core
config
storage
shared-bukkit
platform-legacy
platform-bukkit
platform-paper
platform-folia
plugin-legacy
plugin-bukkit
plugin-paper
plugin-folia
```

继续使用 JDK 21 `jar.exe` 更新四个 dist jar：

```text
dist/BLWorldTrashCan-legacy-1.12.jar       161339 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  162873 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   163056 bytes
dist/BLWorldTrashCan-folia-1.20.jar        218900 bytes
```

静态检查：

```text
Legacy jar 的 WorldTrashRouter 包含 addWorldTrash(Block,int,boolean)
Legacy jar 的 WorldTrashRouter 包含 getEffectiveMaxTrashCanCount(World,int)
Legacy jar 的 TrashFeature 告示牌路径调用 Player.isOp() 后进入三参数 addWorldTrash
Legacy jar 的 WorldTrashRouter class major version = 52
```

### 1.12.2 真实玩家验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
2. 使用 Java 8 启动 `paper-1.12.2-1620.jar`。
3. 启动真实 Forge 1.12.2 客户端实例 `forge-runner-file-a`，玩家 `AIClientAlpha` 连接测试服。
4. RCON 确认 `AIClientAlpha` 在线。
5. 初始 `data/worlds.yml` 为 `world.max-count: 5` 且已有 1 个登记位置。
6. RCON 通过在线 `Player` 对象连续执行 `tp AIClientAlpha ...` 和 `blwtc debugworldtrash AIClientAlpha`。
7. 4 次新增成功后 `debugsummary` 显示世界垃圾桶数量为 5。
8. 第 5 次新增返回失败，数量保持 5。
9. 执行 `blwtc add world 1`，返回上限已调整为 6。
10. 再新增 1 个成功，`debugsummary` 显示数量为 6。
11. 第 7 个新增返回失败，数量保持 6。

结果：

```text
上限 5 -> 第 2/3/4/5 个登记成功
上限 5 -> 第 6 个登记失败
blwtc add world 1 -> 世界 world 垃圾桶上限已调整为 6
上限 6 -> 第 6 个登记成功
上限 6 -> 第 7 个登记失败
data/worlds.yml -> world.max-count: 6，locations 共 6 个
```

本轮未用真实客户端手动放置告示牌；告示牌玩家分支以最终 jar 字节码确认 `Player.isOp()` 会传入三参数 `addWorldTrash`。运行态用真实在线玩家对象覆盖了路由登记和动态上限生效路径。窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-effective-max-20260602-rcon-smoke.log
paper-1.12.2-test-server/ai-blwtc-effective-max-20260602-rcon-client-wait.log
paper-1.12.2-test-server/ai-blwtc-effective-max-20260602-rcon-main.log
paper-1.12.2-test-server/ai-blwtc-effective-max-20260602-rcon-add-then-create.log
paper-1.12.2-test-server/ai-blwtc-effective-max-20260602-latest-before-stop.log
paper-1.12.2-test-server/ai-blwtc-effective-max-20260602-final-latest.log
客户端自动化测试工作区/runs/20260602-blwtc-effective-max-live-player/control/client-task.properties
客户端自动化测试工作区/runs/20260602-blwtc-effective-max-live-player/control/client-response.properties
客户端自动化测试工作区/runs/20260602-blwtc-effective-max-live-player/logs/forge-latest-before-stop.log
```

## 2026-06-03 玩家掉落 owner 追踪补齐

### 背景

继续审计旧配置 `Set.PersonalTrashCan.NoWorldTrashCanEnterPersonalTrashCan` 时发现，新架构把它迁移到 `personal-trash.track-player-dropped-items`，但普通清理只读取 `ItemSnapshot.ownerUuid`。Paper/Folia 可以通过 PDC 读取 owner，Legacy 1.12 和 Bukkit 1.13-1.15 没有 PDC，因此玩家主动丢弃物在“没有可用世界垃圾桶”时会退到公共垃圾桶，不能按旧语义优先进入个人垃圾桶。

旧插件该功能本身要求高版本持久 PDC，低版本无法做到“重启后仍有效”。本轮补齐的是当前运行态内的短期 owner 追踪：玩家丢弃事件记录 `Item UUID -> Player UUID`，普通清理和损坏回收都可以读取；过期或路由完成后立即释放，避免长期占用内存。

### 修改

- 新增 `DropOwnerTracker`，保存短期掉落物归属和过期时间。
- `TrashFeature` 玩家主动丢弃时继续调用平台 PDC 标记，同时把掉落实体写入 `DropOwnerTracker`。
- 追踪时间至少 60 秒；如果自动清理间隔更长，则覆盖一轮清理间隔再多 5 秒。
- `CleanupFeature` 和 `FoliaRegionCleanupFeature` 生成 `ItemSnapshot` 后，如果 PDC owner 为空，则用短期 owner 记录补齐。
- 成功路由或删除后移除 owner 记录。
- 四个平台入口共享同一个 `DropOwnerTracker`。
- 四个平台 `debugdrop` 测试命令在生成掉落物后设置 `pickupDelay=200`，避免测试夹具把物品生成在玩家脚下后被立即捡回，导致清理扫描不到实体。

### 构建

全模块重新编译通过：

```text
core
config
storage
shared-bukkit
platform-legacy
platform-bukkit
platform-paper
platform-folia
plugin-legacy
plugin-bukkit
plugin-paper
plugin-folia
```

继续使用 JDK 21 `jar.exe` 从 staging 目录更新四个 dist jar：

```text
dist/BLWorldTrashCan-legacy-1.12.jar       164935 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  166472 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   166643 bytes
dist/BLWorldTrashCan-folia-1.20.jar        222821 bytes
```

静态检查：

```text
四个 jar 均包含 pixeltech/bluenine/blworldtrashcan/bukkit/trash/DropOwnerTracker.class
Legacy DropOwnerTracker major version = 52
Legacy BLWorldTrashCanLegacyPlugin major version = 52
Paper BLWorldTrashCanPlugin major version = 61
FoliaRegionCleanupFeature major version = 61，并包含 DropOwnerTracker / snapshotWithTrackedOwner 调用
Legacy debugDrop 字节码包含 Item.setPickupDelay(int)
```

### 1.12.2 真实玩家验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份并部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
2. 临时备份并修改 `plugins/BLWorldTrashCan/data/worlds.yml`，让 `world.locations: []`，模拟当前世界没有可用世界垃圾桶。
3. 启动 Java 8 Paper 1.12.2。
4. 启动真实 Forge 1.12.2 客户端实例 `forge-runner-file-a`，玩家 `AIClientAlpha` 连接测试服。
5. RCON 确认 `AIClientAlpha` 在线。
6. 先执行失败复现：未设置 `pickupDelay` 的 debug 掉落物会被玩家立即捡回，`/blwtc clear` 扫不到掉落实体；据此补充 debug 夹具。
7. 重新打包部署后执行正向用例：`debugdrop AIClientAlpha STONE 2 owner -> clear -> debugsummary/stats`。
8. 执行负向对照：`debugdrop AIClientAlpha COBBLESTONE 3 -> clear -> debugsummary/stats`。
9. 停止客户端和服务端，恢复原 `data/worlds.yml`。

结果：

```text
正向 owner 掉落:
blwtc clear -> 回收物品 2，个人路由 1 个堆叠
debugsummary -> 个人垃圾桶物品 2，公共垃圾桶物品 0

负向未标记掉落:
blwtc clear -> 回收物品 3，公共路由 1 个堆叠
debugsummary -> 公共垃圾桶物品 3，个人垃圾桶物品仍为 2

恢复后 data/worlds.yml -> world.max-count: 6，locations 共 6 个
```

窄匹配 `\[BLWorldTrashCan\].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误)` 未发现 BLWorldTrashCan 自身异常。

证据：

```text
paper-1.12.2-test-server/ai-blwtc-drop-owner-20260603-rcon-main.log
paper-1.12.2-test-server/ai-blwtc-drop-owner-20260603-latest-before-redeploy.log
paper-1.12.2-test-server/ai-blwtc-drop-owner-20260603-retry-rcon-main.log
paper-1.12.2-test-server/ai-blwtc-drop-owner-20260603-retry-rcon-control.log
paper-1.12.2-test-server/ai-blwtc-drop-owner-20260603-retry-final-latest.log
客户端自动化测试工作区/runs/20260603-blwtc-drop-owner-live-player/control/client-response.properties
客户端自动化测试工作区/runs/20260603-blwtc-drop-owner-live-player/logs/forge-latest-after-failed-drop.log
客户端自动化测试工作区/runs/20260603-blwtc-drop-owner-live-player-retry/control/client-response.properties
客户端自动化测试工作区/runs/20260603-blwtc-drop-owner-live-player-retry/logs/forge-latest-before-stop.log
```

## 2026-06-03 旧功能补齐最终收口

本轮在 `补齐玩家掉落物归属追踪` 提交完成后，继续做最终收口审计和远端交付。

### 远端提交

```text
git status --short --branch -> ## master...origin/master [ahead 1]
git log -3 --oneline -> 8180229 补齐玩家掉落物归属追踪
git push origin master -> e0b5d69..8180229 master -> master
git status --short --branch -> ## master...origin/master
```

### 功能缺口复核

复核范围：

```text
README.md
docs/重构执行记录.md
旧插件 WorldListTrashCan/src/main/resources/config.yml
旧插件 WorldListTrashCan/src/main/resources/plugin.yml
BukkitLegacyConfigMigrator
四个平台 plugin.yml / 默认配置资源
```

结论：

```text
迁移器 recordUnsupportedKeys 当前为空，没有继续列出需要人工确认的旧配置字段。
旧配置里的公共垃圾桶、世界垃圾桶、个人垃圾桶、实体清理、通知、实体限制、聊天/命令限频、防丢弃和简单优化字段均已在新拆分配置中承接。
旧 plugin.yml 的主命令、WTC/wtc 别名、旧权限节点均已在四个平台产物保留兼容入口或对应权限。
本轮没有发现新的“旧功能未实现”项。
```

仍需诚实保留的边界不是当前旧功能缺口，而是平台或外部前置导致的验收限制：

```text
Folia PAPI 变量需要支持 Folia 的 PlaceholderAPI 前置，本机 PlaceholderAPI 2.11.6 不能作为验收前置。
Folia BossBar / Title / Sound 等玩家可见通知已有代码路径和后台 smoke，但尚未做 Folia 客户端视觉验收。
Folia 密集实体限制为避免跨 region 查询，只覆盖当前 chunk；这是明确的 Folia 安全取舍。
world-trash.allow-load-unloaded-chunks 默认 false 会改变旧插件远处箱子尽量写入的行为；这是为避免强制加载区块导致卡顿的性能保护。
```

## 2026-06-03 Paper/Folia PDC 归属标记叠加修复

用户指出 PDC 可能导致物品无法正常叠加。复核当前实现后确认：此前 Paper/Folia 的 `markOwner` 把 `player_uuid` 写进 `ItemStack` 的 `ItemMeta` PDC，Bukkit 叠加判断会比较物品元数据，因此不同 owner 或带/不带 owner 的同类物品可能无法正常叠加，且被写入垃圾桶后仍可能带隐藏标记。

### 修复内容

```text
ItemSnapshotMapper 新增 toSnapshot(Item) 和 sanitizeForStorage(ItemStack) 默认方法。
PaperItemSnapshotMapper / FoliaItemSnapshotMapper:
  - markOwner 改为写入掉落实体 Item 的 PDC。
  - toSnapshot(Item) 优先读取掉落实体 PDC。
  - 兼容读取旧 ItemStack PDC，避免旧掉落物归属丢失。
  - sanitizeForStorage 会删除旧 ItemStack 上的 player_uuid PDC。
GlobalTrashService / PersonalTrashService / WorldTrashRouter:
  - 容量判断和入库写入前统一使用 sanitizeForStorage。
TrashFeature:
  - 真实玩家丢弃和 debugdrop owner 共用 trackDroppedItem，保证测试夹具也走平台 owner 标记。
```

这样 Paper/Folia 新生成的 owner 标记只存在于掉落实体，不污染物品本身；进入世界、公共、个人垃圾桶前会清理旧版本残留在物品元数据上的内部标记。

### 构建和静态检查

```text
javac 全模块通过:
core -> config -> storage -> shared-bukkit -> platform-legacy -> platform-bukkit -> platform-paper -> platform-folia -> plugin-legacy -> plugin-bukkit -> plugin-paper -> plugin-folia

CorePolicySelfTest passed

dist/BLWorldTrashCan-legacy-1.12.jar       165755 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  167298 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   167766 bytes
dist/BLWorldTrashCan-folia-1.20.jar        223980 bytes

Legacy BLWorldTrashCanLegacyPlugin major version = 52
PaperItemSnapshotMapper:
  markOwner 调用 org.bukkit.entity.Item.getPersistentDataContainer()
  未出现 Item.setItemStack 调用
  sanitizeForStorage 调用 ItemMeta.getPersistentDataContainer().remove(...)
FoliaItemSnapshotMapper:
  markOwner 调用 org.bukkit.entity.Item.getPersistentDataContainer()
  未出现 Item.setItemStack 调用
  sanitizeForStorage 调用 ItemMeta.getPersistentDataContainer().remove(...)
```

### 1.20.4 后台 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.20.4-test-server
```

步骤：

1. 备份旧 `plugins/BLWorldTrashCan-paper-1.16-1.20.jar` 为 `BLWorldTrashCan-paper-1.16-1.20.jar.ai-backup-pdc-sanitize-20260603`。
2. 部署新 `dist/BLWorldTrashCan-paper-1.16-1.20.jar`。
3. 使用 JDK 21 启动 Paper 1.20.4。
4. RCON 执行 `plugins`、`blwtc platform`、`blwtc stats`、`blwtc clear`、`blwtc debugstock`。
5. RCON `stop` 正常停服，保存 `latest.log` 快照。

结果：

```text
plugins -> BLWorldTrashCan / PlaceholderAPI 已加载
blwtc platform -> 当前平台 paper-1.16-1.20，item-pdc-tag 启用
blwtc stats -> 正常返回公共垃圾桶页数、库存、下次清理剩余秒数
blwtc clear -> 清理完成，世界 3，无异常
blwtc debugstock -> 正常返回公共垃圾桶库存
窄匹配 [BLWorldTrashCan].*(ERROR|WARN|Exception|NoSuch|Unsupported|Could not|failed|失败|错误) 未发现插件自身异常
```

本轮没有 1.20 真实玩家在线，因此未跑 `debugdrop` 玩家掉落实体链路；实体 PDC 迁移和入库清理由最终 jar 字节码证明，服务端加载与命令链路由 1.20.4 RCON smoke 证明。

证据：

```text
paper-1.20.4-test-server/ai-blwtc-pdc-sanitize-paper1204-20260603-console.log
paper-1.20.4-test-server/ai-blwtc-pdc-sanitize-paper1204-20260603-error.log
paper-1.20.4-test-server/ai-blwtc-pdc-sanitize-paper1204-20260603-rcon-wait.log
paper-1.20.4-test-server/ai-blwtc-pdc-sanitize-paper1204-20260603-rcon-main.log
paper-1.20.4-test-server/ai-blwtc-pdc-sanitize-paper1204-20260603-rcon-stop.log
paper-1.20.4-test-server/ai-blwtc-pdc-sanitize-paper1204-20260603-final-latest.log
```

## 2026-06-03 bStats 统计合规移植

用户要求服主不应有能力关闭 bStats。该要求不能按字面实现：bStats 官方 Metrics 模板明确要求保留 opt-out，不能移除、隐藏或绕过 `plugins/bStats/config.yml` 的全局关闭机制，否则会违反 bStats 使用规则。因此本轮按合规方式移植旧插件统计，不实现强制不可关闭统计。

### 实现内容

```text
从旧插件复制官方 Metrics.java，仅修改 package 为 pixeltech.bluenine.blworldtrashcan.bukkit.bstats。
新增 BStatsMetricsService，沿用旧 serviceId 24350。
保留旧统计图表:
  players
  servers
  players_and_servers
新增 platform 图表，用于区分 legacy/bukkit/paper/folia 四个重构产物。
四个平台入口 onEnable 启动 bStats，onDisable 调用 Metrics.shutdown()。
不新增插件私有统计开关，不新增第二套统计配置，只使用 bStats 官方全局配置。
```

### 构建和静态检查

```text
javac 全模块通过:
shared-bukkit 追加 Metrics.java 和 BStatsMetricsService.java 后，继续编译 core/config/storage/platform-*/plugin-*。

CorePolicySelfTest passed

dist/BLWorldTrashCan-legacy-1.12.jar       191651 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  193190 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   193660 bytes
dist/BLWorldTrashCan-folia-1.20.jar        249860 bytes

四个 jar 均包含:
  pixeltech/bluenine/blworldtrashcan/bukkit/bstats/Metrics.class
  pixeltech/bluenine/blworldtrashcan/bukkit/bstats/BStatsMetricsService.class

四个平台入口字节码均包含:
  BStatsMetricsService.start
  Metrics.shutdown

主类 class major:
  legacy 52
  bukkit 52
  paper 61
  folia 61
```

### 1.20.4 后台 smoke

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.20.4-test-server
```

步骤：

1. 部署新 `dist/BLWorldTrashCan-paper-1.16-1.20.jar` 到测试服 `plugins`。
2. 用 JDK 21 启动 Paper 1.20.4。
3. RCON 执行 `plugins`、`blwtc platform`、`blwtc stats`。
4. RCON `stop` 正常停服，保存 `latest.log` 快照。
5. 检查 `plugins/bStats/config.yml`。

结果：

```text
plugins -> BLWorldTrashCan / PlaceholderAPI 已加载
blwtc platform -> 当前平台 paper-1.16-1.20，item-pdc-tag 启用
blwtc stats -> 正常返回公共垃圾桶页数、库存、下次清理剩余秒数
plugins/bStats/config.yml -> 官方全局配置存在，enabled: true
窄匹配 BLWorldTrashCan / bStats 异常未发现；仅有测试服离线模式和 Paper timings 既有警告
```

证据：

```text
paper-1.20.4-test-server/ai-blwtc-bstats-20260603-032536-console.log
paper-1.20.4-test-server/ai-blwtc-bstats-20260603-032633-console.log
paper-1.20.4-test-server/ai-blwtc-bstats-20260603-rcon-main-2.log
paper-1.20.4-test-server/ai-blwtc-bstats-20260603-rcon-stop-2.log
paper-1.20.4-test-server/ai-blwtc-bstats-20260603-final-latest-2.log
```

## 2026-06-03 个人垃圾桶自动回收提示

用户要求公共垃圾桶已有提示时，个人垃圾桶也应在自动回收时提示在线玩家。单个掉落实体被仙人掌、岩浆、虚空等损坏回收时逐条提示；后台扫地或 `/blwtc clear` 这类批量清理时合并为一次提示。批量提示最多完整显示 `trash.yml` 中配置的条目数，超过后使用语言文件的省略格式。

### 实现内容

```text
TrashConfig.PersonalTrashConfig 新增 notify.enabled 与 notify.max-display-items。
四个平台默认 trash.yml 新增 personal-trash.notify 中文注释配置。
四个平台 message_zh/message_zh_TW/message_en/message_es 新增 personal-trash.recycle 文案。
ServerPlatform 新增 sendMessage(UUID, message)，Folia 使用玩家实体 scheduler 发送。
PersonalTrashService 新增 notifySingle / notifyBatch，并按显示名合并同名物品数量。
TrashFeature 在损坏回收进入 PERSONAL_TRASH 时发送 single 提示。
CleanupFeature 与 FoliaRegionCleanupFeature 在本轮清理结束后按玩家发送 batch 提示。
```

默认 `trash.yml` 配置片段：

```yaml
personal-trash:
  notify:
    enabled: true
    max-display-items: 3
```

默认 `message_*.yml` 消息片段：

```yaml
personal-trash:
  recycle:
    single: "{prefix}&a已回收到个人垃圾桶: {items}"
    batch: "{prefix}&a本次清理已回收到个人垃圾桶: {items}"
    list: "&7[{items}&7]"
    separator: "&7, "
    item: "&f{name}&7*&f{amount}"
    item-single: "&f{name}"
    ellipsis: "&7..."
```

### 构建和静态检查

```text
javac 全模块通过:
core -> config -> storage -> shared-bukkit -> platform-legacy -> platform-bukkit -> platform-paper -> platform-folia -> plugin-legacy -> plugin-bukkit -> plugin-paper -> plugin-folia

CorePolicySelfTest passed

dist/BLWorldTrashCan-legacy-1.12.jar       197741 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  199286 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   199758 bytes
dist/BLWorldTrashCan-folia-1.20.jar        257818 bytes

四个 jar 均包含 PersonalTrashService.class、trash.yml 的 max-display-items: 3、message_zh.yml 的“本次清理已回收到个人垃圾桶”。
主类 class major: legacy/bukkit=52，paper/folia=61。
```

### 1.12.2 真实客户端验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份并部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
2. 临时把测试服 `trash.yml` 设置为 `personal-trash.notify.enabled=true`、`max-display-items=3`、`damage-recovery.mode=personal-trash`。
3. 临时清空 `data/worlds.yml` 的世界垃圾桶登记，避免世界垃圾桶优先路由；同时临时补齐 `messages/message_zh.yml` 的 `personal-trash.recycle`，验证提示格式可配置。
4. 启动 Paper 1.12.2，RCON 验证 `plugins/platform/stats/debugstock/list`。
5. `client-1.12.2/PCL/LatestLaunch.bat` 本轮只启动到主菜单，因此复用同一 Java 启动行追加 `--server 127.0.0.1 --port 25565`，让真实玩家 `babyZiXuan` 进入测试服。
6. RCON 执行 `debugdamage babyZiXuan STONE 2`，验证单个损坏回收提示。
7. RCON 执行 3 个 `debugdrop ... owner` 后 `/blwtc clear`，验证未超过上限时完整展示。
8. RCON 执行 4 个 `debugdrop ... owner` 后 `/blwtc clear`，验证超过上限时追加 `...`。
9. 停服后恢复临时 `config.yml`、`trash.yml`、`messages/message_zh.yml` 和 `data/worlds.yml`，保留完整日志。

结果：

```text
客户端聊天:
[BLWorldTrashCan] 已回收到个人垃圾桶: [STONE*2]
[BLWorldTrashCan] 本次清理已回收到个人垃圾桶: [STONE*5, COBBLESTONE*30, DIRT]
[BLWorldTrashCan] 本次清理已回收到个人垃圾桶: [STONE*5, COBBLESTONE*30, DIRT, ...]

RCON:
debugdamage -> 损坏回收测试成功
debugsummary -> 个人垃圾桶物品 2，公共/世界 0
三类 debugdrop + clear -> 回收物品 36，个人 3，公共/世界 0
四类 debugdrop + clear -> 回收物品 40，个人 4，公共/世界 0

服务端:
debugDamageRecovery player=babyZiXuan, material=STONE, amount=2, recovered=true
[Cleanup] worlds=1, itemsRouted=36 ...
[Cleanup] worlds=1, itemsRouted=40 ...
```

证据：

```text
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-rcon-smoke.log
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-rcon-client-wait.log
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-rcon-cases.log
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-rcon-stop.log
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-041425-console.log
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-041425-error.log
paper-1.12.2-test-server/ai-blwtc-personal-notify-20260603-final-latest.log
client-1.12.2/logs/ai-blwtc-personal-notify-client-20260603-041613-out.log
client-1.12.2/logs/ai-blwtc-personal-notify-client-autoserver-20260603-041859-out.log
client-1.12.2/logs/ai-blwtc-personal-notify-client-autoserver-20260603-041859-err.log
```

### 工作流回写

本轮发现 `client-1.12.2/PCL/LatestLaunch.bat` 可能只启动到主菜单，不一定自动进入测试服。已回写根目录测试检查流程：如果客户端未自动进服，可复用 `LatestLaunch.bat` 中的 Java 启动行追加 `--server 127.0.0.1 --port 25565`，并用 RCON `list` 与客户端 stdout/stderr 证明真实玩家在线。

## 2026-06-03 reload 补齐缺失默认配置

用户反馈：删除多个 yml 配置文件后执行 `wtc reload`，插件不会把缺失的默认 yml 重新生成。这个行为会让服主误删配置后只能重启服务端才能恢复，属于 reload 自愈缺口。

### 根因

四个平台入口在 `onEnable()` 中会调用 `saveDefaultConfigs()`，但 `reloadPlugin()` 只执行 `reloadConfig()`、读取拆分配置和刷新功能模块。也就是说，默认资源释放只发生在启动阶段，运行中删除 `cleanup.yml`、`trash.yml`、`messages/*.yml` 等文件后，reload 直接读取缺失文件，自然不会补回。

### 修复内容

```text
BLWorldTrashCanLegacyPlugin.reloadPlugin()
BLWorldTrashCanBukkitPlugin.reloadPlugin()
BLWorldTrashCanPlugin.reloadPlugin()
BLWorldTrashCanFoliaPlugin.reloadPlugin()
```

四个平台 `reloadPlugin()` 开头统一先调用 `saveDefaultConfigs()`，再执行 `reloadConfig()`、`loadConfigBundle()`、`messageService.reload(...)` 和 `featureRegistry.reloadAll()`。

`saveDefaultConfigs()` 内部仍只调用 `saveDefaultConfig()` 和 `saveResourceIfMissing(...)`，因此只会补齐缺失文件，不会覆盖服主已有配置。

### 构建和静态检查

本机 `mvn` 仍不在 PATH，本轮只改四个平台入口类，使用当前已验收 staging 目录增量编译入口 class，并用 JDK 21 `jar.exe` 更新四个 dist jar。

```text
build/compile-reload-self-heal-20260603-043837

dist/BLWorldTrashCan-legacy-1.12.jar       197746 bytes
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar  199298 bytes
dist/BLWorldTrashCan-paper-1.16-1.20.jar   199767 bytes
dist/BLWorldTrashCan-folia-1.20.jar        257827 bytes

主类 class major:
legacy/bukkit=52
paper/folia=61

javap 确认四个平台 reloadPlugin() 字节码均为:
saveDefaultConfigs()
reloadConfig()
loadConfigBundle()
```

Java 8 目标编译时只出现 JDK 对 `--release 8` 的常规过时选项警告。

### 1.12.2 后台验证

测试服：

```text
C:\Users\pc\Desktop\ai开发插件\paper-1.12.2-test-server
```

步骤：

1. 备份并部署新 `dist/BLWorldTrashCan-legacy-1.12.jar`。
2. 启动 Paper 1.12.2，RCON 执行 `plugins` 和 `wtc platform`，确认 `BLWorldTrashCan` 启用且平台为 `legacy-1.12`。
3. 将以下 12 个 yml 移动到测试备份目录，模拟用户删除配置文件：

```text
config.yml
platform.yml
cleanup.yml
trash.yml
notify.yml
entity-limits.yml
protections.yml
messages/message_zh.yml
messages/message_zh_TW.yml
messages/message_en.yml
messages/message_es.yml
data/worlds.yml
```

4. RCON 执行 `wtc reload`。
5. 检查 12 个缺失 yml 是否全部重新出现。
6. 将测试前原始配置复制回数据目录，再次执行 `wtc reload`，避免污染后续测试。
7. RCON 正常 `stop` 停服，保留完整日志。

结果：

```text
missing_before=config.yml,platform.yml,cleanup.yml,trash.yml,notify.yml,entity-limits.yml,protections.yml,messages/message_zh.yml,messages/message_zh_TW.yml,messages/message_en.yml,messages/message_es.yml,data/worlds.yml
restored_after=config.yml,platform.yml,cleanup.yml,trash.yml,notify.yml,entity-limits.yml,protections.yml,messages/message_zh.yml,messages/message_zh_TW.yml,messages/message_en.yml,messages/message_es.yml,data/worlds.yml
still_missing=

>>> wtc reload
[BLWorldTrashCan] BLWorldTrashCan 已重载。
```

已确认测试后原始 yml 已复制回去并二次 reload 成功。

### 证据

```text
paper-1.12.2-test-server/ai-blwtc-reload-self-heal-20260603-044107-console.log
paper-1.12.2-test-server/ai-blwtc-reload-self-heal-20260603-044219-rcon-pre.log
paper-1.12.2-test-server/ai-blwtc-reload-self-heal-20260603-044244-rcon-main.log
paper-1.12.2-test-server/ai-blwtc-reload-self-heal-20260603-044306-rcon-restore.log
paper-1.12.2-test-server/ai-blwtc-reload-self-heal-20260603-044320-rcon-stop.log
paper-1.12.2-test-server/plugins/BLWorldTrashCan/ai-reload-self-heal-missing-backup-20260603-044244/
paper-1.12.2-test-server/plugins/BLWorldTrashCan/ai-reload-self-heal-restored-defaults-20260603-044244/
```

### 测试环境噪声

测试服仍存在其他插件的 MySQL 连接失败日志，例如 AttributePlusViewer/Druid 连接 `localhost:3306/test` 被拒绝；本轮验证目标是 BLWorldTrashCan 的 reload 自愈，RCON 返回和文件检查均已证明该功能生效。未发现 BLWorldTrashCan 启动失败、`UnsupportedClassVersionError`、`NoSuchMethodError`、`NoSuchFieldError`。

## 2026-06-03 合并 notify.yml 到 cleanup.yml

用户认为清理倒计时通知完全是扫地功能的不同表现，不应拆成独立 `notify.yml`。用户同时明确当前仍在开发阶段，不需要兼容已经生成过的旧 `notify.yml`。

### 实现内容

```text
cleanup.yml 新增 notify.* 配置区。
ConfigBundleLoader 改为从 cleanup.yml 读取 notify.chat/actionbar/bossbar/title/sound/command。
四个平台入口 saveDefaultConfigs() 不再释放 notify.yml。
四个平台 loadConfigBundle() 不再读取 notify.yml。
四个平台默认资源删除 notify.yml。
BukkitLegacyConfigMigrator 仍把旧 WorldListTrashCan config.yml 中的通知字段迁移到 cleanup.yml 的 notify.*。
```

新的配置结构示例：

```yaml
notify:
  chat:
    enabled: true
    messages:
      - "60;&d还剩下 60 秒清理物品和实体"
  bossbar:
    enabled: false
    messages:
      - "30;&d&l即将清理物品和实体;SOLID;RED"
```

### 验证结果

构建和 1.12.2 测试服已覆盖：

```text
1. 四个 dist jar 内不再包含 notify.yml。
2. 四个 dist jar 内 cleanup.yml 包含 notify.chat 与 notify.bossbar。
3. reload 自愈只补回 cleanup.yml，不再生成 notify.yml。
4. /wtc reload、/wtc platform、/wtc stats、/wtc clear 正常返回。
5. Legacy 产物和共享配置类 class major 仍符合 Java 8。
```

构建产物：

```text
build/dist-cleanup-notify-merge-20260603-052final/BLWorldTrashCan-bukkit-1.13-1.15.jar
build/dist-cleanup-notify-merge-20260603-052final/BLWorldTrashCan-folia-1.20.jar
build/dist-cleanup-notify-merge-20260603-052final/BLWorldTrashCan-legacy-1.12.jar
build/dist-cleanup-notify-merge-20260603-052final/BLWorldTrashCan-paper-1.16-1.20.jar
```

1.12.2 测试服验证步骤：

```text
1. 部署新的 Legacy jar 到 paper-1.12.2-test-server/plugins/BLWorldTrashCan-legacy-1.12.jar。
2. 启动测试服，确认 /plugins 中 BLWorldTrashCan 启用。
3. 临时移走测试服数据目录中的 cleanup.yml 和 notify.yml。
4. RCON 执行 wtc reload。
5. 检查 plugins/BLWorldTrashCan/cleanup.yml 已补回且包含 notify:。
6. 检查 plugins/BLWorldTrashCan/notify.yml 未被补回。
7. RCON 执行 wtc platform、wtc stats、wtc clear。
8. RCON stop 正常停服，保留完整测试日志。
```

结果：

```text
cleanup_exists=True
cleanup_has_notify=True
notify_exists=False

>>> wtc reload
[BLWorldTrashCan] BLWorldTrashCan 已重载。

>>> wtc clear
[BLWorldTrashCan] 清理完成: 世界 1, 回收物品 3, 移除物品 0, 移除实体 20。
```

测试日志：

```text
paper-1.12.2-test-server/ai-blwtc-cleanup-notify-merge-20260603-045754-console.log
paper-1.12.2-test-server/ai-blwtc-cleanup-notify-merge-20260603-045754-error.log
paper-1.12.2-test-server/ai-blwtc-cleanup-notify-merge-20260603-045834-rcon-pre.log
paper-1.12.2-test-server/ai-blwtc-cleanup-notify-merge-20260603-0500-reload-new-structure.log
paper-1.12.2-test-server/ai-blwtc-cleanup-notify-merge-20260603-0501-rcon-post.log
paper-1.12.2-test-server/ai-blwtc-cleanup-notify-merge-20260603-050xx-stop.log
```

测试环境噪声：控制台仍有其他插件的 Druid/MySQL 连接失败日志，原因是测试机未开放 `localhost:3306/test`；本轮 error log 为空，未发现 BLWorldTrashCan 自身异常。

## 2026-06-05 接入 PrismaticAPI 统一 RGB 消息

用户确认 PrismaticAPI 可以纳入通用插件开发工作流，后续基础版本会按开源要求发行。因此本轮以 PrismaticAPI 作为 BLWorldTrashCan 的统一 RGB 富文本渲染库，目标是让 1.16.5+ 的 Spigot/Paper/Folia 都能使用 RGB 消息，同时让 1.12.2 和 1.13-1.15 自动降级。

### 实现内容

```text
新增 RichTextRenderer 作为唯一富文本渲染入口。
PrismaticAPI 版本固定为 1.5.2。
四个平台最终 jar 内置 PrismaticAPI，并 relocation 到 pixeltech.bluenine.blworldtrashcan.libs.croabeast。
shade 过滤 PrismaticAPI 自带 plugin.yml，避免覆盖本插件 plugin.yml。
普通 Chat、可点击 Chat、ActionBar、Title、BossBar 标题、GUI 标题和平台层 sendMessage 都改走 RichTextRenderer。
stripColor 也统一走 RichTextRenderer，保证命令通知这类需要去色的路径不会散落 ChatColor 调用。
```

BossBar 需要保留一个明确边界：BossBar 标题文本可以走 RGB；BossBar 条颜色仍然只能是 Bukkit `BarColor` 枚举，不支持任意 `#RRGGBB`。

### 构建与包内检查

构建日志：

```text
build/rgb-prismatic-20260605-032221/mvn-package.log
build/rgb-prismatic-20260605-032221/jar-inspection.txt
build/rgb-prismatic-20260605-032221/class-major.txt
```

检查结论：

```text
四个平台 jar 均包含 pixeltech/bluenine/blworldtrashcan/libs/croabeast/prismatic/PrismaticAPI.class。
四个平台 jar 均不残留原始 me/croabeast 类路径。
四个平台 jar 的 plugin.yml 仍为 BLWorldTrashCan。
Legacy/Bukkit 主类和 PrismaticAPI 相关类 class major 为 52，Paper/Folia 主类为 61。
```

最终产物已同步到 `dist`：

```text
dist/BLWorldTrashCan-legacy-1.12.jar
dist/BLWorldTrashCan-bukkit-1.13-1.15.jar
dist/BLWorldTrashCan-paper-1.16-1.20.jar
dist/BLWorldTrashCan-folia-1.20.jar
```

### 运行验证

Paper 1.20.4：

```text
paper-1.20.4-test-server/ai-blwtc-rgb-prismatic-20260605-032221-paper1204-rcon-main.log
paper-1.20.4-test-server/ai-blwtc-rgb-prismatic-20260605-032221-paper1204-rconhex-rcon-main.log
paper-1.20.4-test-server/ai-blwtc-rgb-prismatic-20260605-032221-paper1204-timer-latest.log
```

结论：`blwtc platform/reload/stats/clear` 正常，`rgb-message: 启用`。RCON 响应里出现 `§x§f§f§3§3§6§6`，证明 `&#ff3366` 已被 PrismaticAPI 转为 1.16+ RGB 颜色码。测试用 `cleanup.yml` 和 `message_zh.yml` 已归档，测试服配置已恢复。

Paper 1.13.2：

```text
paper-1.13.2-test-server/ai-blwtc-rgb-prismatic-20260605-032221-bukkit113-final-rcon-main.log
paper-1.13.2-test-server/ai-blwtc-rgb-prismatic-20260605-032221-bukkit113-final-latest.log
```

结论：`reload/stats/clear` 正常，`rgb-message: 禁用`，符合低版本降级预期。

Paper 1.12.2：

```text
paper-1.12.2-test-server/ai-blwtc-rgb-prismatic-20260605-032221-legacy112-rcon-main.log
paper-1.12.2-test-server/ai-blwtc-rgb-prismatic-20260605-032221-legacy112-latest.log
```

结论：`reload/stats/clear` 正常，`rgb-message: 禁用`。latest 中存在其它插件 MySQL/CoreBridge/EasyLib/MythicMobs 噪声，但未发现 BLWorldTrashCan 或 PrismaticAPI 启用异常。

Folia 1.20.1：

```text
folia-1.20.1-test-server/ai-blwtc-rgb-prismatic-20260605-032221-folia1201-console.log
folia-1.20.1-test-server/ai-blwtc-rgb-prismatic-20260605-032221-folia1201-latest.log
folia-1.20.1-test-server/ai-blwtc-rgb-prismatic-20260605-032221-folia1201-rcon-main.log
```

结论：插件启用成功，`rgb-message: 启用`，Folia 定时清理正常输出三轮 `[FoliaCleanup]`。该测试服 RCON 对 `blwtc ...` 和 `stop` 都返回 `Error executing ... (null)`，因此本轮 Folia 只证明启动和后台定时链路，命令链路需要后续用 console/latest 或真实入口补证据。

### 工作流回写

根目录工作流补充 RGB 消息标准：

```text
1. 需要 1.16.5+ RGB 消息时，默认优先评估 PrismaticAPI。
2. 内置 PrismaticAPI 前必须确认发行策略满足 GPL-3.0-only 要求。
3. 所有正式消息必须走统一富文本入口，不允许散落 ChatColor.translateAlternateColorCodes。
4. 测试必须包含 1.16.5+ RGB 证据、低版本降级证据、shade/relocation 包内检查和 Folia 入口证据。
5. Paper 控制台可能剥离颜色，不能只用 latest.log 判断 RGB 是否生效；RCON 原始响应、客户端截图或能看到 `§x` 的证据更可靠。
```

## 2026-06-07 新增 universal 通用总包

用户希望保留当前“四个版本各一个轻量 jar”的架构，同时额外提供一个特殊 jar，满足服主习惯的“同一个 jar 高低版本无缝切换”。本轮新增 `bl-world-trashcan-plugin-universal`，生成 `BLWorldTrashCan-universal.jar`。

### 实现内容

```text
新增 universal 插件入口 BLWorldTrashCanUniversalPlugin。
新增 UniversalCommand、UniversalPlaceholderExpansion、UniversalVaultPaymentService。
universal 主类和命令适配层按 Java 8 编译，避免 1.12.2 Java 8 启动阶段无法加载。
四个平台实现仍保留在各自 platform 模块，universal 只在运行时选择并延迟加载。
FoliaRegionCleanupFeature 和 FoliaEntityLimitFeature 从 plugin-folia 移到 platform-folia，供 Folia 分包和 universal 共同复用。
PrismaticAPI 继续 shade relocation 到 pixeltech.bluenine.blworldtrashcan.libs.croabeast。
plugin.yml 不声明 api-version，并保留 folia-supported: true。
```

运行时选择策略：

```text
Folia: Bukkit.getName()/Bukkit.getVersion() 包含 folia -> folia-1.20
Minecraft 1.12.x -> legacy-1.12
Minecraft 1.13-1.15 或 Java 17 以下 -> bukkit-1.13-1.15
其它 Java 17+ 现代端 -> paper-1.16-1.20
```

本轮曾发现 Paper 1.20.4 上 `Bukkit.getGlobalRegionScheduler` 存在会导致误判 Folia，因此 Folia 检测已改为只看服务端名称和版本文本中的 `folia` 标记。

### 构建与检查

```text
.\build\tools\apache-maven-3.9.9\bin\mvn.cmd -DskipTests package
.\build\tools\apache-maven-3.9.9\bin\mvn.cmd test
```

检查结论：

```text
BLWorldTrashCan-universal.jar 已复制到 dist。
universal 主类 Java 8 加载 smoke 输出 loaded-universal-main。
jar 内 PrismaticAPI relocation 后原始 me/croabeast 类数量为 0。
测试结束后四个测试服已恢复为原轻量 jar，plugins 下没有残留 universal jar。
```

### 四端 console smoke

同一个 `BLWorldTrashCan-universal.jar` 已用控制台 stdin/console 方式验证：

```text
legacy112 -> paper-1.12.2-test-server -> platform=legacy-1.12 -> PASS
bukkit113 -> paper-1.13.2-test-server -> platform=bukkit-1.13-1.15 -> PASS
paper1204 -> paper-1.20.4-test-server -> platform=paper-1.16-1.20 -> PASS
folia1201 -> folia-1.20.1-test-server -> platform=folia-1.20 -> PASS
```

测试日志：

```text
build/universal-console-smoke-summary-20260607-012407.txt
paper-1.12.2-test-server/ai-blwtc-universal-console-20260607-012407-legacy112.log
paper-1.13.2-test-server/ai-blwtc-universal-console-20260607-012407-bukkit113.log
paper-1.20.4-test-server/ai-blwtc-universal-console-20260607-012407-paper1204.log
folia-1.20.1-test-server/ai-blwtc-universal-console-20260607-012407-folia1201.log
```

`legacy112` summary 中的 `ErrorPattern=true` 来自测试服其它前置插件噪声，不是 BLWorldTrashCan 自身异常；同轮日志中 BLWorldTrashCan 正常启用、命令正常、平台识别正确。

### 工作流回写

根目录工作流补充跨版本通用总包标准：

```text
1. 跨版本插件优先保留轻量分版本 jar，同时可以额外交付 universal 总包 jar。
2. universal 主类必须是 Java 8 bootstrap，不直接 import 高版本 Paper/Folia API。
3. 高版本平台实现只能延迟/反射加载，不能让旧端启用阶段提前解析 Java 17 class。
4. Folia 检测必须有真实 Folia 和现代 Paper 对照，不能只看某个调度方法是否存在。
5. universal 必须用同一个 jar 在所有目标测试服跑 smoke，控制台 stdin/PTY 可以作为 RCON 不可靠时的真实入口。
```

## 2026-06-07 真实客户端工作流回归补证

用户明确指出玩家可见功能不能用服务端日志判定通过。本轮把 BLWorldTrashCan 的最终验收依据切回真实客户端工作流：服务端日志、RCON 和后台 smoke 只作为辅助排障，不作为 GUI、聊天提示和玩家入口的最终通过依据。

### 客户端 run

```text
runRoot: C:\Users\pc\Desktop\ai开发插件\客户端自动化测试工作区\runs\20260607-031912-blwtc-client-workflow-regression
玩家: AIClientAlpha
测试服: paper-1.12.2-test-server
客户端: Forge 1.12.2 + AIClientAutomationMod runner-file chat-sequence
命令数量: 30
客户端聊天行数: 99
```

### 客户端证据

```text
control/client-response.properties -> status=PASS, message=chat sequence sent: 30 commands, chatLines=99
client-workflow-assertions.txt -> 全部断言 PASS，screenshotCount=32
logs/client-chat.log -> 客户端收到平台、统计、reload、旧别名、个人垃圾桶提示和调试反馈
logs/client-screen.log -> 多次记录 net.minecraft.client.gui.inventory.GuiChest, slots=90
logs/forge-latest.log -> 客户端真实连接测试服、发送 30 条聊天命令并截图
screenshots/runner_sequence_9.png -> 公共垃圾桶 GUI
screenshots/runner_sequence_10.png -> 个人垃圾桶 GUI
screenshots/runner_sequence_11.png -> 世界黑名单 GUI
screenshots/runner_sequence_12.png -> 公共黑名单 GUI
```

### 断言覆盖

```text
clientResponsePass=PASS
platformVisible=PASS
statsVisible=PASS
reloadVisible=PASS
oldAliasVisible=PASS
globalGuiSeen=PASS
personalGuiSeen=PASS
banGuiSeen=PASS
globalBanGuiSeen=PASS
dropModeVisible=PASS
debugDropVisible=PASS
personalNotifyBatchVisible=PASS
personalNotifySingleVisible=PASS
debugWorldTrashVisible=PASS
debugRouteVisible=PASS
debugSummaryVisible=PASS
screenshotCountEnough=PASS
```

### 结论

BLWorldTrashCan 1.12.2 玩家可见主链路本轮以真实客户端证据判定通过。后台 smoke 和服务端日志仍保留，但只用于解释异常、确认测试夹具动作或辅助排查，不能替代上述客户端证据。

### 工作流回写

根目录工作流已补充硬规则：

```text
1. GUI、聊天提示、Title、ActionBar、BossBar、背包物品、客户端发包变量和渲染效果不能用服务端日志判定通过。
2. 服务端日志、RCON、控制台输出和后台 smoke 只能作为辅助排障。
3. 最终通过依据必须来自真实客户端响应、client-chat.log、client-screen.log、Forge/latest 归档、真实 PNG 截图或同等级客户端证据。
```
